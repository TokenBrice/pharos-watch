# Scripts

## Overview

Operational and CI helper scripts live in `scripts/`, while worker-bound operational tooling that imports `worker/src/**` lives in `worker/scripts/`. Together they support build integrity, smoke checks, data sync, and targeted maintenance tasks.

## Script Inventory

| Script                                                                | Purpose                                                                                                                                                                                                                                     | Inputs                                                                                                                                                                                                                                                                                                                                                                                                                                                      | Output / Side Effects                                                                                                                                                                                                                                                                                                                                                                                             |
| --------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `scripts/maintenance/sync-digests.ts`                                             | Fetch digest archive from an explicit API source before frontend build or CI artifact creation                                                                                                                                              | `--api-url`, `DIGEST_API_URL`, `SMOKE_API_BASE`, or `API_BASE_URL`; optional `DIGEST_API_KEY`; optional `--output`                                                                                                                                                                                                                                                                                                                                          | Writes `data/digests.json` by default, or the requested output path                                                                                                                                                                                                                                                                                                                                               |
| `scripts/maintenance/sync-depeg-events.ts`                                        | Fetch confirmed depeg events from an explicit API source with deterministic slugs (`<symbol-lowercase>-<YYYY-MM-DD>` plus `-up`/`-down` for same-day direction collisions) before frontend build or CI artifact creation                    | `--api-url`, `DEPEG_EVENTS_API_URL`, `SMOKE_API_BASE`, or `API_BASE_URL`; optional `DEPEG_EVENTS_API_KEY` / `SMOKE_API_KEY`; optional `--output`                                                                                                                                                                                                                                                                                                            | Writes `data/depeg-events.json` (committed seed; produces zero static pages until refreshed by CI) sorted `startedAt DESC, id DESC`, confirmed events only                                                                                                                                                                                                                                                        |
| `scripts/ci/check-seo-static.mjs`                                        | Validate static-export SEO/meta/link integrity                                                                                                                                                                                              | `out/` build output                                                                                                                                                                                                                                                                                                                                                                                                                                         | Fails non-zero on SEO/crawlability issues                                                                                                                                                                                                                                                                                                                                                                         |
| `scripts/maintenance/build-world-map-svg.ts`                                      | Generate or check the static Natural Earth world map SVG used by the alt-pegs atlas                                                                                                                                                         | `scripts/data/world-countries-110m.json`, `scripts/data/m49-to-iso2.ts`; pass `--check` to fail on generated-file drift                                                                                                                                                                                                                                                                                                                                     | Writes deterministic `public/maps/world-countries.svg`, or verifies it is current in `check:world-map`                                                                                                                                                                                                                                                                                                            |
| `scripts/ci/classify-deploy-changes.mjs`                                 | Classify whether a push diff requires worker/API validation, Worker promotion, Pages deploy work, or no deploy work                                                                                                                         | `DEPLOY_EVENT_NAME`, `DEPLOY_BASE_SHA`, `DEPLOY_HEAD_SHA`, local git history                                                                                                                                                                                                                                                                                                                                                                                | Emits GitHub Actions outputs such as `deploy_required=true/false`, `worker_changed=true/false`, `worker_promotion_required=true/false`, and `pages_changed=true/false`                                                                                                                                                                                                                                            |
| `scripts/ci/pharos-change-contract.mjs`                                  | Classify the current repo diff into Pharos task families, docs to read/update, checks to consider, warnings, and deploy impact for Codex/Claude hooks, PR comments, or manual agent routing                                                 | Current dirty diff by default; optional `--staged`, `--base-ref`, `--head-ref`, repeated `--file`, `--json`, `--markdown`, or hook modes `--hook=session-start`, `--hook=user-prompt-submit`, `--hook=pre-tool-use`, `--hook=permission-request`, `--hook=post-tool-use`, `--hook=post-tool-batch`, and `--hook=stop`                                                                                                                                       | Prints a text, JSON, or Markdown change contract. In agent-hook mode, `SessionStart` injects routing context and writes a session baseline under `.cache/pharos-agent-hooks/`, prompt/post-tool hooks inject advisory docs/check context, hard-block hooks deny deterministic unsafe operations, and `Stop` asks the agent to continue once when the session delta still needs explicit docs/check consideration. |
| `scripts/ci/check-depeg-operational-integrity.mjs`                       | Operator-run SQL integrity checks for depeg history, replay provenance, replay lifecycle, recovery consistency, and PegScore audit eligibility                                                               | `--print-sql`, or `--database <D1_NAME>` with optional `--remote` for Wrangler D1 execution                                                                                                                                                                                                                                                                                                                                                                  | Prints SQL checks or runs them through Wrangler; exits non-zero when duplicate/overlap/recovery/provenance/replay/audit rows require operator review                                                                                                                                                                                                                                                              |
| `scripts/ci/upsert-github-pr-comment.mjs`                                | Create or update the sticky Pharos Change Contract comment on same-repo pull requests                                                                                                                                                       | `GITHUB_TOKEN`, `GITHUB_REPOSITORY`, `PR_NUMBER`, `COMMENT_FILE`, optional `COMMENT_MARKER`                                                                                                                                                                                                                                                                                                                                                                 | Calls the GitHub issue-comments API to update an existing marker comment or create one when missing                                                                                                                                                                                                                                                                                                               |
| `scripts/maintenance/run-validate-prebuild.mjs`                                   | Run the shared prebuild validate guardrail set with bounded parallel npm-script fan-out                                                                                                                                                     | Command registry from `scripts/lib/validate-contract.mjs`                                                                                                                                                                                                                                                                                                                                                                                                   | Runs dependency/pricing audits, lint/typecheck, import/docs/generated-artifact/data guardrails, and exits non-zero after any failing bounded parallel execution unit                                                                                                                                                                                                                                              |
| `scripts/maintenance/run-noncritical-tests.mjs`                                   | Run the deploy/merge-gate Vitest lane for tests not already owned by critical coverage                                                                                                                                                      | Excludes the critical test file list from `scripts/lib/critical-test-files.mjs`; passes through extra Vitest args such as `--shard=N/3`; in CI appends `--silent=passed-only` unless an explicit `--silent` is supplied (`PHAROS_CI_VITEST_COMPACT=0` opts out)                                                                                                                                                                                             | Runs non-critical Vitest tests so the parallel critical-coverage lane owns the critical test files                                                                                                                                                                                                                                                                                                                |
| `scripts/maintenance/run-critical-coverage.mjs`                                   | Run the critical Vitest coverage suite and critical line-coverage gate                                                                                                                                                                      | Test list from `scripts/lib/critical-test-files.mjs`; `CRITICAL_COVERAGE_*` env handled by `check-critical-coverage.mjs`; in CI appends `--silent=passed-only` unless an explicit `--silent` is supplied (`PHAROS_CI_VITEST_COMPACT=0` opts out)                                                                                                                                                                                                            | Writes `coverage/lcov.info`, then fails non-zero on critical coverage threshold or ratchet regressions                                                                                                                                                                                                                                                                                                            |
| `scripts/maintenance/profile-vitest.mjs`                                          | Run Vitest once with JSON reporting and write a summarized runtime profile                                                                                                                                                                  | Required `--output`; optional `--baseline`; trailing Vitest args after `--`                                                                                                                                                                                                                                                                                                                                                                                 | Writes the requested summary JSON plus the raw `*.vitest.json` report beside it                                                                                                                                                                                                                                                                                                                                   |
| `scripts/maintenance/serve-static-export.mjs`                                     | Serve the built static export locally and proxy `/api/*` plus `/_site-data/*` for pre-deploy browser smoke                                                                                                                                  | `STATIC_EXPORT_ROOT`, `STATIC_EXPORT_HOST`, `STATIC_EXPORT_PORT`, `STATIC_EXPORT_API_BASE`, optional `STATIC_EXPORT_API_KEY` (forwarded as `X-API-Key`), optional `STATIC_EXPORT_SITE_API_BASE`, optional `STATIC_EXPORT_SITE_API_SHARED_SECRET`                                                                                                                                                                                                            | Starts a local HTTP server for the exported app + API/site-data proxy                                                                                                                                                                                                                                                                                                                                             |
| `scripts/maintenance/smoke-api.mjs`                                               | HTTP smoke checks for strict API contract paths                                                                                                                                                                                             | `--base-url`, `--timeout-ms`, `--retry-count`, `--retry-delay-ms`, or `SMOKE_API_BASE` / `API_BASE_URL`, optional `SMOKE_API_KEY`, `SMOKE_API_TIMEOUT_MS`, `SMOKE_API_RETRY_COUNT`, `SMOKE_API_RETRY_DELAY_MS`                                                                                                                                                                                                                                              | Exits non-zero on shape/range/status failures                                                                                                                                                                                                                                                                                                                                                                     |
| `scripts/maintenance/smoke-ops.mjs`                                               | Private post-deploy smoke for the Access-protected ops UI and ops API                                                                                                                                                                       | `SMOKE_OPS_UI_URL`, `SMOKE_OPS_API_BASE`, `SMOKE_OPS_SCOPE=full\|canary`, `OPS_SMOKE_CF_ACCESS_CLIENT_ID`, `OPS_SMOKE_CF_ACCESS_CLIENT_SECRET`                                                                                                                                                                                                                                                                                                              | Exits non-zero on ops-host shell or admin API failures                                                                                                                                                                                                                                                                                                                                                            |
| `scripts/maintenance/smoke-transport.mjs`                                         | HTTP-to-HTTPS edge redirect smoke for the public API hosts                                                                                                                                                                                  | `--api-url`, `--site-api-url`, `--timeout-ms`, or `SMOKE_TRANSPORT_API_URL`, `SMOKE_TRANSPORT_SITE_API_URL`, `SMOKE_TRANSPORT_TIMEOUT_MS`                                                                                                                                                                                                                                                                                                                   | Exits non-zero when `api.pharos.watch` or `site-api.pharos.watch` fail to return `308` with the expected `Location`                                                                                                                                                                                                                                                                                               |
| `scripts/maintenance/smoke-ui.mjs`                                                | Browser smoke check for homepage data state plus either the full local mobile overflow sweep or a narrower live canary route set                                                                                                            | `--url`, `--mode`, `--skip-overflow`, `SMOKE_UI_URL`, `SMOKE_UI_MODE`, optional `SMOKE_UI_BROWSER_CHANNEL`, `SMOKE_UI_BROWSER_EXECUTABLE_PATH`, `SMOKE_UI_WAIT_TIMEOUT_MS`, `SMOKE_UI_RETRY_COUNT`, `SMOKE_UI_RETRY_DELAY_MS`, `SMOKE_UI_OVERFLOW_ROUTES`, `SMOKE_UI_CANARY_ROUTE`, `SMOKE_UI_OVERFLOW_WORKERS`, `SMOKE_UI_OVERFLOW_WAIT_MS`, `SMOKE_UI_OVERFLOW_SETTLE_SAMPLES`, `SMOKE_UI_OVERFLOW_SAMPLE_INTERVAL_MS`, `SMOKE_UI_STYLE_READY_TIMEOUT_MS` | Exits non-zero on homepage outage/empty state or sustained horizontal overflow on the configured local or live route set                                                                                                                                                                                                                                                                                          |
| `scripts/ci/check-worker-import-boundary.mjs`                            | Enforce the shared boundary: `worker/src/**` may not import `src/**`, and `src` / `shared` / `scripts` / `functions` may not import `worker/src/**`                                                                                         | `worker/src/**`, `src/**`, `shared/**`, `scripts/**`, `functions/**`                                                                                                                                                                                                                                                                                                                                                                                        | Exits non-zero with offending import locations                                                                                                                                                                                                                                                                                                                                                                    |
| `scripts/ci/check-shared-cycles.mjs`                                     | Detect circular dependencies inside `shared/`, `worker/src`, and `src` so runtime code does not regress into import cycles                                                                                                                  | `shared/**`, `worker/src/**`, `src/**`, local `tsconfig.json`, `madge`                                                                                                                                                                                                                                                                                                                                                                                      | Exits non-zero when any of those code roots contains a circular dependency                                                                                                                                                                                                                                                                                                                                        |
| `scripts/ci/check-shared-types-imports.mjs`                              | Detect runtime value imports from the broad `@shared/types` barrel so value imports use concrete shared type submodules instead                                                                                                             | TypeScript files under `functions`, `shared`, `src`, `worker/src`, and `scripts`                                                                                                                                                                                                                                                                                                                                                                            | Manual guardrail; exits non-zero on non-type imports from `@shared/types`                                                                                                                                                                                                                                                                                                                                         |
| `scripts/ci/check-unused-code.mjs`                                       | Detect unreferenced internal runtime modules and unused named exports across all runtime surfaces                                                                                                                                           | Runtime source graph across `src`, `shared`, `worker/src`, and `functions`; explicit module/export allowlists for intentional exceptions                                                                                                                                                                                                                                                                                                                    | Exits non-zero with dead-module / unused-export findings                                                                                                                                                                                                                                                                                                                                                          |
| `scripts/ci/check-hotspot-ratchet.mjs`                                   | Prevent enrolled hotspot shells/body modules from regrowing and require generated large-file/function/branch candidates to be enrolled or explicitly waived                                                                                 | `scripts/lib/hotspot-ratchet-baseline.json`, `scripts/lib/hotspot-ratchet-waivers.json`, `scripts/lib/hotspot-ratchet.mjs`, and the current repo source tree                                                                                                                                                                                                                                                                                                | Exits non-zero on enrolled hotspot regressions, missing candidate coverage, or stale baseline/waiver entries                                                                                                                                                                                                                                                                                                      |
| `scripts/ci/check-cron-abort-contract.mjs`                               | Verify leased cron entrypoints accept/pass `AbortSignal` instead of silently dropping timeout/lease-loss cancellation                                                                                                                       | `worker/src/handlers/scheduled/**`, `worker/src/cron/**`, and `scripts/lib/cron-abort-contract-waivers.json`                                                                                                                                                                                                                                                                                                                                                | Exits non-zero when a leased cron callback drops the signal or a cron entrypoint uses `_signal` without an explicit waiver                                                                                                                                                                                                                                                                                        |
| `scripts/ci/check-redemption-backstops.ts`                               | Validate the split redemption-backstop registry through the shared manifest/validator, prevent duplicate IDs across route-family modules, and optionally emit reviewer JSON reports                                                         | `shared/lib/redemption-backstop-configs/*`, `shared/lib/redemption-backstops.ts`, `docs/redemption-backstops.md`, `shared/lib/stablecoins`                                                                                                                                                                                                                                                                                                                  | Exits non-zero on module overlap, route-family drift, unknown IDs, stale coverage-count docs, or validation errors; warnings include active tracked coins without configured routes                                                                                                                                                                                                                               |
| `scripts/maintenance/audit-redemption-registry-parity.ts`                         | Generate or compare deterministic Redemption Backstop registry and static-score parity snapshots for behavior-preserving refactors                                                                                                          | `shared/lib/redemption-backstop-configs/*`, `shared/lib/redemption-backstop-scoring.ts`                                                                                                                                                                                                                                                                                                                                                                     | Exits non-zero when compared snapshots differ                                                                                                                                                                                                                                                                                                                                                                     |
| `scripts/maintenance/audit-redemption-v4-score-diff.ts`                           | Generate a reviewer score-diff report comparing v3.997-style static scoring against v4 current-capacity scoring                                                                                                                             | `shared/lib/redemption-backstop-configs/*`, `shared/lib/redemption-backstop-scoring.ts`; optional `--threshold`, `--format json\|markdown`, `--out`, `--supply-usd`, and `--v4-profile`                                                                                                                                                                                                                                                                     | Prints or writes a route-level diff with old score, new score, delta, confidence movement, and reason codes                                                                                                                                                                                                                                                                                                       |
| `scripts/maintenance/generate-redemption-coverage-audit.ts`                       | Generate the Redemption Backstop v4 active-gap, lifecycle-exclusion, and heuristic-route coverage audit                                                                                                                                     | `shared/lib/stablecoins/registry.ts`, `shared/lib/redemption-backstop-configs/*`                                                                                                                                                                                                                                                                                                                                                                            | Exits non-zero only if active unconfigured gaps are unclassified; supports markdown, JSON, and `--report` output                                                                                                                                                                                                                                                                                                  |
| `scripts/maintenance/update-hotspot-ratchet-baseline.mjs`                         | Refresh the hotspot ratchet baseline after an intentional refactor                                                                                                                                                                          | Current metrics for the targeted hotspot files                                                                                                                                                                                                                                                                                                                                                                                                              | Rewrites `scripts/lib/hotspot-ratchet-baseline.json`                                                                                                                                                                                                                                                                                                                                                              |
| `scripts/ci/check-cron-schedule-sync.ts`                                 | Verify `shared/lib/cron-jobs.ts` and `shared/lib/scheduled-runner-registry.ts` stay aligned with `worker/wrangler.toml` cron declarations                                                                                                   | `shared/lib/cron-jobs.ts`, `shared/lib/scheduled-runner-registry.ts`, `worker/wrangler.toml`                                                                                                                                                                                                                                                                                                                                                                | Exits non-zero on cron trigger mismatches                                                                                                                                                                                                                                                                                                                                                                         |
| `scripts/ci/check-cron-connection-budget.ts`                             | Verify each cron slot stays within the documented per-trigger outbound-connection budget                                                                                                                                                    | `shared/lib/cron-jobs.ts`, `worker/src/handlers/scheduled/*`, slot-budget metadata                                                                                                                                                                                                                                                                                                                                                                          | Exits non-zero on slot over-budget or connection-allocation drift                                                                                                                                                                                                                                                                                                                                                 |
| `scripts/ci/check-worker-migrations.mjs`                                 | Replay every worker SQL migration against a throwaway SQLite database, enforce the frozen duplicate-prefix allowlist, and guard the new-migration rollout-safety contract including destructive DDL and required-column hazards             | `worker/migrations/*.sql`, `worker/migrations/MANIFEST.md`, local `sqlite3` CLI or Node `node:sqlite` fallback                                                                                                                                                                                                                                                                                                                                              | Exits non-zero on invalid migration SQL, rollout-safety violations, ordering issues, or new duplicate migration prefixes                                                                                                                                                                                                                                                                                          |
| `scripts/ci/check-critical-coverage.mjs`                                 | Enforce line coverage floor and ratchet checks for critical files                                                                                                                                                                           | `coverage/lcov.info`; optional `CRITICAL_COVERAGE_THRESHOLD`, per-file `CRITICAL_COVERAGE_THRESHOLD_*` overrides, `CRITICAL_COVERAGE_COMPARE_REF`, `CRITICAL_COVERAGE_CHANGED_FILES`, `CRITICAL_COVERAGE_BASELINE_FILE`, `CRITICAL_COVERAGE_RATCHET_TOLERANCE`, and `CRITICAL_COVERAGE_RATCHET_ALL`                                                                                                                                                         | Exits non-zero if critical files are below threshold or regress against the configured baseline                                                                                                                                                                                                                                                                                                                   |
| `scripts/maintenance/test-merge-gate.mjs`                                         | Local merge gate that mirrors the shared CI validate core from `scripts/lib/validate-contract.mjs`, adds the common postbuild test/coverage phase for deploy-impacting diffs, and adds Pages/Worker checks only when those surfaces changed | Local git diff + npm scripts                                                                                                                                                                                                                                                                                                                                                                                                                                | Runs the CI validate core locally, then `test:noncritical` + `coverage:critical` for deploy-impacting diffs, plus conditional `build`/`seo:check` and Worker typechecks                                                                                                                                                                                                                                           |
| `scripts/maintenance/freeze-stablecoin.ts`                                        | Generate the manual JSON edits needed to freeze a tracked stablecoin into the archived/frozen lifecycle path                                                                                                                                | `<coinId>`, `PHAROS_API_KEY`, optional `PHAROS_API_BASE`                                                                                                                                                                                                                                                                                                                                                                                                    | Prints the frozen-snapshot entry and per-coin registry patch skeleton for operator review                                                                                                                                                                                                                                                                                                                         |
| `scripts/maintenance/update-critical-coverage-baseline.mjs`                       | Refresh `.ci/critical-coverage-baseline.json` from current report                                                                                                                                                                           | `coverage/lcov.info`                                                                                                                                                                                                                                                                                                                                                                                                                                        | Updates baseline coverage ratchet file                                                                                                                                                                                                                                                                                                                                                                            |
| `scripts/maintenance/fetch-logos.ts`                                              | Refresh logo map from DefiLlama + CoinGecko                                                                                                                                                                                                 | Public APIs                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Writes `data/logos.json`                                                                                                                                                                                                                                                                                                                                                                                          |
| `scripts/maintenance/register-telegram.ts --action webhook`                                | One-time Telegram webhook registration                                                                                                                                                                                                      | `TELEGRAM_BOT_TOKEN`, `TELEGRAM_WEBHOOK_SECRET`, optional `WEBHOOK_BASE_URL`                                                                                                                                                                                                                                                                                                                                                                                | Calls Telegram `setWebhook` with the webhook URL plus the JSON `secret_token` field                                                                                                                                                                                                                                                                                                                               |
| `scripts/maintenance/register-telegram.ts --action commands`                               | Manually force native Telegram slash-command autocomplete entries when Worker reconciliation cannot be used                                                                                                                                 | `TELEGRAM_BOT_TOKEN`                                                                                                                                                                                                                                                                                                                                                                                                                                        | Calls Telegram `setMyCommands` for the bot; normal deploys reconcile this from `worker/src/lib/telegram-webhook-registration.ts`                                                                                                                                                                                                                                                                                  |
| `scripts/maintenance/register-telegram.ts --action profile`                                | Manual recovery tool that reconciles the Telegram bot's profile metadata (display name, short description, long description) when the Worker's 15-minute reconciliation cache cannot be used                                                | `TELEGRAM_BOT_TOKEN`                                                                                                                                                                                                                                                                                                                                                                                                                                        | Calls Telegram `setMyName`, `setMyShortDescription`, and `setMyDescription`; canonical payload metadata lives in `shared/lib/telegram-bot-registration.ts`                                                                                                                                                                                                                                                          |
| `scripts/maintenance/rollback-pages-deployment.mjs`                               | Roll back Cloudflare Pages production to a previous deployment id                                                                                                                                                                           | `CLOUDFLARE_API_TOKEN`, `CLOUDFLARE_ACCOUNT_ID`, `CF_PAGES_PROJECT_NAME`, `CF_PAGES_DEPLOYMENT_ID`                                                                                                                                                                                                                                                                                                                                                          | Calls the Cloudflare Pages rollback REST API                                                                                                                                                                                                                                                                                                                                                                      |
| `scripts/maintenance/refresh-reserve-html-fixtures.ts`                            | Refresh checked-in HTML fixtures used by reserve adapter tests                                                                                                                                                                              | Live upstream reserve pages for configured adapters                                                                                                                                                                                                                                                                                                                                                                                                         | Updates reserve adapter fixture files                                                                                                                                                                                                                                                                                                                                                                             |
| `scripts/ci/check-duplicate-exports.mjs`                                 | Detect duplicate named export declarations within individual files under `shared/lib`, `src/lib`, and `worker/src/lib`                                                                                                                      | `shared/lib/**`, `src/lib/**`, `worker/src/lib/**`                                                                                                                                                                                                                                                                                                                                                                                                          | Exits non-zero on duplicate export names inside one file (catches post-merge conflicts from parallel worktrees)                                                                                                                                                                                                                                                                                                   |
| `scripts/ci/check-doc-counts.mjs`                                        | Detect stale hardcoded counts (stablecoins, adapters, bluechip slugs, live-enabled coins) in primary docs                                                                                                                                   | `shared/data/stablecoins/canonical-order.json`, `shared/data/stablecoins/coins/*.json`, `shared/data/stablecoins/coins.generated.json`, `shared/lib/shadow-stablecoins.ts`, `worker/src/cron/reserve-adapters/index.ts`, `shared/lib/bluechip-slugs.ts`, primary doc files                                                                                                                                                                                  | Exits non-zero listing files with stale counts                                                                                                                                                                                                                                                                                                                                                                    |
| `scripts/ci/check-agent-doc-sync.mjs`                                    | Ensure `AGENTS.md` and `CLAUDE.md` stay mirrored apart from their reciprocal sync-file reference                                                                                                                                            | `AGENTS.md`, `CLAUDE.md`                                                                                                                                                                                                                                                                                                                                                                                                                                    | Exits non-zero when durable agent rules, gotchas, or repo map guidance drift between the two files                                                                                                                                                                                                                                                                                                                |
| `scripts/ci/check-verified-doc-links.mjs`                                | Validate relative doc links and heading anchors across the verified documentation corpus (`README.md` + `docs/**`)                                                                                                                          | Verified documentation markdown files                                                                                                                                                                                                                                                                                                                                                                                                                       | Exits non-zero on broken relative links, bad repo-absolute targets, or missing heading anchors                                                                                                                                                                                                                                                                                                                    |
| `scripts/ci/check-doc-source-paths.mjs`                                  | Validate backtick source-path references across the verified documentation corpus (`README.md` + `docs/**`)                                                                                                                                 | Inline code spans in verified documentation markdown files                                                                                                                                                                                                                                                                                                                                                                                                  | Exits non-zero on repo path references that no longer resolve to a file or directory                                                                                                                                                                                                                                                                                                                              |
| `scripts/ci/check-doc-sync.ts`                                           | Thin CLI runner for the modular doc-sync checks that detect stale exact methodology versions, metric weights, thresholds, and enforced limits in high-risk docs                                                                             | `scripts/lib/doc-sync/checks.ts`, `scripts/lib/doc-sync/methodology-manifest.ts`, canonical version modules in `shared/lib/*-version.ts`, metric source files in `shared/lib/**` and `worker/src/**`, scoped doc files in `docs/`                                                                                                                                                                                                                           | Exits non-zero listing files with stale exact-value documentation                                                                                                                                                                                                                                                                                                                                                 |
| `scripts/maintenance/generate-agent-code-map.mjs`                                 | Generate the compact agent code map used for fast source entrypoint/export discovery                                                                                                                                                        | Source files under `src/`, `shared/`, `functions/`, `worker/src/`, and `scripts/`                                                                                                                                                                                                                                                                                                                                                                           | Writes `docs/agent-code-map.md`                                                                                                                                                                                                                                                                                                                                                                                   |
| `scripts/ci/check-env-contract.mjs`                                      | Keep `.env.example` aligned with runtime binding contracts and env names documented in the env-focused verified docs                                                                                                                        | `.env.example`, `worker/src/lib/env.ts`, `functions/lib/ops-env.ts`, `functions/lib/site-api-env.ts`, env references in source/workflow files, env-focused verified docs                                                                                                                                                                                                                                                                                    | Exits non-zero on missing runtime keys, duplicate example keys, or stale documented env names                                                                                                                                                                                                                                                                                                                     |
| `scripts/ci/check-frozen-invariants.ts`                                  | Verify frozen stablecoins stay excluded from live/public-active data surfaces while retaining historical and metadata access                                                                                                                | Stablecoin metadata, public route/catalog helpers, and live admission helpers                                                                                                                                                                                                                                                                                                                                                                               | Exits non-zero when a frozen asset appears on a live/public-active surface or loses required historical access                                                                                                                                                                                                                                                                                                    |
| `scripts/ci/check-sql-interpolation-safety.mjs`                          | Detect unsafe SQL template-literal interpolation in worker runtime, worker scripts, and root scripts (table names or values must be bound, validated, allowlisted, or explicitly justified)                                                 | `worker/src/**/*.{ts,tsx,js,mjs,cjs,mts,cts}`, `worker/scripts/**/*.{ts,tsx,js,mjs,cjs,mts,cts}`, and `scripts/**/*.{ts,tsx,js,mjs,cjs,mts,cts}` (excluding tests and mocks)                                                                                                                                                                                                                                                                                | Exits non-zero on unguarded dynamic SQL interpolation                                                                                                                                                                                                                                                                                                                                                             |
| `scripts/ci/check-supply-helper-usage.mjs`                               | Ensure route/component cached StablecoinData current-supply reads use the canonical `getCirculatingRaw()` helper instead of raw bucket summing                                                                                              | `src/app/**`, `src/components/**`, and `worker/src/api/**`, with named waivers for raw bucket parsers                                                                                                                                                                                                                                                                                                                                                       | Exits non-zero when non-waived route or component files import `sumPegBuckets` for current supply                                                                                                                                                                                                                                                                                                                 |
| `scripts/ci/check-stablecoin-data.ts`                                    | Validate stablecoin data JSON files against Zod schemas, cross-check canonical-order/dependency/reserve references, validate contract deployments, and require active assets to have a `/api/stablecoins` runtime cache admission path      | `shared/data/stablecoins/coins/*.json`, `shared/data/stablecoins/coins.generated.json`, `shared/data/stablecoins/canonical-order.json`, `shared/lib/stablecoins/schema.ts`, `shared/lib/chains/index.ts`, runtime admission metadata (`llamaId`, `detailProvider`, `geckoId`, contracts)                                                                                                                                                                          | Exits non-zero on schema violations, canonical-order/data-file mismatches, unknown dependency or reserve `coinId` references, unknown contract chain keys, malformed EVM contract addresses, duplicate deployments, or active assets that can be statically routed but cannot enter the stablecoins cache                                                                                                         |
| `scripts/maintenance/audit-pricing-provider-config.ts`                            | Audit CEX and oracle pricing provider configs against live exchange market APIs to detect stale or missing symbol mappings                                                                                                                  | `shared/lib/pricing-provider-config.ts`, live Binance/Bitstamp/Coinbase/Kraken/RedStone APIs                                                                                                                                                                                                                                                                                                                                                                | Reports missing/stale provider configs, skips Binance when a runner region is geoblocked (`403/451`), and runs in CI validate                                                                                                                                                                                                                                                                                     |
| `scripts/maintenance/audit-price-source-depth.ts`                                 | Audit active stablecoin price source depth against prod or fixture site-data payloads, including candidate, agreeing, and depeg-authoritative agreeing source distributions and `>=3` market-cap reach                                      | `/_site-data/peg-summary`, `/_site-data/stablecoins`, `shared/lib/stablecoins/registry.ts`, `shared/lib/pricing-source-registry.ts`, optional fixture `active-stablecoins.json`                                                                                                                                                                                                                                                                             | Manual research command; writes JSON/Markdown reports and optional `agents/source-depth-baseline-YYYY-MM-DD.json`; findings do not fail CI                                                                                                                                                                                                                                                                        |
| `scripts/maintenance/audit-dia-provider-poc.ts`                                   | Audit-only DIA exact-address probe for below-target price-source rows ranked by market cap; records hit rate, freshness, source metadata, and agreement vs Pharos price without entering consensus                                          | `agents/source-depth-baseline-YYYY-MM-DD.json`, `shared/lib/stablecoins/registry.ts`, DIA `GET /v1/assetQuotation/{blockchain}/{address}`                                                                                                                                                                                                                                                                                                                   | Manual research command; no registry, circuit, source-depth, or depeg-authority effect; production use requires false-positive review and methodology approval                                                                                                                                                                                                                                                    |
| `scripts/maintenance/yield-pys-v8-calibration.ts`                                 | Build a Markdown calibration artifact comparing the v7 PYS baseline against the v8 source-risk candidate formula from a saved `/api/yield-rankings` payload                                                                                  | `--input <yield-rankings.json>`, optional `--out` / `--report`, optional `--generated-at`; default output is `agents/yield-pys-v8-calibration-template.md`                                                                                                                                                                                                                                                                                                  | Manual methodology evidence command via `npm run calibrate:yield-pys-v8 -- --input ...`; writes scratch reports under `agents/` unless an explicit reviewed docs/process archive path is supplied                                                                                                                                                                                                                 |
| `worker/scripts/repair-non-usd-fiat-depeg-history.ts`                 | Replay and repair non-USD non-commodity backfill depeg history against the current native-fiat historical policy, canonicalizing legacy IDs and purging known dead orphans                                                                  | `COINGECKO_API_KEY`, `OPS_API_SERVICE_TOKEN_ID`, `OPS_API_SERVICE_TOKEN_SECRET`, Wrangler auth, local D1 by default, optional `--remote`, `--execute --confirm repair-non-usd-fiat-depeg-history`, `--stablecoin=...`, `--peg=...`                                                                                                                                                                                                                          | Dry-run prints replay deltas; live mode rewrites `depeg_events` `source='backfill'` rows and triggers bounded PSI recomputation                                                                                                                                                                                                                                                                                   |
| `scripts/maintenance/screenshot-og.mjs`                                           | Capture OG images for public pages                                                                                                                                                                                                          | Workspace Playwright dependency + live `pharos.watch` by default; `OG_BASE_URL` can point at a local static export                                                                                                                                                                                                                                                                                                                                          | Writes `public/og-*.png`                                                                                                                                                                                                                                                                                                                                                                                          |
| `scripts/maintenance/dev-api-proxy.mjs`                                           | Dev-only local proxy used by the `next dev` `/api/*` rewrite to authenticate local API reads against production `site-api`, mimicking the Pages Functions upstream auth for local dev                                                       | Optional `.env.local`, `SITE_API_SHARED_SECRET`, optional `DEV_PROXY_UPSTREAM`, `DEV_PROXY_PORT`                                                                                                                                                                                                                                                                                                                                                            | Starts a local HTTP proxy on port 3001 (default) with local environment bindings loaded first                                                                                                                                                                                                                                                                                                                     |
| `scripts/maintenance/generate-sitemap-dates.ts`                                   | Generate or check per-route `lastmod` dates from git history for the sitemap                                                                                                                                                                | `src/app/` page files + local git history; pass `--check` to fail on generated-file drift                                                                                                                                                                                                                                                                                                                                                                   | Writes generated sitemap-dates JSON (gitignored), or verifies it is current in `check:generated-artifacts`                                                                                                                                                                                                                                                                                                        |
| `scripts/maintenance/generate-docs-metadata.ts`                                   | Generate or check first/last git dates for public docs pages                                                                                                                                                                                | `shared/lib/public-docs.ts`, `docs/*.md`, and full local git history; pass `--check` to fail on generated-file drift                                                                                                                                                                                                                                                                                                                                        | Writes generated docs metadata JSON (gitignored), or verifies it is current in `check:generated-artifacts`                                                                                                                                                                                                                                                                                                        |
| `scripts/maintenance/generate-stablecoin-per-coin-asset.ts`                       | Generate or check the stablecoin catalog's per-coin source index artifact                                                                                                                                                                   | Per-coin source files through `scripts/lib/stablecoin-catalog-sources.ts`; pass `--check` to fail on generated-file drift                                                                                                                                                                                                                                                                                                                                   | Writes or verifies the generated per-coin asset index                                                                                                                                                                                                                                                                                                                                                             |
| `scripts/maintenance/generate-cemetery-dataset.ts`                                | Generate or check citation-ready Stablecoin Cemetery dataset exports                                                                                                                                                                        | Merged cemetery entries from `shared/lib/cemetery-merged.ts`, backed by `shared/data/dead-stablecoins.json` and `shared/data/stablecoins/coins.generated.json`, cause metadata, and the checked-in `SITE_ORIGIN` from `shared/lib/runtime-origins.ts` / `shared/lib/runtime-origins.json`; pass `--check` to fail on generated-file drift                                                                                                                                                              | Writes deterministic `public/datasets/stablecoin-cemetery.json` and `.csv`, including per-source provenance metadata, or verifies both are current in `check:cemetery-dataset`                                                                                                                                                                                                                                     |
| `scripts/maintenance/generate-public-datasets.ts`                                 | Generate or check 90-day rolling public dataset mirrors (`/datasets/<topic>/{latest,<date>}.{csv,json,ndjson}`) and CORS-enabled Sheets variants (`/sheets/<topic>.csv`) from the W2-A daily snapshot API plus the depeg-events API         | `PUBLIC_DATASETS_API_URL` (fallback: `SMOKE_API_BASE` or `API_BASE_URL`), optional `PUBLIC_DATASETS_API_KEY`, optional `PUBLIC_DATASETS_DATE`, optional `PUBLIC_DATASETS_REQUIRE_API=1`; `--check` only verifies output files exist with valid preambles and row floors (does not re-fetch)                                                                                                                                                                   | Writes `public/datasets/<topic>/{latest,<YYYY-MM-DD>}.{csv,json,ndjson}` and `public/sheets/<topic>.csv` for the four W3-G topics (`top-stablecoins`, `depeg-history`, `scores-latest`, `peg-mechanism-distribution`); prunes dated artifacts older than 90 days                                                                                                                                                  |
| `scripts/maintenance/generate-postman-collection.ts`                              | Generate or check Postman onboarding artifacts for the public API                                                                                                                                                                           | Curated public API request list, public/site base URLs, and Postman v2.1 collection format; pass `--check` to fail on generated-file drift                                                                                                                                                                                                                                                                                                                  | Writes deterministic `public/postman/pharos-api.postman_collection.json` and `.postman_environment.json`, or verifies both are current in `check:postman`                                                                                                                                                                                                                                                         |
| `scripts/maintenance/generate-openapi-spec.ts`                                    | Generate or check the public OpenAPI endpoint catalogue                                                                                                                                                                                     | Curated public API request list, auth policy, and OpenAPI 3.1 output shape; pass `--check` to fail on generated-file drift                                                                                                                                                                                                                                                                                                                                  | Writes deterministic `public/openapi.json`, or verifies it is current in `check:openapi`                                                                                                                                                                                                                                                                                                                          |
| `scripts/maintenance/generate-llms-txt.ts`                                        | Generate or check the curated `/llms.txt` LLM-facing site index                                                                                                                                                                             | `shared/lib/stablecoins`, `shared/lib/cemetery-merged.ts`, `shared/lib/classification.ts`, `data/digests.json`, and curated route lists; pass `--check` to fail on generated-file drift                                                                                                                                                                                                                                                                     | Writes deterministic `public/llms.txt` for Cloudflare Pages, or verifies it is current in `check:llms-txt`                                                                                                                                                                                                                                                                                                        |
| `scripts/maintenance/generate-markdown-exports.ts`                                | Generate Markdown variants for negotiated public routes after the static export exists                                                                                                                                                      | `out/`, `shared/lib/public-docs.ts`, stablecoin metadata, digest data, changelog data, and methodology version data                                                                                                                                                                                                                                                                                                                                         | Writes colocated `out/<route>/index.md` files during `postbuild`                                                                                                                                                                                                                                                                                                                                                  |
| `scripts/maintenance/generate-methodology-pdfs.ts`                                | Generate methodology white-paper PDFs (1 index + 9 versioned changelogs) via Playwright Chromium `page.pdf()` over the static export, with Pharos URN + canonical URL in the header/footer                                                  | Built `out/` (postbuild lane), methodology version + changelog-path exports under `shared/lib/*-version.ts`, `shared/lib/citation/urn.ts`, and `scripts/maintenance/serve-static-export.mjs`; pass `--check` to fail on rendered-text manifest drift, size drift beyond a 20% tolerance, or total/largest PDF budget overflow (`PHAROS_SIZE_BUDGET_METHODOLOGY_PDF_*` overrides)                                                                                  | Writes `public/methodology/pdf/<key>-v<version>.pdf` (one per methodology version) plus `public/methodology/pdf/index.pdf`; old version PDFs stay in tree so citations remain pinned                                                                                                                                                                                                                              |
| `worker/scripts/rebuild-blacklist-current-balances.ts`                | Rebuild `blacklist_current_balances` cache from existing blacklist events and on-chain balance lookups                                                                                                                                      | Wrangler auth, local D1 by default, optional `--remote`, chain RPCs, `--execute --confirm rebuild-blacklist-current-balances` for mutation                                                                                                                                                                                                                                                                                                                  | Dry-run summarizes active addresses; live mode rewrites `blacklist_current_balances` rows for active blacklisted addresses                                                                                                                                                                                                                                                                                        |
| `worker/scripts/reconcile-blacklist-events-from-kyc-rip.ts`           | Reconcile blacklist event rows against an external KYC-rip dataset to backfill missing events                                                                                                                                               | Wrangler auth, prod `stablecoin-db`, external dataset, viem RPC; dry-run by default; pass `--apply` for remote D1 reads/inserts; supports `--help`, `--timeout-ms`, `--min-rows`, `--database`, and `--provider-url`                                                                                                                                                                                                                                        | Dry-run validates and summarizes the external source without D1 access; `--apply` inserts missing `blacklist_events` rows from external source                                                                                                                                                                                                                                                                    |
| `worker/scripts/reconcile-blacklist-current-balances-from-kyc-rip.ts` | Reconcile `blacklist_current_balances` against an external KYC-rip frozen-balance snapshot                                                                                                                                                  | Wrangler auth, prod `stablecoin-db`, external dataset; dry-run by default; pass `--apply` for remote D1 replacement; supports `--help`, `--timeout-ms`, `--min-rows`, `--database`, and `--provider-url`                                                                                                                                                                                                                                                    | Dry-run validates and summarizes the external source without D1 access; `--apply` replaces target-scope `blacklist_current_balances` rows from the snapshot                                                                                                                                                                                                                                                       |
| `scripts/ci/check-build-attribution.mjs`                             | Classify `out/_next/static/chunks/*.js` against the signature set and fail when a known classification group grows >50 KiB or a new unclassified chunk exceeds 200 KiB                                                                      | `out/_next/static/chunks/*.js`, `scripts/lib/build-attribution-signatures.json`, `scripts/lib/build-attribution-baseline.json`, and `scripts/maintenance/explain-build-chunks.mjs`                                                                                                                                                                                                                                                                          | Exits non-zero on chunk-attribution regressions; printable detail report from `node scripts/maintenance/explain-build-chunks.mjs` or with `--report <path>` / `--json`                                                                                                                                                                                                                                            |
| `scripts/maintenance/update-build-attribution-baseline.mjs`          | Refresh the build attribution baseline after an intentional chunk-composition change                                                                                                                                                        | Current classifier output from `scripts/maintenance/explain-build-chunks.mjs`                                                                                                                                                                                                                                                                                                                                                                              | Rewrites `scripts/lib/build-attribution-baseline.json`                                                                                                                                                                                                                                                                                                                                                            |

The SQL safety checker now scans both `worker/src/**` and `worker/scripts/**`, and its regression fixtures live under `scripts/__tests__/fixtures/sql-safety/`.

`pharos-change-contract.mjs` is the shared local agent hook engine for Codex and Claude. `SessionStart` records fingerprints for pre-existing dirty files, while `PostToolUse`, `PostToolBatch`, and `Stop` compare the current diff against that baseline so reminders focus on files changed during the active agent session. `PreToolUse` and `PermissionRequest` are intentionally narrow: they block destructive git cleanup, raw production deploys, remote D1 mutations, direct writes to env/git/build-output paths, and obvious destructive migration SQL without running heavy validation automatically. Deploy and remote-D1 guards inspect executable shell command positions, so searches and patches can still mention blocked release commands.

`worker/package.json` keeps `npm run deploy` as a guarded command that exits with release-flow guidance. Production Worker releases use the CI-documented sequence: upload a Worker Version, run validation and migration checks, preview-smoke the uploaded version, promote it with `wrangler versions deploy`, and then sync triggers.

## CI-Critical Scripts

These are wired into the GitHub Actions CI workflows (`.github/workflows/validate-ci.yml`, `.github/workflows/dependency-audit.yml`, `.github/workflows/pull-request-checks.yml`, `.github/workflows/deploy-cloudflare.yml`, `.github/workflows/pages-prepare.yml`, `.github/workflows/pages-publish.yml`, `.github/workflows/pages-release.yml`, `.github/workflows/rebuild-pages.yml`, and `.github/workflows/safe-browsing-monitor.yml`) directly, or indirectly through `npm run build`:

- `sync-digests.ts` in the reusable Pages-prepare job and the direct production Pages release job before the build artifact is created
- `sync-depeg-events.ts` in the same Pages prebuild paths so `/depeg/<event>/` static params and feed/static exports use current confirmed events
- `generate-public-datasets.ts` in the same Pages prebuild paths so `/datasets/*` and `/sheets/*` mirrors are generated from the selected API environment before `npm run build`
- `generate-sitemap-dates.ts` via the `prebuild` hook that runs automatically before `npm run build`
- `generate-docs-metadata.ts` via the same `prebuild` hook, immediately after `generate-sitemap-dates.ts`
- `generate-cemetery-dataset.ts` via the same `prebuild` hook, immediately after `generate-docs-metadata.ts`
- `generate-public-datasets.ts` via the same `prebuild` hook, immediately after `generate-cemetery-dataset.ts`
- `generate-postman-collection.ts` via the same `prebuild` hook, immediately after `generate-public-datasets.ts`
- `generate-openapi-spec.ts` via the same `prebuild` hook, immediately after `generate-postman-collection.ts`
- `generate-llms-txt.ts` via the same `prebuild` hook, immediately after `generate-openapi-spec.ts`
- `generate-markdown-exports.ts` via the `postbuild` hook, after `next build` creates `out/`
- `generate-stablecoin-per-coin-asset.ts --check` via `npm run check:stablecoin-data` when the stablecoin catalog guard validates generated per-coin source assets
- `check:world-map` via `validate:prebuild` to fail if `public/maps/world-countries.svg` drifts from `scripts/maintenance/build-world-map-svg.ts`
- `check:generated-artifacts` via `validate:prebuild` to fail if any generated artifact in `scripts/lib/automation-registry.mjs` drifts, including sitemap dates, docs metadata, Cemetery exports, Postman exports, OpenAPI, `/llms.txt`, the stablecoin frozen registry, and the generated block in `docs/api-reference.md`
- `check:agent-doc-sync` via `validate:prebuild` to fail if top-level agent guidance files drift out of sync
- `check-seo-static.mjs` via `npm run seo:check`
- `check-phishing-signatures.mjs` via `npm run check:phishing-signatures` after Pages builds
- `check-classifier-sensitive-copy.mjs` via `npm run check:classifier-sensitive-copy` after Pages builds
- `report-build-size.mjs --check` via `npm run check:build-size` after Pages builds; this includes the Cloudflare Pages 20,000-file direct-upload cap so oversized exports fail before the Pages publish step
- `check-safe-browsing.mjs` via the daily/manual `Safe Browsing Monitor` workflow
- `classify-deploy-changes.mjs` via the `detect-changes` jobs in `.github/workflows/deploy-cloudflare.yml` and `.github/workflows/pull-request-checks.yml`
- `pharos-change-contract.mjs` via `.github/workflows/pharos-change-contract.yml` for same-repo PR comments, `.codex/config.toml` for local Codex hooks, and `.claude/settings.json` for local Claude hooks
- `upsert-github-pr-comment.mjs` via `.github/workflows/pharos-change-contract.yml` to keep the PR comment sticky instead of creating duplicates
- `generate-methodology-pdfs.ts --check` via `npm run check:methodology-pdfs` after `npm run build` in Pages validate/prepare lanes. CI provisions Playwright Chromium through `setup-workspace`; the checker fails with a setup error instead of installing browsers implicitly. Production `pages-release` does not block deployment on PDF rendering drift.
- `serve-static-export.mjs` via `npm run serve:static-export` in the pre-deploy `smoke-ui` job inside `.github/workflows/pages-prepare.yml` and the direct production Pages release job
- `smoke-api.mjs` via `npm run test:smoke-api`
- `smoke-ops.mjs` via `npm run test:smoke-ops`
- `smoke-transport.mjs` via `npm run test:smoke-transport`
- `smoke-ui.mjs` via `npm run test:smoke-ui`
- `check-worker-import-boundary.mjs` via `npm run check:worker-boundary`
- `check-shared-cycles.mjs` via `npm run check:shared-cycles`
- `check-unused-code.mjs` via `npm run check:unused-code`
- `check-hotspot-ratchet.mjs` via `npm run check:hotspot-ratchet`
- `check-cron-abort-contract.mjs` via `npm run check:cron-abort-contract`
- `check-redemption-backstops.ts` via `npm run check:redemption-backstops`
- `check-worker-migrations.mjs` via `npm run check:migrations`
- `check-critical-coverage.mjs` via `npm run coverage:critical`
- `check-cron-schedule-sync.ts` via `npm run check:cron-sync`
- `check-cron-connection-budget.ts` via `npm run check:cron-connections`
- `check-doc-counts.mjs` via `npm run check:doc-counts`
- `check-verified-doc-links.mjs` via `npm run check:verified-doc-links`
- `check-doc-source-paths.mjs` via `npm run check:doc-source-paths`
- `check-doc-sync.ts` via `npm run check:doc-sync`
- `check-env-contract.mjs` via `npm run check:env-contract`
- `check-duplicate-exports.mjs` via `npm run check:duplicate-exports`
- `check-sql-interpolation-safety.mjs` via `npm run check:sql-safety`
- `check-stablecoin-data.ts` via `npm run check:stablecoin-data`
- `check-supply-helper-usage.mjs` via `npm run check:supply-helper-usage`
- `build-world-map-svg.ts --check` via `npm run check:world-map`
- `audit-pricing-provider-config.ts` via `npm run audit:pricing-providers`
- `audit-price-source-depth.ts` via `npm run audit:price-source-depth -- --prod` for manual source-depth baseline research
- `audit-price-source-depth.ts` fixture mode via `npm run audit:price-source-depth -- --input scripts/__tests__/fixtures/audit-price-source-depth --json` for deterministic local validation
- `audit-dia-provider-poc.ts` via `npm run audit:dia-provider -- --input agents/source-depth-baseline-YYYY-MM-DD.json` for audit-only DIA exact-address provider research
- `yield-pys-v8-calibration.ts` via `npm run calibrate:yield-pys-v8 -- --input agents/yield-rankings.json` for manual PYS v8 calibration artifacts

## Operational Notes

### `sync-digests.ts`

- Requires an explicit digest API source via `--api-url` or `DIGEST_API_URL`; falls back to `SMOKE_API_BASE` or `API_BASE_URL` when those are already set by CI/local shell context.
- Sends `X-API-Key` when `DIGEST_API_KEY` is set, which is required once `GET /api/digest-archive` is protected on `api.pharos.watch`.
- Accepts `--output` so CI can write digest data into an artifact path before the Pages build job runs.
- The shared Pages prepare workflow and direct production Pages release job run this once before `npm run build`, writing the normalized digest JSON directly to `data/digests.json`.

### `sync-depeg-events.ts`

- Requires an explicit depeg-events API source via `--api-url` or `DEPEG_EVENTS_API_URL`; falls back to `SMOKE_API_BASE` or `API_BASE_URL` when those are already set by CI/local shell context.
- Sends `X-API-Key` when `DEPEG_EVENTS_API_KEY` is set; Pages workflows fall back to `DIGEST_API_KEY` when no dedicated depeg-events credential exists.
- Runs before every Pages build path so static `/depeg/<event>/` pages, feeds, and dataset projections use current confirmed events.

### `generate-public-datasets.ts`

- Runs before every Pages build path with `PUBLIC_DATASETS_API_URL` pointed at the selected API environment. On combined Worker + Pages deploys, that is the uploaded Worker preview URL.
- Sends `X-API-Key` when `PUBLIC_DATASETS_API_KEY` is set; Pages workflows fall back to `DIGEST_API_KEY` when no dedicated public-datasets credential exists.
- Pages workflows clear `PUBLIC_DATASETS_API_URL`, `PUBLIC_DATASETS_API_KEY`, `PUBLIC_DATASETS_REQUIRE_API`, `SMOKE_API_BASE`, and `API_BASE_URL` on the following `npm run build` step so the prebuild hook preserves the just-synced mirrors instead of making a second live API fetch.
- Production Pages paths set `PUBLIC_DATASETS_REQUIRE_API=1`, so missing API configuration fails closed instead of shipping stale mirrors. Local and PR validation builds without a dataset API preserve already checked-in mirrors when `--check` would pass; they fail if those mirrors are missing or below the row floors.
- `--check` verifies that latest checked-in mirrors and Sheets variants exist, carry the expected preambles, and meet per-topic row floors. It does not re-fetch the API or prove the dated mirrors were regenerated today.

### `fetch-logos.ts`

- Designed to run locally (CoinGecko blocks Worker-origin access patterns).
- Pulls DefiLlama stablecoin list, maps `gecko_id` to internal IDs, then fetches CoinGecko market data in batches.

### `dev-api-proxy.mjs`

- `npm run dev` starts `scripts/maintenance/dev-api-proxy.mjs` and `next dev` under a shared process supervisor for local frontend work against production-compatible site-data auth.
- Use `npm run dev:proxy` to run only the local API proxy, or `npm run dev:next` to run only Next.js when the proxy is already running elsewhere.
- Loads `.env.local` through `process.loadEnvFile()`, so existing shell environment values keep precedence.
- Accepts `DEV_PROXY_UPSTREAM` and forwards `SITE_API_SHARED_SECRET` to the upstream site-data host.
- Forwards only normalized `/api/*` paths, rejects traversal segments, and treats upstream redirects as proxy errors.
- `DEV_PROXY_PORT` changes both the proxy process port and the `next dev` `/api/*` rewrite target when `SITE_API_SHARED_SECRET` is set.

### `worker/scripts/repair-non-usd-fiat-depeg-history.ts`

- Uses local D1 by default. Pass `--remote` only when intentionally reading or mutating production D1; PSI recomputation runs only for confirmed remote live repairs.
- Dry-run is the default and compares stored `source='backfill'` rows against the current replay policy without mutating D1.
- Live mode requires `--execute --confirm repair-non-usd-fiat-depeg-history`; production live mode also requires `--remote`.
- Replays each target from its full historical supply or curated launch-date anchor rather than from the currently stored backfill-event window, which avoids split/merge oscillation on sparse non-USD histories.
- `--existing-window` is the explicit fallback for timeout-prone assets: it constrains replay to the currently stored backfill window plus a 30-day pad on each side.
- Live mode deletes and rewrites non-USD non-commodity backfill rows under canonical stablecoin IDs. Legacy per-coin alias folding has been retired; orphan rows must now already be canonical before replay.
- Known dead orphan IDs such as `eura-angle` are purged instead of replayed back into the active depeg history surface.

### `worker/scripts/yield-history-cleanup.ts`

- One-shot operator tool for the five tracked savings-wrapper ownership handoffs (`USDe`, `USDS`, `DAI`, `frxUSD`, `crvUSD`).
- Supports dry-run inventory, artifact export, delete, and restore paths against either a local SQLite rehearsal dataset (`--sqlite`) or Wrangler D1 (`--remote` / `--local`).
- Remote mutation and remote restore both require `--execute --confirm yield-history-cleanup` plus an armed writer pause guard (`--arm-writer-pause`) so the hourly `sync-yield-data` publisher cannot race the cleanup window.
- The source-selection contract is shared with the live read path and hourly purge path through `worker/src/lib/yield-history-cleanup.ts`.

### `yield-pys-v8-calibration.ts`

- Manual evidence tool for PYS v8 source-risk calibration; it does not change runtime data, methodology versions, or checked-in generated artifacts.
- Run with `npm run calibrate:yield-pys-v8 -- --input <saved-yield-rankings.json> [--out agents/report.md]`.
- Keep scratch reports under `agents/`; only move reviewed durable evidence into `docs/process/archive/`.

### `register-telegram.ts`

- Requires `TELEGRAM_BOT_TOKEN`; `--action webhook` and `--action all` also require `TELEGRAM_WEBHOOK_SECRET`.
- Supports `WEBHOOK_BASE_URL` / `--webhook-base-url`; defaults to `https://api.pharos.watch`.
- Supports `--action commands`, `--action profile`, `--action webhook`, and `--action all`.
- Uses `shared/lib/telegram-bot-registration.ts` for command, profile, and allowed-update payloads so manual recovery matches Worker reconciliation.

### `screenshot-og.mjs`

- Imports Playwright from the workspace dependency, matching `smoke-ui.mjs` and avoiding a system-global Playwright path.
- Captures fixed 1200×628 screenshots for selected routes and writes them into `public/`.

### `smoke-ui.mjs`

- Uses the workspace Playwright package directly. Local runs use the Playwright-managed browser by default, while GitHub Actions uses the system Chrome channel unless `SMOKE_UI_BROWSER_CHANNEL` or `SMOKE_UI_BROWSER_EXECUTABLE_PATH` overrides it. Homepage data/analytics checks run once, then overflow route checks run through deterministic route chunks across `SMOKE_UI_OVERFLOW_WORKERS` browser contexts while preserving the same overflow assertions.
- In CI deploys, the browser targets a locally served `out/` export before Pages production deploy; the Pages builds set `NEXT_PUBLIC_FORCE_SITE_DATA_PROXY=true` so browser reads on `127.0.0.1` use the production same-origin `/_site-data/*` lane. `npm run serve:static-export` runs `scripts/maintenance/serve-static-export.mjs`, which proxies direct `/api/*` calls to the configured public API base and `/_site-data/*` calls to the explicitly configured `STATIC_EXPORT_SITE_API_BASE` so the smoke still exercises the live website data path against the exact built frontend artifact. Local `validate:pages-smoke` reads `.env.local` and maps `PHAROS_API_KEY` / `SITE_API_SHARED_SECRET` into the static-export proxy when the explicit `STATIC_EXPORT_*` env vars are unset.
- On combined worker + Pages deploys, the Pages-prepare workflow points the local site-data proxy at the uploaded worker preview URL so the predeploy artifact rehearsal happens against the exact worker candidate before Worker production promotion.
- The shared Pages publish workflow also runs the same smoke script against `https://pharos.watch` after `deploy-pages`, so the pipeline checks both the pre-publish artifact and the real public host. Production deploys skip live overflow because the full route sweep already ran against the exact local artifact.
- Verifies homepage is not in outage/empty state (`Failed to load data` or `Failed to load this dataset`, `stablecoins:404`, `Data not yet available` or `Waiting for first sync`, `Connection issue` or `Unable to reach the Pharos data API right now.`, `No stablecoin data available`, missing rows/ticker).
- When `SMOKE_UI_EXPECT_GA_ID` is set, verifies the expected GA script, runtime `window.gtag` setup, dataLayer `page_view`, successful `gtag.js` load, and a GA4 `page_view` collect signal. Live mode requires successful collect delivery; after that success, expected-measurement GA collect `net::ERR_ABORTED` reports are treated as browser beacon noise. Local mode also accepts a Playwright `net::ERR_ABORTED` report for an issued collect URL with the configured measurement id because Chromium can abort the beacon as the local smoke context closes.
- Homepage data wait retries once on timeout by default (configurable via `SMOKE_UI_RETRY_COUNT` / `SMOKE_UI_RETRY_DELAY_MS`) and includes a compact DOM text preview in timeout diagnostics.
- Local mode runs mobile overflow checks at `390x844` on a default critical route set:
  - `/`, `/alt-pegs/`, `/dependency-map/`, `/flows/`, `/yield/`, `/liquidity/`, `/depeg/`, `/freezewatch/`, `/stability-index/`, `/safety-scores/`, `/api/`
- Live mode keeps the homepage/GA checks and a narrow canary overflow route set (`/yield/`, `/alt-pegs/`, `/freezewatch/`, `/stability-index/` by default; override with `SMOKE_UI_CANARY_ROUTE`) unless `--skip-overflow` is set.
- Overflow detection samples layout multiple times and retries once before failing, which filters transient layout jitter while still catching sustained overflow regressions.
- Override checked routes via `SMOKE_UI_OVERFLOW_ROUTES` (comma-separated), choose `--mode local|live`, tune overflow parallelism with `SMOKE_UI_OVERFLOW_WORKERS` (`1`-`6`; default `2` local and `1` live), or skip overflow checks with `--skip-overflow`.

### `smoke-ops.mjs`

- Validates the Access-protected operator UI shell plus direct operator API checks. The default `SMOKE_OPS_SCOPE=full` covers `status`, `status-history`, admin samples, and dry-run admin actions; `SMOKE_OPS_SCOPE=canary` keeps only shell/access plus direct and same-origin status checks for deploy-critical smoke.
- Same-origin `ops.pharos.watch/api/admin/status` smoke first tries the service-token path, then replays a bootstrapped `CF_Authorization` cookie when the UI host yields one.
- Retries up to two transient `502`/`504` gateway responses on that same-origin proxy assertion to absorb post-deploy warmup without masking persistent proxy failures.
- Starts the independent direct ops API probes together with the ops UI shell fetch, and starts the same-origin status proxy check as soon as a UI Access cookie is available, so the smoke does not serialize unrelated network waits.

### `serve-static-export.mjs`

- Defaults to serving `out/` on `127.0.0.1:4173`.
- Serves exported `/api/` page files first, including static RSC payloads such as `/api/__next.api.txt`; other nested `/api/*` paths proxy to `STATIC_EXPORT_API_BASE` (default: `https://api.pharos.watch`).
- Proxies `GET`/`HEAD` requests under `/_site-data/*` to `STATIC_EXPORT_SITE_API_BASE` when set; otherwise it falls back to `STATIC_EXPORT_API_BASE`, then the default public API origin. CI sets `STATIC_EXPORT_SITE_API_BASE` explicitly for site-data smoke, forwards `STATIC_EXPORT_SITE_API_SHARED_SECRET` as `X-Pharos-Site-Proxy-Secret` when set, and builds the artifact with `NEXT_PUBLIC_FORCE_SITE_DATA_PROXY=true` so local-host browser reads hit this path instead of the protected direct `/api/*` lane.
- Exists to keep browser smoke pre-deploy while still exercising the same API-backed UI code paths as production.

### `classify-deploy-changes.mjs`

- Used by the deploy and pull-request `detect-changes` jobs. On PRs, the workflow sets `DEPLOY_EVENT_NAME=push` with PR base/head SHAs so the same deploy-surface classifier controls conditional Pages build/SEO and Worker typechecks.
- On `push`, diffs `DEPLOY_BASE_SHA...DEPLOY_HEAD_SHA` and emits `deploy_required`, `worker_changed`, `worker_promotion_required`, and `pages_changed`.
- `worker_changed` turns on worker validation/typecheck coverage when the diff touches worker/shared runtime, package/deploy infra, `.github/actions/`, `scripts/lib/`, shared guardrail scripts, worker operational scripts, or worker-specific checks/smokes.
- `worker_promotion_required` turns on Worker upload/promotion/production API smoke only for deployed Worker runtime/config, D1 migrations, Worker assets, shared runtime files that can affect the Worker, and root package/lock changes to packages consumed by the Worker bundle. Root package/tooling changes that only touch non-Worker packages and known Pages-only shared contract files can still set `worker_changed=true` for validation without shifting production Worker traffic.
- `pages_changed` turns on Pages build/browser-smoke/deploy when the diff touches Pages runtime paths, package/deploy infra, `.github/actions/`, `scripts/lib/`, shared guardrail scripts, Pages workflow files, or selected build/static-export scripts.
- `deploy_required=false` lets the push workflow skip the heavy deploy path entirely for docs-only or other non-deploy diffs.
- On `workflow_dispatch`, forces the full deploy path for safety.

### `pharos-change-contract.mjs`

- Powers repo-local Codex hooks configured in `.codex/config.toml` and the same-repo PR comment workflow in `.github/workflows/pharos-change-contract.yml`. CI and pre-push validation remain authoritative.
- Default CLI mode reads the current dirty diff, including untracked files. Use `--staged` for staged-only review, `--base-ref` / `--head-ref` for a specific comparison, `--file <path>` for deterministic single-file checks, `--json` for machine-readable output, or `--markdown` for the GitHub PR comment body.
- `SessionStart` injects compact Pharos routing context. If files are already changed, it includes matched task families, docs to read, likely docs to update, checks to consider, and core rules.
- `Stop` emits Codex continuation JSON once per turn when changed files match a medium- or high-risk task family. Low-risk-only changes (doc-only edits) skip the continuation, and `PostToolUse`/`PostToolBatch` reminders apply the same risk gate. The continuation asks the agent to run relevant checks/docs updates or state why they were unnecessary or unavailable; `stop_hook_active=true` is respected to avoid loops.
- Codex hook entries in `.codex/config.toml` use the documented nested command-handler shape with the current `features.hooks` flag. `PreToolUse`, `PermissionRequest`, and `PostToolUse` use catch-all matchers because Codex native command/apply-patch tool names differ from Claude's `Bash` / `Edit` names. The shared script still only blocks deterministic unsafe operations.
- Codex stores per-hook trust under user/session `hooks.state`, not in project config. A new or modified project hook requires a one-time `/hooks` review in the Codex TUI before it runs.
- The PR workflow uses `pull_request` and only comments for same-repo PRs, so it does not run untrusted fork code with a write token.
- This script is advisory. It should steer agents toward the right Pharos docs/checks, not replace `npm run test:merge-gate`, CI, or human review.

### `upsert-github-pr-comment.mjs`

- Reads a rendered Markdown body from `COMMENT_FILE` and requires the stable marker `<!-- pharos-change-contract -->`.
- Finds the most recent existing marker comment on the PR and patches it; otherwise it creates a new comment.
- Intended for GitHub Actions only. It does not run locally unless `GITHUB_TOKEN`, `GITHUB_REPOSITORY`, and `PR_NUMBER` are set.

### `test-merge-gate.mjs`

- Skips cleanly when the changed-file set is not deploy-impacting.
- For deploy-impacting diffs, always runs the same validate core as deploy CI: dependency audit, lint, worker-boundary, blocking cycle detection across `shared/`, `worker/src`, and `src`, migrations, cron schedule/connection checks, doc link/source-path/sync checks, duplicate-export and redemption-backstop checks, unused-code, hotspot-ratchet, non-critical tests, and critical coverage.
- Adds `npm run build` and `npm run seo:check` only when the changed-file set is Pages-impacting, using the same matcher as `classify-deploy-changes.mjs`.
- Adds Worker runtime typecheck only when the changed-file set is worker-impacting.
- After `npm run validate:prebuild` passes, uses `buildCommandPlan` to construct a per-trigger execution plan covering Pages build/SEO, non-critical-test, critical-coverage, and Worker typecheck groups, then runs them **serially by default** to avoid local CPU contention. Set `MERGE_GATE_PARALLEL=1` to opt into parallel execution; when enabled, a failing parallel group aborts siblings and reports the failing command explicitly. CI always runs the parallel matrix via separate runners regardless of this env var.
- Supports `--staged`, `MERGE_GATE_BASE_REF=<ref>`, `MERGE_GATE_HEAD_REF=<ref>`, `MERGE_GATE_FULL_DEPLOY=1`, `MERGE_GATE_DRY_RUN=1`, and `MERGE_GATE_PARALLEL=1`. The repo pre-push hook passes Git's exact pushed main ref range through `MERGE_GATE_BASE_REF` and `MERGE_GATE_HEAD_REF` so local classification matches the Cloudflare deploy workflow's push range.

### `generate-agent-code-map.mjs`

- Scans the repo's main runtime/tooling source roots and writes `docs/agent-code-map.md`.
- Captures route files, top-level exports, and grouped stablecoin JSON counts as a compact discovery aid for agents.
- Regenerate it after structural route, handler, cron, hook, or script entrypoint changes.
- The generated map is a navigation aid only. Read the implementation before editing.

### `smoke-api.mjs`

- Validates `/api/health` and every strict contract endpoint mirrored from `shared/lib/api-endpoints/`; `src/lib/__tests__/api-endpoints.test.ts` guards drift between the shared strict list and the smoke assertions.
- Can target either production or a Worker preview URL via `--base-url` / `SMOKE_API_BASE`; the deploy workflow now uses it both before production promotion and after cutover.
- Sends `X-API-Key` when `SMOKE_API_KEY` is set, which is required once protected public routes are enforced.
- Executes strict endpoint checks sequentially to avoid post-deploy request fan-out against a freshly deployed worker.
- Retries transient request failures once by default (timeouts/network errors/5xx/429/408); tune with `SMOKE_API_RETRY_COUNT` and `SMOKE_API_RETRY_DELAY_MS`. The production post-promotion workflow overrides these to a larger retry budget before rollback so custom-domain cutover warmup does not fail on the first slow health probe.
- Per-request timeout defaults to `12000ms`; tune with `SMOKE_API_TIMEOUT_MS`.

### `smoke-ops.mjs`

- Uses Cloudflare Access service-token headers (`OPS_SMOKE_CF_ACCESS_CLIENT_ID` / `OPS_SMOKE_CF_ACCESS_CLIENT_SECRET`) rather than the legacy single-token admin flow.
- Defaults to `https://ops.pharos.watch/admin/` and `https://ops-api.pharos.watch`, with overrides via `SMOKE_OPS_UI_URL` / `SMOKE_OPS_API_BASE`.
- Verifies the operator UI shell plus `/api/status`, `/api/status-history?limit=5`, admin samples, and safe dry-run admin paths in the default full scope. Set `SMOKE_OPS_SCOPE=canary` for shell/access plus direct and same-origin status-only smoke.
- Full-scope blacklist backfill dry-run probes default to `USDT` on `optimism`; override them with `SMOKE_OPS_BLACKLIST_BACKFILL_STABLECOIN` and `SMOKE_OPS_BLACKLIST_BACKFILL_CHAIN_ID`.
- Starts the independent direct ops API probes together with the ops UI shell fetch, and starts the same-origin status proxy check as soon as a UI Access cookie is available, so the smoke does not serialize unrelated network waits.
- When the `ops.pharos.watch` flow returns a bootstrapped UI session cookie (for example `CF_Authorization`), the script reuses that browser-style session to smoke same-origin `/api/admin/status`.
- If the service-token flow reaches the UI shell but still does not yield a bootstrapped UI session cookie, the script keeps the shell assertion and direct `ops-api` smoke but skips the same-origin proxy assertion instead of failing on a non-browser auth shape the Pages proxy cannot use.
- If `ops.pharos.watch` only returns the interactive Cloudflare Access redirect, the script also skips the same-origin proxy assertion for the same reason.
- If the Pages proxy remains `401 Unauthorized` even after the best-effort cookie replay, the script also records a skip instead of a failure; that CI lane can prove the direct `ops-api` auth path, but not a full browser-backed Access session on Pages Functions.

### `smoke-transport.mjs`

- Sends `HEAD` requests with `redirect: "manual"` to exercise the edge redirect rather than following it.
- Defaults to `http://api.pharos.watch/api/health?smoke=transport` and `http://site-api.pharos.watch/api/stablecoins?limit=1&smoke=transport`.
- Requires `308` plus an exact `Location` match that preserves host, path, and query while upgrading only the scheme to `https`.
- Checks both redirect hosts concurrently because the edge assertions are independent.
- Exists specifically to keep HTTP->HTTPS redirect ownership at the Cloudflare edge layer instead of drifting into worker code.

## Safe Usage Guidelines

- Treat `backfill-*` scripts as admin operations: run against staging/dev first when possible.
- Run smoke scripts against explicit URLs during incident debugging (avoid relying on default env fallback).
- Keep script-owned generated artifacts (`data/digests.json`, `data/logos.json`, `public/og-*.png`) under version control policy used by this repo.
