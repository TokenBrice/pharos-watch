# Live Reserve Sync

Dedicated documentation for the live reserve-composition subsystem that powers `GET /api/stablecoin-reserves/:id`, the stablecoin-detail reserve card, and `/status` reserve-sync health.

> **Agent navigation** — Grep the heading you need: Overview · Metadata Contract · Cron Behavior · Storage Model · API Contract · Adapter Registry · Frontend Consumers · Scope Boundaries.

---

## Overview

- **Cron:** `sync-live-reserves` (`worker/src/cron/sync-live-reserves.ts`)
- **Schedule:** `11 */4 * * *` (every 4 hours at :11 UTC)
- **Shared 4-hourly lane:** after live reserve sync, the same slot runs redemption backstop sync, Kinesis supply sync, and the named `reserve-post-sync-watchdog` child for collateral-drift cache updates and stale-source alerts (`worker/src/handlers/scheduled/hourly-live-reserves.ts`)
- **Current coverage:** derived from `liveReservesConfig` in stablecoin metadata and the adapter declarations. Use those sources and the generated coverage reports for the current enabled, configured, bound, and unbound sets.
- **Storage:** `reserve_composition`, `reserve_composition_history`, `reserve_sync_state`, `reserve_sync_attempt_history`
- **API:** `GET /api/stablecoin-reserves/:id`
- **Frontend consumers:** `useStablecoinReserves()`, stablecoin detail view model, `/status` reserve-sync health
- **Operational telemetry:** `sync-live-reserves` emits per-coin progress into `cron_run_progress`, including the current coin, adapter, breaker key, and running synced / failed / skipped counters

This pipeline is intentionally separate from curated reserve metadata in `StablecoinMeta.reserves`. Historically, Safety v5.8 through v8.17 used score-grade snapshots for `collateralFromLive`, collateral-quality, and Dependency Risk inputs. Current V9 instead compiles accepted fresh, clean, independent live slices into Backing reserve exposures and reconciles tracked `coinId` slices into effective dependency edges; unmapped share remains self-backed/non-stablecoin exposure. A configured adapter is not enough by itself. When no accepted live rows are captured, V9 falls back to reviewed static/curated reserve rows rather than treating a degraded live attempt as score evidence. Adapter validation and stored-row decoding reject unknown or self-referential `coinId` links and `depType` without `coinId`; subject-aware adapters keep self-held reserve slices visible without turning them into dependency edges. Cyclic or invalid live dependency evidence falls back or fails closed according to the V9 fact-set contract while the reserve snapshot remains available for reserve/status surfaces.

Score-grade adapters may emit a namespace-qualified stable `sourceKey` for each source-owned reserve category. V9 joins a keyed live slice to reviewed metadata only when that key is unique on both sides; an explicit mismatch or collision fails closed. Historical rows without keys retain a unique normalized-name compatibility join. Daily percentages are never part of slice identity: the live percentage remains the scoring and dependency weight, while the reviewed percentage is evidence about the reviewed composition rather than a join threshold.

Stable keys use source asset codes, token/feed addresses, or fixed source bucket identifiers, never display labels. The live producer and reviewed sidecar must be updated together. Aggregate NAV buckets for `tbill-openeden` and `wtgxx-wisdomtree` intentionally remain unkeyed: their reviewed compositions describe multiple underlying holdings, not the aggregate fund-share observation. Assigning the aggregate key to one constituent (or every constituent) would create a false or ambiguous classification join.

Assurance reports also retain source-native category keys when the reviewed sidecar is at a different level of detail: AUSD's combined cash review does not match separate fund/company cash observations, USDG's individual fund look-through rows do not match the aggregate government-MMF category, AUDD's aggregate deposits do not match individual bank accounts, and BRLV's bond/ETF aggregates do not match separate primary/secondary report rows. Newly disclosed PYUSD Treasury bills have no historical reviewed slice. These categories retain their source classifications without forcing a reviewed join; a composition review, rather than an identity migration, is required to reconcile the different scopes.

Adapters that can emit mapped dependency targets are enrolled in the reviewed mapping registry in `shared/data/coverage-dispositions/dependency-target-dispositions.ts`. Each adapter-level record identifies the implementation source files that were reviewed and records the reviewer, date, rationale, and source-file provenance. The dependency coverage gate rejects missing, stale, duplicate, or source-free adapter reviews when a report-card dependency set reports `live-reserve` as its base source. The registry attests to the adapter mapping rule; canonical target identities remain owned by the adapter and its per-coin configuration, so the registry does not invent per-target fingerprints or duplicate provenance onto every emitted live slice.

`sodax-sonic` reads bnUSD's Sonic SODAX borrower collateral, not the historical ICON-hub contract inventory. The issuer API supplies candidate addresses; their pinned `scaledBalanceOf` sum must exactly equal the non-transferable variable-debt token's `scaledTotalSupply`, otherwise the census fails closed. The pool's live reserve list (31 at the September 9 verification, not the earlier 24 nonzero holdings) determines every aToken read. At most 128 borrowers × 40 reserves plus source timestamps fit eight 650-call Multicall3 pages, run two at a time; two discovery batches precede them, with at most five reviewed oracle code checks and separate two-at-a-time supply diagnostics. Every positive reserve uses the pool Aave oracle price: zero/unreadable prices fail closed, and timestamps older than one hour degrade. Reviewed immutable fixed-price sources require exact runtime-code hashes; the two RedemptionOracle wrappers use their immutable redemption-rate feed's timestamp because their fixed USD leg has no timestamp method. Each live slice uses `sodax-sonic:<reserve-address>`; the reviewed aggregate tail intentionally remains unkeyed, and new/unmapped reserves remain quantified. The adapter is `weak-live-probe`, publishes `totalReserveUsd` but no collateralization ratio, and emits `supply-inventory-unverified` until the v2 global issuance/contract inventory is reviewed and reconciled; that review is the promotion prerequisite. Fixed-constant oracle legs (for example the USDT reserve priced by the immutable MockAggregator constant 1e8) are valued with the live DefiLlama quote, keeping the reviewed constant only as a fallback flagged `price-insensitive` in `details.priceSources`; the census fails closed when neither a live quote nor a readable oracle price is available.

---

## Metadata Contract

Live reserve support is declared per coin in `StablecoinMeta.liveReservesConfig` (`shared/types/live-reserves.ts`, loaded from `shared/data/stablecoins/coins/*.json` via `shared/lib/stablecoins/registry.ts` and validated by `shared/lib/stablecoins/schema.ts`).

`LiveReservesConfig` fields:

| Field              | Meaning                                                                               |
| ------------------ | ------------------------------------------------------------------------------------- |
| `adapter`          | Registered adapter key from `shared/lib/live-reserve-adapters.ts`                     |
| `version`          | Increment when adapter semantics or parsing change materially                         |
| `semantics`        | One of `collateral-mix`, `protocol-reserve`, `attestation-mix`, `single-asset`        |
| `breakerScope`     | Optional circuit-breaker grouping override; defaults to adapter key                   |
| `display`          | Optional UI/source link metadata (`url`, `label`)                                     |
| `inputs.primary`   | Primary source input                                                                  |
| `inputs.fallbacks` | Optional fallback inputs                                                              |
| `params`           | Adapter-specific validated settings enforced by the shared live-reserve config schema |
| `scoring`          | Optional per-coin scoring policy (`maxSourceAgeSec`, `allowedDegradedWarningCodes`)   |
| `suspended`        | Optional operator kill switch (`reason`, `since`, optional `reviewBy`, ISO dates)     |

A `suspended` marker is for dead or semantically unusable upstream feeds (for example a provider moving to keyed API access with no key issued, or a reachable feed whose measurement basis cannot represent the configured reserve composition). The registry strips a suspended `liveReservesConfig` at load (`shared/lib/stablecoins/registry.ts`), so every consumer — the sync queue, V9 backing evidence, presentation, and circuit breakers — treats the coin as having no live feed and falls back to curated reserve evidence. The source file keeps the full adapter config, so re-enabling is a one-line revert. First use: the seven `m0`-adapter coins on 2026-08-19, after M0's Protocol API moved to keyed access; re-enabled on 2026-08-20 once an issuer API key (`M0_API_KEY`) was configured and the adapter was reworked against the surviving keyed schema. USDXL uses the same fail-closed mechanism because its branch feed measures lending-pool cash instead of gross collateral and omits the separate USDT0 PSM facilitator.

All configured source URLs must be absolute URLs. The schema enforces this for HTTP/indexer inputs, `display.url`, and adapter-specific URL params such as RPC endpoints, dashboard APIs, and liquidity endpoints. Adapter definitions also declare their supported `semantics` values and config `version` set, so metadata changes cannot silently pair an adapter with unsupported reserve semantics.

### Registry-Defined Adapter Classes

`shared/types/live-reserve-adapter-declarations.ts` is the single authoring surface: it owns each adapter key, its Zod params schema (referenced directly by the declaration entry, not through a string identifier or a separate schema module), accepted primary input kinds, config validation policy, public source definition, provenance status, and display badge metadata. Shared `validation` tiers are named constants in `shared/types/live-reserve-adapter-policy.ts` so adapters on the same policy share one definition. `shared/lib/live-reserve-adapter-descriptors.ts` enriches the declarations and publishes `LIVE_RESERVE_ADAPTER_DEFINITIONS` as the single map for config, provenance, display, and adapter consumers; the Worker's `ReserveAdapterDefinition` is a `Pick<>` of the declaration type rather than a hand-copied structural type. `LIVE_RESERVE_ADAPTER_KEYS` and `LiveReserveAdapterKey` remain declaration-derived key views. Five important definition properties are not user-configured per coin:

| Property              | Meaning                                                                                                      |
| --------------------- | ------------------------------------------------------------------------------------------------------------ |
| `sourceModel`         | Distinguishes `dynamic-mix`, `validated-static`, and `single-bucket` reserve shapes                          |
| `evidenceClass`       | Distinguishes scoring-eligible `independent` feeds from `static-validated` and `weak-live-probe` feeds       |
| `sourceOriginClass`   | Records who produced the evidence without changing its admission, liveness, or scoring strength             |
| `sharedSourceMode`    | Distinguishes per-coin fetches (`none`) from explicitly source-invariant result sharing (`source-invariant`) |
| `redemptionTelemetry` | Declares whether the adapter can emit direct/proxy redemption capacity and current-fee telemetry             |

- `dynamic-mix`: independently measured reserve compositions. These can be `independent` evidence for scoring when the retained snapshot passes admission.
- `validated-static`: live validation/probe adapters over curated/static slices. These remain authoritative for the reserve detail API, but they are tagged `static-validated` and do not count as independent live collateral inputs for report-card scoring.
- `single-bucket`: one-slice live proofs/attestations. Some are true independent evidence (`anzen-usdz`, `astherus-earn-wrapper`, `blast-usdb-yield-manager`, `btcfi`, `chainlink-nav`, `chainlink-por`, `chronicle-nav`, `erc4626-single-asset`, `escrow-balance`, `hive-hbd-protocol`, `initia-wrapper-vault`, `liquity-native-active-pool`, `liquity-v1`, `m0`, `m0-wrapper-underlying`, `ripple-transparency`, `sgforge-coinvertible`, `spiko-api`, `superstate-liquidity`, `united-por`, `usd1-bundle-oracle`, `usdai-hub`, `yamato`), while weak liveness-only or proof-class summary feeds such as `single-asset`, `solstice-attestation`, and `river-protocol-info` are tagged `weak-live-probe`. wiTRY (`witry-brix`) binds `erc4626-single-asset` over its Ethereum ERC-4626 staking wrapper.
- `independent`: scoring-eligible live evidence when the retained snapshot is fresh, authoritative, config-matched, and has no unexcused snapshot degradation; a later failed attempt does not revoke it.
- `static-validated`, `weak-live-probe`: detail/status-visible evidence classes that never override curated collateral scoring. Nested same-run redemption telemetry (`live-direct`, `live-direct-bounded`, `live-queue`, `live-proxy-validated`, or `documented-bound` with `same-run-onchain`, `same-run-api`, or `verified-source-timestamp`) may still bound the holder-facing exit route when the snapshot class is `weak-live-probe`; legacy `immediateRedeemableUsd` on that class stays rejected.
- `sourceOriginClass` is orthogonal to `evidenceClass`. It records `issuer-attested`, `onchain-observation`, `independent-assurance`, `reviewed-curation`, or `unknown` for diagnostic provenance. Unreviewed adapters resolve explicitly to `unknown`; the registry never guesses origin from an adapter name or input kind. Origin does not change reserve admission, standalone reserve facts, V9 facts, scores, or public API fields. The private V9 evidence-journal schema can carry the resolved value when journal writers are enabled.
- `source-invariant`: opt-in within-run result sharing for adapters whose returned payload is coin-invariant. All other adapters run per coin even when configs look similar.

`single-asset` now supports optional `reserveProbe`, `supplyProbe`, and `timestampProbe` paths so weak single-bucket feeds can persist honest reserve/supply ratio telemetry when the upstream exposes it. The family remains `weak-live-probe` unless the source is strong enough to justify promotion into a more independent adapter class. `getJsonPath` now accepts numeric array indices (for example `chains[0].lastSyncedAt`), so QCAD (`qcad-stablecorp`) drives a `timestampProbe` from the Stablecorp balances API for verified freshness, and MXNe (`mxne-real-mxn`) binds the onchain-EVM `single-asset` probe as a CETES Stablebonds wrapper mirroring `cetes-etherfuse`.

`attestation-pdf-index` is a validated-static adapter for issuer pages that expose dated PDF attestations. It selects the newest dated attestation, audit, or report PDF link, including Webflow-style gated PDF attributes, ignores unrelated dated documents such as whitepapers, validates source freshness from the report date, and emits configured static reserve slices until full PDF text extraction is implemented. ZARP (`zarp-zarp`) binds its Kempen AUP transparency index (latest 2026-08-12) and emits the reviewed single ZAR-fiat-reserve slice. Ripio's four wFIAT coins (`wbrl-ripio`, `wcop-ripio`, `wmxn-ripio`, `wars-argentine-peso`) bind the shared Ripio local-stablecoins page, whose accountant-certification PDFs carry compact `YYYYMMDD` dates in their filenames (newest 2026-03-31) — the parser now accepts these as the lowest-precedence day format while rejecting 8-digit tokens that are not valid dates, and each coin emits its reviewed 100% local-currency bank-deposit slice. An optional `linkMatch` regex param (validated to compile) is matched against each dated link's href plus anchor text, so Ripio's four wFIAT coins can each pin their own currency token; when no link matches, the adapter omits `reportPdfUrl` and emits an info `attestation-pdf-index-link-unmatched` warning instead of citing a sibling currency's certificate.

The independent-assurance family (`shared/lib/independent-assurance.ts` plus per-publisher adapters such as AUDX, EUROP, PAXG, and StraitsX XSGD/XUSD) is the score-grade successor for report-backed issuers: each reviewed report is compiled OFFLINE into a manifest entry (source URL, SHA-256 of the official PDF bytes, report date, examiner identity, asset/liability figures, composition slices) via `scripts/maintenance/refresh-independent-assurance-reports.ts`, and the Worker only verifies the exact official artifact at runtime — fetching the PDF bytes and failing closed on hash mismatch, URL/host drift, PDF magic/content-type/size drift, missing liabilities, inconsistent reported totals, or a newer unreviewed report appearing on the index. Observed reserve shortfalls and non-positive liability rows publish the slices with a structured reconciliation finding and a `reserve-undercollateralized` degraded warning; the ratio is omitted when total liabilities are non-positive. Freshness is the examined report date (`verified` mode) under per-publisher cadence age caps; a stale verified report degrades the sync and drops score eligibility rather than pretending recency. No PDF parsing happens in the Worker.

Assurance manifests register through `shared/data/live-reserves/independent-assurance/index.ts`; offline extraction profiles register independently through `scripts/lib/independent-assurance-profiles/index.ts`. The schema derives `assuranceTier` from the reviewed `conclusion` and rejects contradictory explicit tiers: favorable examination/assurance conclusions are `independent-assurance`, while `agreed-upon-procedures` and `issuer-attested` conclusions retain those non-independent tiers. Hash-pinning does not promote examiner quality: these latter reports must use a sibling descriptor with `evidenceClass: static-validated` and `sourceOriginClass: issuer-attested`; the shared fetcher rejects other descriptors before fetching. Freshness remains `verified` only from the report's examined as-of instant, never its download time. Paxos products select reviewed `{pin, profile}` entries using `params.product` (default `PAXG`); unregistered products fail closed. Bare compiler `--check` checks all registered manifests against profile metadata, row definitions, and reconciliation without PDF re-extraction; `--product X --pdf /path/report.pdf --check` retains strict full recompilation, including Poppler provenance. See `agents/live-reserve-upgrade/ASSURANCE-HOWTO.md` for the addition sequence.

`flying-tulip-ftusd` consumes Flying Tulip's timestamped ftUSD dashboard API, requires the reviewed Ethereum and Sonic chain set and exact USDC/USDT/USSD collateral addresses, aggregates collateral by underlying, and retains borrow/stake leverage and health-factor diagnostics. The API is a first-party index of publicly verifiable on-chain reserve state. The reviewed chain set is now Ethereum, Sonic, and Binance Smart Chain (whose reviewed collaterals add FDUSD, `coinId: fdusd-first-digital`); a chain that ships with zero TVL and zero supply is treated as an inactive launch placeholder, every expected chain must be present, and an active chain outside the reviewed set surfaces as a `degraded` `unexpected-chain` warning rather than a sync error so an issuer deployment on a new chain never flips ftUSD to `error`. The adapter remains deliberately `weak-live-probe` with `issuer-attested` origin because Pharos currently consumes that index instead of deriving the complete wrapper inventory and values through direct RPC reads; the classification describes the ingestion path, not off-chain custody or an unobservable reserve balance.
The adapter normally fetches issuer index pages with browser-style `Origin`, `Referer`, and `Accept-Language` headers because some WordPress/hosting stacks rate-limit generic Worker HTML requests while still serving normal browser navigations. Hostinger/WordPress pages that reject browser-origin hints, currently Schuman's reserve-audit page, use the neutral Pharos fetch identity instead.
USDv uses the dedicated `solomon-protocol` adapter against Solomon Labs' public protocol-data JSON API (`data.solomonlabs.io/api/solomon-protocol/protocol-data`) rather than a scraped attestation index.

`dgld-gold-mapper` consumes Gold Token SA's live bar registry (`production-gold-mapper-backend.onrender.com/v1`), summing the live PAMP-bar fine-oz weights (`/bars`) against the per-network reconciled token supply (`/networks`, `reconState: matched`, zero `gap`) to publish the reserve-oz-per-token ratio with `verified` freshness from `reconCheckedAt`. The registry is issuer-maintained with no third-party audit, so the adapter stays `weak-live-probe` with `issuer-attested` origin and an isolated breaker scoped to the Render host.

`matrixdock-frs` reads Matrixdock XAGm's FallbackReserveFeed (`latestRoundData()`) and MTokenMain (`totalSupply()`, `ozPerToken()`) on Ethereum plus the Sui `coinMetadata` supply, pinning the feed answer unit against `ozPerToken` (the issuer maintains `totalSupply == totalReserves / ozPerToken`) to emit silver reserve oz and the oz-per-token coverage ratio. The feed is issuer-transmitted (not a Chainlink proxy) and updates only on reserve changes, so the adapter is `weak-live-probe` with `issuer-attested` origin and `not-applicable` freshness anchored to the pinned Ethereum block and Sui checkpoint.

`icp-gldt` measures Gold DAO GLDT's backing directly on the Internet Computer: it queries the pinned swap canister's `get_swap_configs` to enumerate the ORIGYN ICRC-7 NFT canisters and their per-NFT `division` (100 GLDT per gram, i.e. 10^10 base units), reads each canister's `icrc7_balance_of` for the swap canister's default account, and divides the summed locked-NFT grams by the ICRC-1 GLDT ledger supply. Every config must fractionalize against the pinned ledger, and `Custom` per-token divisions fail closed. The slice is a single allocated-gold bucket with the `chainlink-por` `XAU_G` gram convention, `onchain-observation` origin, and `not-applicable` freshness carrying the certified state time in `details`.

### Fallback Inputs

`inputs.fallbacks` is implemented in `runAdapter()` inside `worker/src/cron/sync-live-reserves.ts`.

- The cron tries `inputs.primary` first.
- If the adapter throws, the orchestrator retries the same adapter against each fallback by temporarily swapping that fallback into `inputs.primary`.
- If every fallback fails, the attempt history records the primary and fallback failure chain, and the cron-run metadata includes per-coin `attemptFailureSummaries` for automated triage.

USDD currently uses this path: the cron retries `latest-collateral` on Ethereum if the Tron source fails, and the adapter now derives the matching `collateral-history` URL from whichever chain endpoint is active so fallback freshness stays chain-consistent. FDUSD retries the issuer's official Webflow origin when the canonical transparency host rejects Worker traffic, and the three Reservoir assets share the protocol application's official Vercel deployment as a same-schema fallback. These transport fallbacks do not change the displayed source identity or relax snapshot freshness. USDT held in USDD PSM vaults is emitted as proportional `collateral` exposure; the `mechanism` relationship type is reserved for serial dependencies rather than fractional reserve inventory.

Supported input kinds:

| Kind             | Meaning                                                              |
| ---------------- | -------------------------------------------------------------------- |
| `http-json`      | JSON API endpoint                                                    |
| `http-html`      | HTML page parsed by the adapter                                      |
| `indexer`        | Indexed external data feed                                           |
| `onchain-solana` | On-chain Solana mint-supply reads via keyed RPCs when configured, then public mainnet RPCs |
| `onchain-evm`    | On-chain EVM reads via `etherscan-proxy`, `alchemy`, or `public-rpc` |

The shared live-reserve config schema enforces adapter-specific primary and fallback input kinds. For example, `curated-validated` can use `onchain-evm` or `onchain-solana`, `single-asset` can use `http-json` or `onchain-evm`, and HTML scrapers such as `circle-transparency` cannot accidentally be configured with an on-chain input that would pass metadata validation but fail during cron execution.

`curated-validated` can now use `onchain-solana` when a tracked coin publishes its canonical Solana mint in `coin.contracts`, allowing the adapter to validate non-zero supply without downgrading the reserve mix to a weak single-bucket feed.

MAI, lisUSD, and GGBR have no active live-reserve configuration after the September 2026 source review. MAI's supply probe did not validate a complete reserve composition; lisUSD's five-branch configuration omitted positive collateral, and the September 2026 re-census of all 22 documented GemJoins still leaves material positive branches without a supported price path — the BNB ilk now holds Ceros `ceaBNBc` receipts (100% of that token's supply, no DefiLlama price and no underlying rate exposed on-chain) and sUSDX's only DefiLlama price is a broken pool print — so it stays unbound rather than valuing a ~$110M branch at a proxy unit price; GGBR's ION.au configuration did not establish the current issuer reserve mapping. Their reviewed static data remains available, but `/api/stablecoin-reserves/:id` returns 404 without reading an old cached snapshot. Reactivation requires a supported complete valuation path, not omission of unpriceable branches or a proxy unit price for a non-unit-rate receipt. THBILL retains its configured proof-style source with a label naming both Wellington ULTRA and Fidelity International FILQ; their allocation is undisclosed.

`onchain-solana` now tries configured keyed RPCs first — Alchemy, then dRPC when each credential is available — followed by any explicit adapter RPCs and the three public RPCs: `https://api.mainnet-beta.solana.com`, `https://api.mainnet.solana.com`, and `https://solana-rpc.publicnode.com`. Duplicate URLs are removed, and keyed provider URLs are redacted from terminal errors. This reduces false reserve incidents caused by single-endpoint Solana RPC reachability failures inside the Worker runtime.

`hylo-solana` binds hyUSD to the Hylo exchange v2.0.5 account graph: discover the registry LUT, then re-read it with all SPL vaults, mints, and pool states in one bounded finalized `getMultipleAccounts` census. `minContextSlot` is only a lower bound, not a historical-slot selector; a changed registry fails closed, and the census's returned slot plus `getBlockTime` is the observation. Pinned program owners, Anchor discriminators, mint/vault links, and exogenous Pyth identities are verified without runtime PDA derivation. Per-LST DefiLlama prices roll up into one LST slice alongside cbBTC, HYPE, and USDC pools; sub-precision USDC dust is disclosed in details. Unknown LSTs, newly activated reviewed-inactive exogenous candidates, or liability reconciliation failure reject the attempt. Paused routes and collateral shortfalls retain measured composition with degraded warnings. The feed is independent on-chain dynamic-mix evidence with not-applicable freshness; exogenous completeness is bounded to the six SDK candidates reviewed on 2026-09-09.

Branch-balance adapters (`evm-branch-balances` and `liquity-v2-branches`) can set a per-branch `priceToken` when the measured collateral balance is a protocol receipt token whose live price should be sourced from a separate underlying token address. The balance still comes from the configured branch token; only the DefiLlama price lookup address changes. Branch-balance configs can also attach reviewed `sourceUrls` so redemption telemetry emitted from same-run on-chain reads carries source provenance into the Redemption Backstop API. Branch-balance reads additionally verify each configured token's on-chain `decimals()` in the same Multicall3 wave that reads the balances: a mismatch emits a fatal warning (rejecting the snapshot rather than storing a 10^12 valuation error), while a reverting `decimals()` on a non-ERC20 token keeps the configured scale with an `info` warning.

`evm-branch-balances` additionally accepts a per-branch `chain` override. It pins one block per chain, retains the primary `observedBlock`, and records all anchors in `details.observedBlocks`; RPC overrides apply only to the primary chain. Optional `balanceRead: { contract, selector, args }` reads a uint256 aggregate view instead of `balanceOf(holder)`, with ABI-word arguments and the same token-decimals identity check, including individual-call fallback. Optional `receipt: { kind: "compound-v2", exchangeRateSelector? }` converts cToken claims using `exchangeRateStored()` (default selector `0x182df0f5`) and the `18 + underlyingDecimals - cTokenDecimals` scale before pricing. A same-chain `priceToken` is required and checked against `underlying()`; unavailable conversion evidence rejects the snapshot. JPYT reads both Optimism aOptUSDT and Base aBasUSDC as reviewed 1:1 receipts priced through USDT/USDC. EURO3 reads the Linea and Polygon `VaultFactory.collateral(token)` inventories, including Mendi receipt conversion and Polygon WMATIC, WETH, DAI and USDC.e. Its Polygon token identities use verified canonical supported contracts: typographical errors in the documentation had caused the prior curated census to omit three positive positions. Neither non-USD coin uses the adapter's USD-denominated debt-ratio option.

NUSD binds `evm-branch-balances` on the Ethereum Synapse nUSD pool (`0x1116898DdA4015eD8dDefb84b6e8Bc24528Af2d8`) with DAI/USDC/USDT branches, publishing the pool's live stablecoin composition.

Honey's `evm-branch-balances` binding is custody-aware. Its configured branch holders are the HoneyFactory collateral vaults, and a vault whose `custodyInfo()` reports custody mode holds no idle collateral: the backing is the factory-owned net vault shares (vault `balanceOf(factory)` minus `collectedAssetFees`) converted through the vault's `convertToAssets(...)`. That conversion is read in the same Multicall3 round as the redemption-capacity block and replaces the idle `balanceOf(holder)` balance for custody-mode branches, so composition and redemption capacity observe the same custody mechanism. When those share/asset reads are unavailable the adapter fails closed with an error instead of publishing the zero idle balance.

Adapters can also pass browser-style request headers through the shared JSON retry helper when an upstream is sensitive to request origin hints. Accountable (APXUSD), Ethena, Mento, OpenEden, and Reservoir use this because production failures showed Cloudflare Worker requests intermittently receiving HTML, 404, or network failures while the same endpoints still served healthy JSON to browser-like clients. Mento, OpenEden, and Reservoir additionally retry with the neutral Pharos fetch identity when the browser-style request fails. The Zephyr Scanner adapter is coin-aware: ZSD consumes the protocol-wide ZEPH reserve, while ZYS consumes the timestamped ZSD yield-reserve balance and circulating ZYS shares. The ZYS path emits the exact tracked ZSD dependency only after the published share rate reconciles with reserve-per-share within 0.5%; stale snapshots remain ineligible under the normal dashboard freshness limit.

USDe's Ethena reserve config also keeps Ethena's main-domain collateral API as a same-provider JSON fallback for cases where the `app.ethena.fi` API route returns the dashboard HTML shell to Worker-origin requests.

Ethena's positions API publishes most backing under the source-native `Liquid Cash` identity. The adapter labels that row `Liquid Cash strategy basket` and assigns the existing `medium` tier because Ethena's companion strategy-breakdown API shows that it combines liquid stablecoins with DeFi lending, RWA, institutional lending, and crypto-basis exposure; it is not modeled as a pure low-risk stablecoin bucket. For the same reason, the adapter does not publish that aggregate row as immediate redemption capacity. Without a defensible liquid-only value in the positions payload, the Redemption Backstop retains USDe's reviewed 0.5% hot-buffer fallback. Collateral freshness now demands timestamp coverage over every material row: when the payload omits timestamps for some rows, the adapter degrades with a `source-timestamp-coverage` warning naming the uncovered count instead of letting the remaining rows' clock stand in for the whole composition.

DOLA's Inverse FiRM market adapter normalizes the upstream `timestamp` field through the shared timestamp parser because the API can emit JavaScript millisecond timestamps while reserve validation requires Unix seconds.

### Snapshot Metadata and Warning Effects

Successful adapters can attach structured snapshot metadata that is stored on the authoritative snapshot row (`reserve_composition.metadata`), not on the mutable latest-attempt state.

Common metadata fields:

| Field                                                                                                                        | Meaning                                                                                                                                     |
| ---------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------- |
| `sourceTimestamp`                                                                                                            | Upstream disclosure timestamp, when independently verified                                                                                  |
| `freshnessMode`                                                                                                              | `verified`, `unverified`, or `not-applicable`                                                                                               |
| `collateralizationRatio`                                                                                                     | Assets ÷ liability ratio, emitted only when the adapter measures both sides over the same scope; adapters must never publish a share price or an accounting identity here                          |
| `details.sharePrice`                                                                                                         | Yield-bearing wrapper's assets-per-share price when the feed exposes one (Astherus, Blast yield manager)                                                                                  |
| `details.navConsistencyRatio`                                                                                                | ERC-4626 `convertToAssets(totalSupply)` ÷ `totalAssets()` conformance check — an accounting-consistency diagnostic, not solvency coverage                                                       |
| `details.freshnessSource` / `details.freshnessReason`                                                                        | Adapter-supplied explanation when freshness is explicitly unverified                                                                        |
| `unknownExposurePct`                                                                                                         | Share of reserve value that could not be mapped cleanly                                                                                     |
| `supplyUsd`, `totalReserveUsd`                                                                                               | Adapter-level reserve / supply totals when exposed                                                                                          |
| `totalAssetsUsd`, `totalLiabilitiesUsd`, `shareholderEquityUsd`                                                              | Raw attestation balance-sheet totals for coarse issuer feeds                                                                                |
| `buyFeeBpsMin`, `buyFeeBpsMax`                                                                                               | Optional raw buy-fee range context retained alongside normalized `redemption.feeBps`                                                         |
| `immediateRedeemableUsd`, `immediateRedeemableRatio`                                                                         | Legacy-read-only. Pre-nested flat capacity telemetry; decoded into `redemption.capacityUsd` / `redemption.capacityRatioOfSupply`. No producer writes these. |
| `redemptionFeeBps`                                                                                                           | Legacy-read-only. Pre-nested flat fee telemetry; decoded into `redemption.feeBps`. No producer writes this.                                       |
| `redemption.capacityUsd`, `redemption.capacityRatioOfSupply`                                                                 | Current redeemable-capacity telemetry reused by redemption backstops; all producers use this nested contract                                |
| `redemption.capacityKind`, `redemption.freshnessKind`                                                                        | Typed redemption evidence tier and freshness basis used by redemption-backstop validation                                                   |
| `redemption.routeStatus`, `redemption.routeStatusSource`, `redemption.routeStatusReason`, `redemption.routeStatusReviewedAt` | Optional current route availability signal and provenance, separate from reserve-sync status                                                |
| `redemption.sourceTimestamp`, `redemption.sourceUrls`                                                                        | Optional redemption-specific source timestamp and source URLs carried through to the redemption API/UI                                      |
| `redemption.settlementDelaySec`, `redemption.queueDepthUsd`                                                                  | Optional queue/delay context for redemption routes that are current but not atomic                                                          |
| `redemption.dailyLimitUsd`, `redemption.minRedeemUsd`                                                                        | Optional adapter-emitted daily redemption limit and minimum redemption size constraints                                                     |
| `redemption.holderEligibility`                                                                                               | Optional live holder-eligibility context when the adapter can sharpen the static route model                                                |
| `redemption.feeBps`                                                                                                          | Normalized current redemption fee in the nested telemetry contract                                                                          |
| `redemption.v9RouteAttempt`                                                                                                  | V9-only accepted or rejected exact route attempt; excluded from standalone public reserve and redemption semantics                           |

`details.freshnessSource = "same-run-render-clock"` marks a verified snapshot whose timestamp is the source's own render or response clock — the instant the payload was rendered or served — rather than an independently dated as-of time for the reserve state (Mento's dashboard SSR fetch time, Flying Tulip's `lastUpdated`, DOLA FiRM's response timestamp). Such clocks are accepted as `verified` per the settled freshness policy, and the basis is named explicitly so consumers can distinguish a render clock from a dated disclosure.

Redemption telemetry is validated before persistence. Negative capacity or constraint values, capacity ratios outside `0..1`, negative fees, malformed redemption source URLs, invalid redemption freshness/capacity/holder-eligibility enum values, invalid `routeStatusReviewedAt` dates, capacity emitted by adapters that do not declare redemption-capacity support, fee telemetry emitted by adapters that do not declare fee support, and direct/proxy/queue capacity evidence emitted by an incompatible adapter capability fail the adapter output validation. Queue capacity that omits queue depth, settlement delay, or daily-limit semantics is stored only with a degraded warning. Legacy flat capacity/fee fields (`immediateRedeemableUsd`, `immediateRedeemableRatio`, `redemptionFeeBps`) are no longer written: the store-row decoder still reads them from historical rows (30-day retention) and maps them into the nested `redemption` shape at decode time, but every other consumer and all producers use the nested contract.

Same-run adapters populate `routeStatusSource` only when the already-fetched source payload explicitly supports current route status. Adapters that derive it from same-run on-chain reads (`routeStatusSource: "onchain"`) include GHO GSM freeze/seize checks, Cap vault pause checks, Liquity V2 branch shutdown checks, the Mezo (`liquity-native-active-pool`) TCR/MCR checks, M0 `swapFacility` pause checks, Resupply pair stability checks, Liquity V1 / Yamato / Sky MakerCore / ERC-4626 single-asset / collateral-positions-api reads, the Ethena mint/redeem contract read, the InfiniFi redeem-controller probe, the Reservoir USDC PSM `paused()` read, and the Superstate RedemptionIdle balance read, among others; a separate group (Falcon, fx, OpenEden, re-metrics, JupUSD, FPI) derives it from the protocol API (`routeStatusSource: "protocol-api"`).

`freshnessMode = "not-applicable"` is the expected scoring-eligible path for intrinsically current reads such as `evm-branch-balances`, `liquity-v1`, `liquity-v2-branches`, `liquity-native-active-pool`, `m0-wrapper-underlying`, `escrow-balance`, `btcfi`, `xdai-bridge`, `hive-hbd-protocol`, `usdai-hub`, and `collateral-positions-api`, where reserve composition comes from latest-state contract/API state rather than a separately timestamped disclosure. Timestamp-less APIs that expose balance-sheet snapshots without a reviewed latest-state contract remain `freshnessMode = "unverified"` and are detail-visible but not score-grade collateral. `reservoir` remains in this unverified bucket because its balance-sheet payload carries no independently verifiable source timestamp or reviewed latest-state proof. Its broader stable-bucket liquidity is retained as diagnostic context, while normalized redemption capacity is fail-closed to the classified USDC slice returned by the modeled terminal route; route-specific policy governs whether that unverified capacity can be used.

Mento reserve sync combines the composition payload from Mento's analytics API with the dashboard's embedded reserve payload timestamp and its per-stablecoin `cdp_backings` totals. When that dashboard timestamp parses successfully, the `mento` adapter emits verified freshness for USDm and Mento CDP assets, stamped `same-run-render-clock` because the dashboard timestamp is its own SSR render clock rather than a dated disclosure; if the dashboard timestamp disappears, it falls back to explicit unverified freshness rather than guessing. For CDP stablecoins the adapter additionally cross-checks the dashboard's per-stablecoin collateral/debt totals against the analytics API's summed trove totals and fails the attempt closed when the two sources diverge by more than 1%, since one of them is then provably wrong.

InfiniFi reserve sync hydrates freshness the same way: the protocol `/data` payload carries no timestamp, so the `infinifi` adapter probes the transparency dashboard's siUSD rate-history series (written by the same backend snapshotter on a 2-hour cadence) and emits verified freshness from the latest point only when that point's rate matches the live staked exchange rate in the reserve payload. A failed probe or a cross-check mismatch falls back to explicit unverified freshness. The registry caps that probe at 21,600 seconds (6 hours, three missed 2-hour snapshotter writes) instead of the generic three-day dashboard cap, and the rate cross-check tolerance tightens from 5e-4 to 6e-5 — the ±5e-5 rounding envelope plus a sliver of sub-cadence drift — so multi-day accumulator drift now fails the probe into unverified freshness.

The registry now also declares the admissible `freshnessMode` set per adapter family so timestamp-backed disclosures, latest-state on-chain proofs, and explicitly unverified dashboard/API feeds cannot silently drift into undocumented freshness semantics. Legacy snapshots persisted before this contract decode an absent `freshnessMode` as the adapter's declared default (`preferredFreshnessMode`, else the single `allowedFreshnessModes` entry, else `unverified`) and tag `metadata.diag.freshnessModeLegacyDefault = true`, so pre-contract rows stay scoring-eligible on first deploy instead of waiting for their next successful sync; newly written rows always carry an explicit mode and are unaffected.

Observed bad states (E4/P3), including reserve shortfalls, genuinely reported non-positive liabilities, and paused routes, publish available slices and ratios with `degraded` warnings rather than adapter errors; only unreadable, malformed, or unverifiable evidence fails closed, and undefined ratios are omitted rather than fabricated.

Warnings now carry both a display `severity` and an execution `effect`:

| Effect     | Meaning                                                                                                                          |
| ---------- | -------------------------------------------------------------------------------------------------------------------------------- |
| `info`     | Informational only; the snapshot can still be stored and remain `ok`                                                             |
| `degraded` | Snapshot is stored, but the per-coin sync state becomes `degraded` and that snapshot is excluded from independent scoring passthrough (judged on the snapshot's own warnings via `selectScoringDegradedWarnings`, so a later failed attempt never demotes a clean prior snapshot) |
| `fatal`    | Snapshot is rejected and the attempt is recorded as an `error`                                                                   |

---

## Cron Behavior

`runFourHourlyReserveSyncSlot()` in `worker/src/handlers/scheduled/hourly-live-reserves.ts` runs the reserve cron on its own 4-hourly trigger so reserve-adapter fetches do not compete with the 30-minute scoring lane or the daily 08:00 jobs.

`syncLiveReserves()`:

1. Filters `ACTIVE_STABLECOINS` to the coins that declare `liveReservesConfig`.
2. Orders the queue by adapter evidence class (`independent`, then `static-validated`, then `weak-live-probe`) and packs coins sharing a `source-invariant` adapter contiguously, so run-budget truncation defers weak probes before score-grade feeds and shared fetches are reused immediately. The durable scheduler checkpoint stores the one next-item resume pointer; a resumed run processes only that deferred suffix and does not wrap back to the queue head.
3. Resolves an adapter from `worker/src/cron/reserve-adapters/index.ts`.
4. Builds a breaker key as `live-reserves:${breakerScope ?? adapter}`.
5. Checks the per-source circuit breaker before each coin fetch.
   Each primary or fallback adapter attempt receives its own start time as `nowSec`, so time spent earlier in the queue cannot make current chain heads appear to be in the future.
6. Persists either a fresh snapshot (`reserve_composition`) plus sync-state row, or an error/degraded/skipped sync-state row.

The `reserve-recovery` lane retains its five-minute trigger. In `recover` mode it fences abandoned in-flight attempts, claims a compatible ready pointer, and replays the suffix after active leases permit takeover. Before scanning, it retires up to 25 incompatible historical checkpoints per tick only when a newer current-hash full cohort and its slot completed successfully. Retirement atomically marks the exact checkpoint `failed` with `checkpoint-superseded-by-newer-full-cohort`, records supersession, and fences only its matching pending domain attempt. Unexpired reserve-child or recovery leases and fresh slot heartbeats prevent retirement; leases themselves are never deleted. Remaining incompatible debt with no ready or eligible checkpoint produces degraded status and `statusCause = "reserve-recovery-zero-eligible-incompatible"` rather than green idle status. Metadata reports `retiredCheckpoints` and post-retirement eligibility. `off` keeps the sweep but skips recovery scans. The checkpoint pointer is the sole run-level resume mechanism; the cache table is not used for reserve cursor ownership.
**Adapter output validation:** After each adapter returns, `validateAdapterOutput()` checks
that all slice `risk` values are valid enum members, all `pct` values are finite and strictly positive,
the slice list is non-empty, and the sum is within 2 points of 100%. Invalid output is treated as
an error. Sum deviation above 0.5 points produces a `degraded` warning, while deviation above 2 points
hard-fails the adapter output with a `fatal` warning.

Source freshness validation also rejects source timestamps that are more than 10 minutes in the future, including nested redemption telemetry timestamps, so upstream clock mistakes or millisecond/second parsing errors cannot be treated as fresh snapshots. Multi-row reserve feeds should use the oldest material contributor timestamp as `sourceTimestamp` for verified freshness and can persist `latest*Timestamp` / `sourceTimestampSpreadSec` metadata for diagnostics.

Adapters can also declare shared validation policy in the registry:

- `maxSourceAgeSec`: if adapter metadata includes `sourceTimestamp` and the upstream disclosure is older than the policy allows, the sync is marked `degraded`
- `maxUnknownExposurePct`: if adapter metadata includes `unknownExposurePct` and unmapped reserve exposure is material, the sync is marked `degraded`
- `allowedFreshnessModes`: explicit per-adapter freshness contract enforced by output validation
- when `maxSourceAgeSec` exists but the adapter can only attest freshness heuristically, `freshnessMode = "unverified"` produces an informational `freshness-unverified` warning instead of forcing a degraded status on the sync itself; the snapshot still cannot enter collateral scoring
- late monthly disclosures that routinely publish after the next calendar month starts use `LATE_MONTHLY_DISCLOSURE_SOURCE_MAX_AGE_SEC = 4000000` rather than ad hoc per-coin caps
- timestamp-less dashboard/API feeds should also populate `details.freshnessSource` / `details.freshnessReason` so the lack of verified recency is explicit in stored metadata
- `preferredFreshnessMode` records the honest target (`verified` or `not-applicable`) separately from tolerated `unverified` fallback. Adapters with no usable upstream reserve timestamp instead declare `freshnessLimitation`; they are not missing-probe promotion candidates. The coverage audit separates latest-known tolerated-not-preferred observations ("probe not built") from these upstream limitations. Supply `--reserve-states <file>` with an array (or `{reserves: [...]}`) of `/api/stablecoin-reserves/<id>` responses; without those snapshots it reports observations as unavailable, not inferred from configuration.

Source-age tiers are publisher-wide admission ceilings, not fetch intervals:

| Tier / policy constant | Maximum age | Reviewed use |
|---|---:|---|
| `DASHBOARD_SOURCE_MAX_AGE_SEC` | 3 days | Timestamped reserve dashboards; faster publishers may declare a tighter bound |
| `WEEKLY_SOURCE_MAX_AGE_SEC` (same duration as `DISCLOSURE_SOURCE_MAX_AGE_SEC`) | 7 days | Circle's weekly reserve chart, not its monthly assurance report |
| `MONTHLY_DISCLOSURE_SOURCE_MAX_AGE_SEC` | 33 days | Monthly disclosures |
| `LATE_MONTHLY_DISCLOSURE_SOURCE_MAX_AGE_SEC` | 4,000,000 seconds (46.3 days) | Reviewed late-published monthly reports, including AUDX, FDUSD, Paxos and StraitsX |
| `QUARTERLY_ASSURANCE_MAX_AGE_SEC` | 100 days | Schuman EUROP quarterly assurance, including publication grace |
| `QUARTERLY_DISCLOSURE_SOURCE_MAX_AGE_SEC` | 10,000,000 seconds (115.7 days) | Legacy disclosure tier, not the EUROP assurance allowance |
| `BUSINESS_DAY_NAV_SOURCE_MAX_AGE_SEC` | 5 days | Business-day NAV publication |

Per-coin `scoring.maxSourceAgeSec` may only tighten a declared adapter cap; the shared config schema rejects widening. Omit redundant equal values and inherit the publisher policy. Where an adapter has no age cap, a coin may supply one (for example, reviewed Chainlink NAV windows); latest-state `not-applicable` freshness still bypasses source-age checks. The September 2026 review removed obsolete 4,000,000-second dashboard overrides and duplicated assurance caps rather than widening publishers to clear alerts. EUROP now expires after 100 days instead of its former 10,000,000-second allowance, so an overdue report can lose score-grade eligibility until a newer examined period is published.

For on-chain branch-balance adapters, branches marked with `depType: "wrapper"` are not treated as plain USD-pegged wrappers for the depeg guard. This avoids false degraded states for yield-bearing wrappers such as sUSDe whose market price can intentionally rise above one dollar while the reserve slice still links dependency risk to the underlying asset.

Unknown exposure now follows one repo-wide policy:

- quantify it and persist `unknownExposurePct` when the adapter can do so honestly
- emit an explicit unknown slice when the reserve mix would otherwise hide that exposure
- use threshold-driven warning effects so immaterial unknowns stay informational and material unknowns degrade the sync
- fail closed when the adapter cannot quantify the missing exposure without inventing precision

These policy warnings preserve last-known live detail/status data but exclude the affected snapshot from V9 Backing/dependency compilation. Admission judges the snapshot's own warnings, not the latest attempt's status.

**Circuit breaker recording:** Breaker outcomes are deferred until the entire sync loop
completes, recording the worst outcome per breaker key (failure trumps success). This
prevents first-coin-wins bias where a successful early coin would mask failures of later
coins sharing the same breaker key. Finalization records failure outcomes first and
skips success heartbeats for breakers that are already closed with no failure debt;
those closed-success skips avoid rewriting hundreds of live-reserve circuit rows at
the end of every healthy run. Only upstream HTTP/network failures contribute negative source-health outcomes. Configuration, output validation, corroboration disagreement, schema-shape, D1/storage, and run-budget failures are neutral: they remain coin failures without opening a shared source breaker. Failure metadata distinguishes `storage-write` (including `D1_ERROR`), `corroboration-mismatch`, `schema-shape`, and `run-budget-exhausted`.

Cron result statuses:

| Status     | When                                                                                                                                                                                                                  |
| ---------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `ok`       | At least one configured coin synced, no coin was budget-deferred (`deferredCoins === 0`), and `failed + skipped <= ceil(total * 0.1)`                                                                                 |
| `degraded` | At least one configured coin synced and either `deferredCoins > 0` or `failed + skipped > ceil(total * 0.1)`; OR no coin synced but every skip was a circuit-breaker hold (no real failures, no budget-deferred tail) |
| `error`    | No configured coin synced and at least one coin truly failed or was budget-deferred (`failed > 0` or `deferredSkipped > 0`)                                                                                           |

Per-coin warnings still matter operationally, but they affect `reserve_sync_state.last_status` for that coin (`degraded`) and the cron metadata warning list, not the run-level `CronResult.status`.

The cron loop remains sequential with at most two adapter HTTP/RPC operations in flight, preserving the six-connection trigger budget. The leased wrapper grants 12 minutes; the internal run budget is 9 minutes. Admission reserves one 20-second attempt, 30-second D1 finalization window, and 5-second margin. Every admitted coin shares the absolute run deadline minus that margin across checkpoint, begin, breaker read, fallback chain, and terminal writes. Primary and fallback attempts each receive at most 20 seconds and cannot extend the shared adapter-chain deadline (which reserves finalization time). Checkpoint/begin/failure writes are bounded by the lifecycle deadline; SQL deadline/ownership fences prevent late D1 work from gaining authority. A timed-out or ownership-rejected begin stops the run with its checkpoint retained, never advances an ambiguous pointer. Success still uses authoritative readback on ambiguous acknowledgement.

Run metadata exports in-memory `phaseTimingsMs.stages` for checkpoint starts, breaker reads, begin writes, failure writes, authoritative writes, and progress reporting alongside aggregate setup/queue/adapter/D1/finalization timings. Adapter telemetry uses `ioActivityBurstCount` for zero-active-to-active I/O transitions, not concurrency “waves”. Status snapshot compaction preserves the producer-bounded latency groups (128 groups / 36 KiB) and histogram samples rather than silently truncating them to 40; producer overflow remains explicitly counted. Per-attempt `durationMs` lives under composition `metadata.diag`, excluded from the payload digest and public responses.

The cron reports `setup`, `syncing`, and `finalizing` progress. Budget exhaustion defers untouched tail rows as skipped with `failureCategory = "run-budget-exhausted"`; these are capacity pressure, not source failures, and are excluded from persistent-stale independent alerts. Deferred rows clear attempt/pending IDs and checkpoint ownership retains the resume pointer. Metadata exports `runBudgetTruncated`, `deferredCoins`, `nextCursorStablecoinId`, and resolved budgets. Exhausted finalization-tail budgets skip remaining breaker writes, artifact cleanup, and history pruning explicitly. Success composition/state and both authoritative-gated histories now commit atomically; there is no post-success history-gap warning or resume-repair hook. Source-invariant adapters alone reuse complete config-keyed source results. Cleanup removes sync-state and breaker artifacts for removed live sources.

Artifact cleanup remains best-effort, but the cron metadata now records `artifactCleanup` delete counts, `artifactCleanupWarningCount`, and structured `artifactCleanupWarnings` when cleanup fails. `/status` and the admin status dashboard summarize non-zero cleanup deletes and cleanup warning counts so ghost-artifact cleanup regressions do not require log scraping.

---

## Storage Model

### `reserve_composition`

Latest successful live snapshot per live-enabled coin.

| Column                                            | Meaning                                                                                               |
| ------------------------------------------------- | ----------------------------------------------------------------------------------------------------- |
| `stablecoin_id`                                   | Canonical Pharos coin ID                                                                              |
| `slices`                                          | JSON-serialized `ReserveSlice[]`                                                                      |
| `fetched_at`                                      | Unix timestamp of the successful snapshot                                                             |
| `source`                                          | Adapter key that produced the snapshot                                                                |
| `metadata`                                        | Snapshot-scoped adapter telemetry (freshness, redeemable capacity, live fee, disclosure totals, etc.) |
| `warning_count` / `warnings`                      | Warning summary persisted alongside the successful snapshot                                           |
| `adapter_source_model` / `adapter_evidence_class` | Registry-derived classification copied onto the snapshot row for authoritative reads                  |
| `attempt_id`                                      | Attempt-fencing identifier for the successful snapshot when the writer stamped one                    |
| `config_fingerprint`                              | SHA-256 of canonical adapter/version/semantics/inputs/params; missing legacy values are temporarily accepted with a diagnostic until the next successful write |
### `reserve_composition_history`

Append-only index of successful live snapshots. Each row carries the attempt ID, adapter classification, and a nullable `payload_sha256` digest of the snapshot payload excluding `metadata.diag`; `slices`, `metadata`, and `warnings` are intentionally empty because `reserve_sync_attempt_history` is the append-only source for attempt payload metadata. Non-null attempt IDs are unique per coin and inserts are idempotent. Both histories commit in the same D1 batch as the composition and success state, gated on the resulting authoritative attempt. A history failure rolls back the entire success; ambiguous acknowledgements use authoritative readback. The 4-hourly cron prunes history older than 30 days while preserving frozen coins and referenced attempts.

### `reserve_sync_state`

Per-coin operational state for the most recent attempt.

| Column                                                               | Meaning                                                                                                                                   |
| -------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------- |
| `stablecoin_id`                                                      | Canonical Pharos coin ID                                                                                                                  |
| `adapter_key`                                                        | Adapter used for the most recent attempt                                                                                                  |
| `breaker_key`                                                        | Per-source circuit-breaker key                                                                                                            |
| `last_attempted_at`                                                  | Unix timestamp of the latest attempt                                                                                                      |
| `last_success_at`                                                    | Unix timestamp of the latest successful live snapshot                                                                                     |
| `last_status`                                                        | `ok`, `degraded`, `error`, or `skipped`                                                                                                   |
| `warning_count`                                                      | Number of adapter warnings                                                                                                                |
| `warnings`                                                           | JSON-serialized warning objects                                                                                                           |
| `last_error`                                                         | Latest failure message, if any                                                                                                            |
| `metadata`                                                           | Attempt-scoped operational metadata only (for example skip/failure reasons or warning-effect counts), not authoritative reserve telemetry |
| `last_attempt_id` / `pending_attempt_id` / `last_success_attempt_id` | Attempt-fencing identifiers used to reject orphaned or partially written live snapshots                                                   |
| `config_fingerprint`                                                | Canonical configuration fingerprint for the latest attempt; written at begin and finalization |

### `reserve_sync_attempt_history`

Append-only source of all reserve-sync attempt payload metadata, including `ok`, `degraded`, `error`, and `skipped` outcomes with warnings, error message, and attempt-scoped metadata. Non-null attempt IDs are unique per coin and inserts are idempotent. The composition-history digest is computed from the successful snapshot payload while this table remains the durable source for warning/metadata payloads. The 4-hourly reserve cron prunes rows older than 30 days with the same frozen-coin and referenced-attempt preservation as the composition history.

Scheduling deferrals use an explicit history mode: null-attempt `skipped` rows record the deferred outcome even when the state upsert deliberately preserves an existing healthy snapshot. They do not claim successful snapshot authority; actual attempt finalizations remain gated on their completed attempt identity and state.

Scheduled healthy persistence uses six SQL statements in three round trips: checkpoint start, SQL ownership-gated domain begin, and the four-statement authority/history batch. Begin checks the exact checkpoint identity and current coin/attempt pointer. It is never folded into its own success batch. Stage B checkpoint/start batching remains deferred until exact-owner gating and crash/ambiguous-acknowledgement recovery are proven. Failure finalization and its guarded attempt history use one atomic batch. Apply migration `0239_live_reserve_config_fingerprint.sql` before Worker activation; Worker rollback leaves its nullable additive columns intact.

Freshness and consistency rules now live across the `worker/src/lib/live-reserves/store*.ts` helper family, with `worker/src/lib/live-reserves/store.ts` kept as the public facade:

- `LIVE_RESERVE_FRESHNESS_SEC = 172800` (48 hours)
- A live snapshot only counts as consistent when `reserve_sync_state.last_success_at === reserve_composition.fetched_at` and, when attempt IDs are stamped, `reserve_sync_state.last_success_attempt_id === reserve_composition.attempt_id`
- Stored snapshots are parsed strictly: unreadable JSON, invalid payloads, empty slice arrays, invalid slices, or materially invalid sums are rejected instead of being partially served
- `resolveReserveResult()` is the canonical detail/API resolver used by `GET /api/stablecoin-reserves/:id`
- `computeReserveCompositionOverview()` is the status-summary resolver used by `/api/status` and `/admin/`
- `loadFreshIndependentLiveReserveMap()`, API `provenance.scoringEligible`, and overview `independentFreshEligible` reuse `evaluateLiveReserveAdmission()` with one evaluation clock. A retained clean snapshot remains eligible after a later failed attempt; operational error/degraded counts remain separate.
- transient network/upstream failures retain the last successful composition for reserve detail/status views while the current sync state records `error` / `skipped`; that composition keeps scoring until the freshness bound retires it, because the failed attempt wrote no snapshot and says nothing about the one on disk (before 2026-09-07 a single failed attempt dropped the coin onto the audited fallback, and one Tether `502` fanned out into ~580 inherited open data points)
- `getLatestSuccessfulReserveSnapshotMetadata()` is the canonical accessor for downstream consumers that need snapshot telemetry such as redeemable capacity or live redemption fees
- failed `reserve_sync_state` / `reserve_sync_attempt_history` rows now also retain `metadata.failureCategory` so parser drift, network issues, upstream HTTP failures, validation failures, and storage write failures are distinguishable without log grep
- authoritative `live` / `live-stale` API responses now also carry a `provenance` envelope plus a separate `displayBadge` so the frontend can distinguish true live feeds from curated-validated and proof-style reserve views

### Snapshot admission gates

The shared result is `{ eligible, reasons: AdmissionRejectionCode[] }`. All gates must pass for independent reserve scoring:

| Gate | Rejection code | Rule |
| --- | --- | --- |
| Current configuration | `unconfigured`, `suspended` | The coin must have an unsuspended live configuration. |
| Snapshot authority and integrity | `missing-snapshot`, `inconsistent-snapshot` | Strictly decoded slices and matching positive success/fetch timestamps; matching nonempty attempt IDs when either is stamped. |
| Semantic configuration identity | `config-mismatch` | Stored fingerprint equals SHA-256 of canonical `{adapter, version, semantics, inputs, params}` (`params` defaults to `{}`). Display/scoring settings do not change the digest. Missing legacy fingerprints are accepted with a diagnostic until the next successful write stamps one. A mismatch is not served as current live detail or redemption metadata. |
| Evidence class | `non-independent` | Only `independent` reserve evidence qualifies. |
| Fetch and source age | `stale` | Fetch age is at most 48 hours; verified source age uses the tighter coin/adapter cap, falling back to 48 hours. |
| Explicit freshness | `invalid-freshness` | Require `verified` plus a finite positive timestamp no more than ten minutes ahead, or explicit `not-applicable`. Absent/malformed modes or timestamps cannot become verified through decoding. |
| Snapshot warnings | `degraded-snapshot` | Apply the current per-coin warning policy to the retained snapshot, not the newest attempt. |
| Slice count | `insufficient-slices` | At least the requested count (one by default). |

This fixes the HBD 164-versus-165 discrepancy: its later node-disagreement error remains an operational error, while its clean retained snapshot contributes to eligible coverage in all three consumers. Redemption capacity consumes the same structural, configuration-identity, and freshness rejections but retains its separate evidence-class and degraded-telemetry policy; independent reserve composition is not a prerequisite for valid same-run redemption telemetry.

`computeReserveCompositionOverview()` aggregates the status-card summary used by `/status`:

- `configuredCoins`
- `freshCoins`
- `staleCoins`
- `missingCoins`
- `degradedCoins`
- `errorCoins`
- `corruptCoins`
- `independentFreshEligible`
- `independentFreshUnverified`
- `staticValidatedFresh`
- `weakProbeFresh`
- `persistentlyStaleIndependentCoins` (independent feeds older than the persistent-staleness window that can escalate status beyond normal short-lived lag; includes old active failures and circuit-open skips, but not run-budget deferred rows)
- `writeTimeoutUncertain`
- `deferredCoins`
- `runBudgetTruncated`
- `deferredAt`
- `nextCursorStablecoinId`
- `cursorRecordedAt`
- `lastSuccessAt`
- `oldestFreshAgeSec`

`errorCoins` includes active adapter failures even before a coin has ever produced a successful live snapshot; those rows no longer remain hidden inside `missingCoins`.
`corruptCoins` counts rows where a matching latest-success snapshot exists in D1 but fails strict integrity validation, so the system fails closed to fallback presentation instead of serving truncated or malformed live data.
When a coin has both an old latest-success snapshot and a newer failing attempt state, the overview now prioritizes the active `error` / `degraded` attempt classification over generic `stale` labeling so status surfaces better reflect live incidents.
`writeTimeoutUncertain` counts coins whose latest attempt hit the D1 write-timeout / finalize-rejection path and whose authoritative success state could not be proven by readback.
`runBudgetTruncated`, `deferredCoins`, `deferredAt`, and `nextCursorStablecoinId` mirror the newest checkpoint attempt when that attempt still has an active next-item resume pointer, so terminal newer attempts cannot resurrect older orphaned pointers on status surfaces. The pointer survives a run-budget truncation and is replayed by the five-minute `reserve-recovery` lane; it is cleared only when the checkpoint reaches the queue end. Atomic authority/history persistence removes the history-gap reconciliation and repair subsystem.

---

## API Contract

`handleStablecoinReserves()` in `worker/src/api/stablecoin-reserves.ts` is only available for known tracked IDs that also declare `liveReservesConfig`.

404 behavior:

- Unknown stablecoin ID
- Tracked coin without live reserve support

Known live-enabled IDs with no usable snapshot or curated/template fallback return HTTP `200` with `mode: "unavailable"`, empty `reserves`, and live `sync` state. This distinguishes supported coins awaiting usable data from unknown or unsupported IDs.

Successful responses return `StablecoinReservesResponse` with one of these modes:

| Mode                | Meaning                                                                                    |
| ------------------- | ------------------------------------------------------------------------------------------ |
| `live`              | Fresh live snapshot from `reserve_composition`                                             |
| `live-stale`        | Stored fetch is older than 48 hours, or verified source evidence exceeds its configured/adapter reporting allowance (48-hour fallback only when neither declares one) |
| `curated-fallback`  | Live snapshot unavailable; falling back to curated `StablecoinMeta.reserves`               |
| `template-fallback` | Live snapshot unavailable; falling back to reserve templates from `getReserves()`          |
| `unavailable`       | Coin is live-enabled, but neither live data nor fallback reserve presentation is available |

`live` / `live-stale` only apply when the stored snapshot matches the latest successful sync state by `fetched_at` / `attempt_id` and passes strict integrity validation. Staleness independently checks the stored fetch time against 48 hours and verified upstream source age against the source-specific allowance. The detail response, overview freshness counts, and scoring snapshot loader share this decision; refreshing an old document never resets its source age. Orphaned partial writes or corrupt stored snapshots fail closed to the fallback modes.

`StablecoinReservesResponseSchema` in `shared/types/live-reserves.ts` is the runtime contract for successful `200` responses, including `unavailable`, and is used by the frontend reserve API client. Adapter-specific `metadata`, `metadata.details`, and nested redemption telemetry remain passthrough. Internal instrumentation lives in declared `metadata.diag`, which `resolveReserveResult()` removes before public serialization.

Cache control:

| Response mode                               | Cache-Control                        |
| ------------------------------------------- | ------------------------------------ |
| `live` (sync `ok`, no uncertain write)      | `public, s-maxage=3600, max-age=300` |
| `live` (sync not `ok`, or `uncertainWrite`) | `public, s-maxage=300, max-age=60`   |
| `live-stale`                                | `public, s-maxage=1800, max-age=120` |
| fallback / unavailable modes                | `public, s-maxage=300, max-age=60`   |

The optional `provenance` object is present only when the response is serving an authoritative `live` or `live-stale` snapshot:

| Field             | Meaning                                                                               |
| ----------------- | ------------------------------------------------------------------------------------- |
| `evidenceClass`   | `independent`, `static-validated`, or `weak-live-probe`                               |
| `sourceModel`     | `dynamic-mix`, `validated-static`, or `single-bucket`                                 |
| `freshnessMode`   | Optional explicit freshness policy (`verified`, `unverified`, `not-applicable`)       |
| `scoringEligible` | Whether the current snapshot is eligible for V9 Backing/dependency fact compilation right now |

The optional `displayBadge` object is also present only for authoritative `live` / `live-stale` snapshots:

| Field   | Meaning                                                                                      |
| ------- | -------------------------------------------------------------------------------------------- |
| `kind`  | `live`, `curated-validated`, or `proof`                                                      |
| `label` | User-facing badge text rendered on the detail page (`Live`, `Curated-Validated`, or `Proof`) |

`displayBadge` is intentionally separate from `mode` and `provenance`:

- `mode` answers whether an authoritative snapshot exists and whether it is stale
- `provenance` answers scoring/evidence semantics
- `displayBadge` answers the honest user-facing reserve label

The optional `displayUrl` and `evidenceUrls` fields are also intentionally separate:

- `displayUrl` is the curated reserve-card destination configured in `liveReservesConfig.display.url`
- `evidenceUrls` are adapter-emitted URLs tied to the authoritative live snapshot metadata
- the detail page can show both when a coin has a curated overview page plus narrower evidence links for the exact live snapshot

The optional `metadata` object is also present only for authoritative `live` / `live-stale` snapshots. It exposes the adapter snapshot metadata already stored with the reserve snapshot row so the UI can surface feed-specific context without re-querying D1. For example, `crvusd` now exposes `yieldBasisCollateralPct` when Yield Basis positions account for part of the live reserve mix.

The optional `sync` object exposes the last operational state:

| Field             | Meaning                                                                                                                             |
| ----------------- | ----------------------------------------------------------------------------------------------------------------------------------- |
| `enabled`         | Coin is live-enabled                                                                                                                |
| `status`          | Last sync status (`ok`, `degraded`, `error`, `skipped`)                                                                             |
| `stale`           | Latest successful live snapshot is older than 48 hours                                                                              |
| `bootstrap`       | No successful live snapshot has been recorded yet                                                                                   |
| `lastAttemptedAt` | Latest attempt timestamp, when present                                                                                              |
| `lastSuccessAt`   | Latest success timestamp, when present                                                                                              |
| `warnings[]`      | Warning messages surfaced by the latest attempt and, when relevant, storage-integrity warnings injected by the fail-closed resolver |
| `lastError`       | Most recent adapter error message (truncated to 200 chars), when present                                                            |
| `failureCategory` | Machine-readable failure class from attempt metadata, when present                                                                  |
| `uncertainWrite`  | `true` when the latest attempt hit the D1 write-timeout / finalize-rejection path and authoritative state could not be proven       |

Uncertain write attempts are intentionally exposed as `sync.uncertainWrite = true` instead of being collapsed into generic stale/error narration. The API may still serve the last consistent snapshot or fallback presentation, but operators can tell that the latest attempted write is ambiguous until a clean follow-up run resolves it. Detail-page polling uses the normal 4-hour reserve cadence only for clean live responses; live responses with `sync.status !== "ok"` or `sync.uncertainWrite = true` use recovery polling and render the active status, failure category, and last error above the reserve card footer.

### Edge Cache Implications for Monitoring

A `live` response with a healthy sync (`sync.status === "ok"` and no uncertain write) is edge-cached for 1 hour (`s-maxage=3600`). When the latest sync is not `ok` or has an `uncertainWrite`, the same `live` response uses the fallback edge cache (`s-maxage=300`, 5 minutes) so the frontend's 2-minute recovery polling can observe the failure and its recovery instead of being served a stale healthy `sync` block for up to an hour.

**For real-time monitoring**, use the Access-gated admin surface (`/admin/`, backed by admin-only `GET /api/status`), which is never edge-cached and always reflects current D1 state.

Fallback, template-fallback, and unavailable responses use a shorter edge cache (`s-maxage=300`, 5 minutes). `live-stale` responses use an intermediate cache (`s-maxage=1800`, 30 minutes); healthy live responses use `s-maxage=3600` (1 hour).

---

## Adapter Registry

`worker/src/cron/reserve-adapters/index.ts` owns the registered adapter keys; `shared/types/live-reserve-adapter-declarations.ts` owns each adapter input, evidence, validation, and redemption contract; stablecoin registry configs own current usage counts. Derive the live roster and counts from those sources rather than copying a volatile table here. A registered adapter may intentionally have zero configured coins while it is parked for a future binding.

`reserve-protocol-dtf` accepts only direct `onchain-evm` observations; the unused
discovery-API path has been removed. It reads the RToken `main()`,
`basketsNeeded()`, BasketHandler `quote(...)` / `fullyCollateralized()`, and
AssetRegistry asset plugins for component prices and collateral status, so its
freshness mode is `not-applicable`.

`resupply-pairs` reads the reviewed Resupply pair allowlist from config, then
each pair's current on-chain accounting and collateral vault conversion to
aggregate positive collateral assets by reviewed underlying stablecoin
(`crvUSD` / `frxUSD`). It fails closed on unmapped positive-collateral pairs
and uses `freshnessMode: not-applicable` because the source is latest Ethereum
state. When a reviewed `redemptionHandlerAddress` is configured, the adapter
also reads `getMaxRedeemableDebt(pair)`, `guardEnabled()`,
`permissionlessPriceThreshold()`, and `reUsdOraclePrice()` from the Resupply
redemption handler in the same run. That nested redemption telemetry is
score-grade only while the permissionless guard is open; if the guard is closed
above the threshold, the route is surfaced as cohort-limited and remains
excluded from V9 Exit.
Each pair's valuation scale is verified rather than assumed: the second stage
reads the collateral vault's `asset()` and the underlying token's `decimals()`
alongside `convertToAssets(...)`, rejects a vault whose `asset()` is not the
pair's `underlying()`, and fails the adapter closed when the decimals read
fails instead of substituting 18 decimals for the borrow, collateral and
redemption-capacity amounts.

Adapter key intent is declared with the adapter descriptor and covered by the registry tests. Every registered key has one of these statuses:

| Status    | Meaning                                                                   |
| --------- | ------------------------------------------------------------------------- |
| `active`  | Bound by at least one active stablecoin `liveReservesConfig`              |
| `staged`  | Implemented for an approved upcoming binding, but not active yet          |
| `retired` | Kept only for historical compatibility while no new binding should use it |
| `parked`  | Retained intentionally while no active coin currently binds it            |

The retired `usdh-native-markets` and `abracadabra` adapters remain registered: their review dates (2026-10-11 and 2027-01-26) have not passed. The unbound `lista` adapter has been deleted; lisUSD stays deferred until every positive branch has a supported valuation path.

Current unbound registered adapters are explicit:

| Adapter               | Status    | Rationale                                                                                                                                                                                                                        | Parked since | Next review |
| --------------------- | --------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------ | ----------- |
| `usdh-native-markets` | `retired` | Native Markets USDH entered the frozen archive after its USDC migration; retain the adapter only for historical review and re-evaluate if the issuer resumes the product.                                                        | 2026-07-11   | 2026-10-11  |
| `abracadabra`         | `retired` | MIM entered the frozen archive after its terminal depeg; retain the adapter only for historical review and re-evaluate if the protocol resumes active issuance.                                                                  | 2026-07-26   | 2027-01-26  |
| `openeden-usdo`       | `parked`  | The 2026-08-13 re-enable probe confirmed the issuer gateway serves ordinary clients, but the first production cron received HTTP 500 on every Worker fetch strategy; re-parked until OpenEden unblocks Cloudflare Worker egress. | 2026-08-14   | 2027-02-14  |

`parked` and `retired` descriptors carry `parkedSince` and `nextReview` ISO dates. Default cadence is a six-month review window; when `nextReview` passes, the adapter is up for one of: revival under an active coin binding, status downgrade to `retired`, or full removal alongside its tests and fixtures. The registry test asserts both fields are populated for every non-active entry.

The staged `stoneyield-router-pool` adapter exists but is parked and not configured by any coin because on-chain evidence contradicts stUSD's reviewed sidecar; see the reviewed ledger at `.github/workflows/artifacts/safety-score-missing-data-reviewed-ledger.json`.

`collateral-positions-api` can now optionally attach direct redemption-capacity telemetry alongside the collateral mix when a reviewed bridge-backed stable exit exists. `zchf-frankencoin` uses this path to publish the current CHFAU StablecoinBridge inventory as `redemption.capacityUsd` for redemption-backstop modeling without changing the reserve-slice composition itself. Until Frankencoin's price API publishes CHFAU directly, this route values the CHFAU bridge balance through the existing VCHF CHF-price proxy.

`cap-vault` reads Cap cUSD's Ethereum vault state directly. Reserve slices are based on each supported asset's total supplied balance, while redemption-capacity telemetry uses unpaused available balances after borrows so the route does not treat borrowed or paused collateral as immediate exit capacity. The adapter emits nested `metadata.redemption` with `capacityKind = "live-direct-bounded"`, `freshnessKind = "same-run-onchain"`, `routeStatusSource = "onchain"` from per-asset pause checks, and zero-second settlement-delay telemetry. For the current USDC + WTGXX proportional basket, the same producer also reads WTGXX's tracked Chainlink NAV feed, fails the snapshot closed when that required NAV is missing, invalid, future-dated, or stale, and pins the complete value weights plus reserve-value-per-cUSD output unit. This source-bound output valuation is carried with its oracle address and timestamp; it is not inferred from the WTGXX symbol or the configured route output list.

`3jane-usd3` reads USD3's Ethereum strategy, MorphoCredit market, waUSDC conversion, and idle USDC accounting directly. It splits the strategy NAV between the pro-rata liquid waUSDC/USDC buffer and deployed private-credit exposure, then uses `availableWithdrawLimit(address(0))` as direct bounded USDC redemption capacity. The same-run telemetry also carries shutdown state, the configured commitment delay, and the documented zero withdrawal fee.

`idle-cdo-epoch-variant` reads an Idle/Pareto credit vault's own epoch accounting. These vaults are deliberately not ERC-4626 — `asset()` and `totalAssets()` revert, and deposited underlying is forwarded straight to the whitelisted borrower's wallet — so the adapter uses the CDO's `getContractValue()`, `lastNAVAA()` / `lastNAVBB()`, `epochDuration()`, `epochEndDate()`, `defaulted()`, and `isEpochRunning()`, plus the tranche's `totalSupply()` and `minter()`, in one multicall round. `getContractValue()` is by construction the unlent underlying balance plus the valued receivable, so `getContractValue() - underlying.balanceOf(cdo)` is exactly the borrower receivable with no double counting. The receivable is published as a `private-credit` slice **with no `coinId`** — it is not a claim on the deposit token, and linking it would present a single-obligor loan book as that token's reserves; the cash-equivalent slice is emitted only when the vault actually holds an unlent balance, never assumed. The vault's `token()`, `AATranche()` / `BBTranche()`, and the tranche's `minter()` are all verified against config before any composition is published. The credit slice's liquidity horizon comes from the structural `epochDuration`, not the time remaining in the current cycle, and drops to `unknown` once `defaulted()` is true. A senior tranche with no live junior NAV beneath it degrades with `idle-cdo-no-junior-subordination` rather than being re-rated upward for its seniority label. When the config declares `expectJuniorTranche: false` for a reviewed single-tranche vault — as `aa-falconx-mev-capital` does — the missing junior is recorded as an `info` structural risk fact instead of a `degraded` warning, leaving the first-loss reality on the slice's `risk`/`riskFactors`. NAV reconciliation compares the tranche NAV sum against `getContractValue()` net of `unclaimedFees()`, so accrued protocol fees alone no longer manufacture a reconciliation degrade. The adapter declares no redemption telemetry: the monthly epoch exit's stressed depth is not observable on-chain, and publishing capacity from NAV would fabricate it. `aa-falconx-mev-capital` binds this adapter; it keeps `variantOf: usdc-circle` and its serial USDC dependency edge while its backing is scored from the honest composition instead of inherited from Circle.

`origin-vault-balances`, `blast-usdb-yield-manager`, and `nest-vault-positions` cover additional strategy-vault reserve sources without widening generic adapters. Origin reads OUSD Vault `checkBalance(asset)` plus `totalValue()` so only mapped assets are credited, and it separately reads idle ERC-20 `balanceOf(vault)` as direct bounded redemption capacity while excluding deployed strategy balances. Blast reads Ethereum USDYieldManager `totalValue()` and reconciles it against Blast USDB supply. Nest consumes the Nest Alpha Vault positions, NAV, and last-price-update APIs, preserving the latest verified update timestamp, classifying tokenized JTRSY/USTB fund shares at the existing `low` tokenized-layer tier, and grouping private/structured vault exposure as high risk. Any Nest vault whose mapped positions (plus pending deposits) cover less than its reported NAV now publishes an explicit high-risk `Nest NAV reconciliation residual` slice and `unknownExposurePct`, rather than silently dropping the unexplained share (previously gated to nOPAL).

`escrow-balance` covers coins whose exit route is paid out of one pinned escrow or reserve contract, where redemption capacity is a single token-denominated view read. The contract, 4-byte selector, pre-encoded 32-byte argument words, and escrowed-asset decimals are all config-owned, so the adapter never resolves the escrow from the tracked coin's own deployments — necessary because the escrow usually sits on a different chain from the circulating token. It emits one reserve slice for the escrowed asset plus same-run `metadata.redemption` telemetry with `capacityKind = "live-direct"`, `freshnessKind = "same-run-onchain"`, and `routeStatusSource = "onchain"`. An optional `pausedSelector` on the same contract marks the route `paused` rather than `open`; a failed, missing, or zero read throws instead of publishing partial or zero capacity. USDCx reads Circle xReserve's `balanceOfNativeCollateral(USDC, 10005)` on Ethereum, which is the Gateway collateral attributable to Movement's domain rather than total xReserve reserves or USDCx supply.

`pusd-polymarket` uses the bounded multi-read form of `escrow-balance`: both Polygon USDC and USDC.e `balanceOf(vault)` reads must succeed. `compareToCoinSupply` reads the coin's same-chain `totalSupply()` using its reviewed deployment decimals, publishes reserve/supply totals and collateralization, retains a shortfall warning, and caps `live-direct-bounded` capacity at supply. Optional pause checks preserve backing but zero executable capacity. No pause getter is asserted for Polymarket's immutable permissionless unwrap route. Its source key follows the first configured token contract. The unused multichain pUSD adapter path was removed; pUSD-Plume remains a weak single-asset probe because its vault census is not fully valued.

`circle-transparency`, `quantoz-transparency`, `ripple-transparency`, and `m0` preserve source freshness when their upstream pages/API expose usable reserve disclosure or update timestamps: Circle uses the public reserve `As of` date, including the unique page-level reserve-composition date when an individual stablecoin tab has no nearer date; Quantoz uses the transparency page's `UPDATED` date and per-token reserve-ratio/allocation row; Ripple uses the RLUSD balance block's `As of` date and reserves-versus-circulation values; and M0 uses the timestamp of the latest `minterGateway_totalCollateralSnapshots` row it publishes from. The `m0` adapter requires keyed access (`M0_API_KEY`, issuer-provided, rate-limited, Pharos-use-only) and fails closed without it. M0 retired its off-chain `CollateralCurrent` composition feed in 2026-08 (the resolver survives in the schema but returns the gateway 500 envelope), so the adapter publishes one protocol-constrained slice — U.S. Treasury bills & cash, the only collateral the Minter Gateway's eligibility rules admit — from the on-chain-indexed total, cross-checks it against the per-minter collateral sum (degrade warning above 0.5% divergence), and degrades when the total snapshot lags the collateral-update event stream by more than 12h. The cap was widened from 6h to 12h by decision P11 on 2026-09-09: the observed ~2h indexing cadence means the total routinely runs ahead of the update stream, production lag reached 27,000s on 2026-09-09, and the widened cap accepts the risk that a stalled total may be admitted for up to 12h. The adapter records the drift as `details.collateralLagSec` (latest update minus total-snapshot time) and `details.collateralLagCapSec` so `/status` metadata can show the lag trend; curated reserve evidence carries the composition detail. `mento` reads reserve composition from the server-side analytics API behind `reserve.mento.org`; that API is usable from the Worker even though the public site currently trips browser CORS errors. Plain Mento configs parse the protocol reserve basket, while configs with `params.cdpStablecoin` parse active CDP troves for GBPm, JPYm, CHMm, and reviewed XOFm shape into USDm collateral slices plus live collateral/debt metadata. XOFm is schema/parser-ready but remains unconfigured until the live analytics payload exposes active XOFm troves. The adapter hydrates verified source freshness from the dashboard's embedded reserve payload timestamp when available, and falls back to explicit unverified freshness if that dashboard timestamp disappears. `circle-transparency` now treats a zero-valued disclosure row as a valid parse — a 0% attribute (for example, no overnight repo exposure) is simply omitted from the slices rather than treated as a missing attribute — and only a coin whose reserve attributes all read zero throws a layout-changed error. Circle's reserve attribute parser accepts only clean non-negative decimals — a valid `0` allocation row passes, and malformed values such as `4.7.18` no longer silently truncate to a wrong slice value.

`sgforge-coinvertible` treats SG Forge slash-form `Last update` dates (with either two- or four-digit years) as European day/month/year by default, but falls back to month/day/year when the European parse would put the disclosure timestamp in the future. This keeps the future-timestamp validator strict while tolerating SG Forge edge/localization variants that can reverse ambiguous dates.

`usdaf-asymmetry` now uses `liquity-v2-branches`. The live six-branch registry (`0x33d68055cd54061991b2e98b9ab326ffce4d60fe`) was verified through its deployed AddressesRegistry, token census, TroveManager-to-ActivePool pointers, collateral identities, debt and shutdown getters. The final branch holds 18-decimal WrappedWbtc (`0xe065bc161b90c9c4bba2de7f1e194b70a3267c47`), not raw 8-decimal WBTC; its verified contract scales deposits and withdrawals by 10^10, so its balance uses the raw WBTC price at a reviewed 1:1 human-unit rate. ERC-4626 branches use the generic share conversion before pricing. The obsolete first-party `asymmetry` API adapter was removed.

`xdai-bridge` reconciles native xDAI against its Ethereum-side bridge collateral in one run: it pins safe-tag blocks on both chains (bounded timestamp skew, re-read stability checks), verifies bridge/token identity wiring and 18-decimal invariants, values sUSDS via `convertToAssets(balanceOf)` after confirming `asset() == USDS` (keeping `maxWithdraw` diagnostic-only), and computes outstanding native supply as `mintedTotallyByBridge - totalBurntCoins`. Slices link `susds-sky` and `usds-sky` as collateral dependencies; coverage shortfall, implausible surplus, cross-chain skew, and material legacy DAI/sDAI balances raise warnings or fail closed. v1 emits no redemption telemetry.

`hive-hbd-protocol` reads Hive consensus state (dynamic global properties, median price feed and treasury account) from two distinct public JSON-RPC node hosts against a pinned chain configuration. It brackets each node's material reads with identical head state and requires cross-node agreement on supplies, thresholds, treasury HBD, and the feed-derived debt quantities and ratio. The second URL is a mandatory corroborating node, so the generic fallback path cannot substitute it for the primary. A coherence failure permits up to twelve resamples within the same 19-second signal; active brackets settle before retry, and Pharos request caching is bypassed for every bracket read. Each complete pair uses six POSTs across the two nodes with at most two concurrent requests. Corroboration accepts heads within three blocks, quantity differences within two relative basis points of the smaller value (zero must match exactly), and an absolute debt-ratio difference of at most two basis points; thresholds and print rate must match exactly. Resampling retains a coherent leading node and refreshes its lagging peer, or both on equal-height disagreement; final disagreement reports mismatching fields and both values. Upstream node/proxy caching remains a source limitation; matching results do not establish a cache-free historical state proof. Treasury-adjusted debt arithmetic uses bigint fixed-point; the adapter emits one unlinked 100% high-risk endogenous-HIVE mechanism slice with `not-applicable` freshness and deliberately no USD reserve totals, collateralization ratio, or redemption-capacity claims. Print-stop state is an informational warning and hard-limit state degrades the snapshot.

`usdai-hub` promotes USDai from curated-validated to independent evidence: one Arbitrum run reads the hub's PYUSD balance, canonical USDai `totalSupply()`, `bridgedSupply()`, pause state, and the proxy's EIP-1967 implementation, compares PYUSD balance against bridge-safe liabilities, and fails closed on identity, implementation-pin, or malformed-source drift. It emits one measured 100% PYUSD slice (`pyusd-paypal` dependency) with `not-applicable` freshness and retains the existing whitelisted redemption-capacity telemetry. Observed shortfalls and zero total liabilities degrade with `reserve-undercollateralized`, while a paused hub retains `routeStatus: paused` and a degraded `route-paused` warning.

`saturn-pyusdx` promotes USDat from curated-validated to independent evidence after Saturn's August 2026 migration to the PYUSDx-backed MultiMint. One Ethereum run reads the proxy's EIP-1967 implementation slot and fails closed if it drifts from the reviewed MultiMint implementation, then a single Multicall3 round reads `pyusdx()`, the wrapper's PYUSDx balance and `totalSupply()`, decimals, and pause state; a `pyusdx()` identity mismatch also fails closed. It emits one measured 100% PYUSDx slice carrying the `pyusd-paypal` dependency: M0 documents PYUSDx extensions as 1:1 PYUSDx wrappers and PYUSDx as MoonPay's PYUSD-backed tokenization framework, so the verified 1:1 PYUSDx backing extends the claim to PayPal USD. `collateralizationRatio = PYUSDx balance / wrapper supply` with `not-applicable` freshness; a ratio below the 0.995 threshold or zero supply degrades with `reserve-undercollateralized` (E4), and a paused MultiMint retains `routeStatus: paused` with a degraded `route-paused` warning. The observation is scoped to the Ethereum MultiMint deployment and its own supply; USDat's other registry deployments are bridged representations outside this wrapper's reserve claim.

`xpr-account-balances` reads the XPR Network (Antelope) chain REST API directly: `get_currency_stats` supplies the coin's token supply, `get_currency_balance` reads the treasury account's balances on a pinned token contract, and `get_info` brackets the run so every snapshot carries its head block number and time under `not-applicable` freshness. It emits one slice per configured measured symbol, an explicit high-risk unknown slice for any supply remainder, `collateralizationRatio = holdings / supply`, and an honest `unknownExposurePct`; each configured node is tried in order and the last error is rethrown. The XMD binding measures XUSDC and XPYUSD at `xmd.treasury` against XMD supply. Because XPR X-tokens are issuer-custodial bridge wrappers whose 1:1 upstream backing is not independently published, the measured slices are labeled bridged and deliberately carry no `coinId` dependency link, with an informational `bridge-wrapper-unverified` note.

`sky-makercore` preserves positive-debt unrecognized groups inside the high-risk `Other modules` slice and reports their combined `unknownExposurePct`. Discovering a new group is informational by itself; the shared 5% material-unknown policy determines whether the snapshot degrades. Zero-debt unknown groups do not emit warnings. The governance-funded allocator groups `osero` and `keel` are recognized as medium-risk allocator slices (like `obex`) with no `coinId`, since their ultimate holdings are allocator-strategy exposure rather than a single tracked stablecoin.

`usdai-proof-of-reserves` consumes USD.AI's first-party on-chain reserve index for `susdai-usd-ai`, not base USDai, preserving oversized fixed-point weights and grouping hardware-loan `DEAL` rows separately from liquid PYUSD. Complete share-bearing rows take precedence over auxiliary amount-only rows. Its score-bearing `evidenceClass` remains `independent` for reconstructible accounting, but `sourceOriginClass` remains `issuer-attested`: physical GPU mapping, appraisal, insurance and legal enforceability are not proven by token balances. Deal-document `timeLastUpdated` clocks are no longer reserve freshness evidence. A single Arbitrum Multicall3 reads the reviewed `reserveLink` token balances and decimals, sUSDai vault asset identity, decimals, `totalAssets` and `totalSupply`, and the executing block number/timestamp. Every configured liquid row must match uniquely; its 18-decimal API amount must reconcile to the measured balance, and its share times vault assets must reconcile to that same balance, within the configured tolerance (100 bps maximum, conservatively reviewed at 1%). Only a passing, nonzero liquid anchor emits `not-applicable` freshness with `details.anchor = { block, checkedRows, tolerance, ... }` and `observedBlock`; the vault share supply is diagnostic, never a multichain liability denominator. Missing anchors/read failures or mismatches retain readable composition as `unverified` plus a degraded warning. A September 9 live probe matched PYUSD exactly but found PoR-implied assets of $459.446M versus vault assets of $464.300M (about 1.06% difference), so it correctly stayed degraded rather than widening tolerance to force a pass.

`crvusd` now reads Curve's Ethereum ControllerFactory and LLAMMA AMMs directly on-chain, summing each market's `bands_y` collateral balances across active bands via Multicall3. Band reads are bounded per market and streamed with two-market concurrency, so adding valid markets cannot trip a global aggregate-call cap and force the adapter onto its unverified API fallback. `bands_x` crvUSD inventory from soft-liquidation state is tracked as metadata rather than external collateral. The adapter also walks the Ethereum Yield Basis factory (`factory.yieldbasis.eth`), unwraps each market's LT position with `preview_emergency_withdraw(totalSupply)`, values the resulting external asset balances with DefiLlama prices, and folds those balances into the same BTC / ETH reserve buckets. `crvusd-curve` config version `3` marks the direct on-chain LLAMMA expansion and emits `freshnessMode: not-applicable`. The Yield Basis leg is optional and time-bounded; if it stalls, the adapter records a degraded warning while preserving the direct Curve collateral snapshot. The checked-in config also keeps Curve's market JSON endpoint as a fallback so transient RPC slowness does not open the reserve-source breaker when the public market feed is still reachable. A markets payload that reports markets but no readable `collateral_amount_usd`/`collateral_token` fields degrades with `curve-markets-unreadable` instead of publishing an empty crvUSD collateral mix.

`money-llamma` binds `money-defi-money` to a live on-chain census of defi.money's Curve-LLAMMA-fork markets: on each of Arbitrum, Base, and Optimism a pinned-block head Multicall3 reads the shared controller's `get_market_count()`/`get_all_markets()` (rejecting list/count drift) and MONEY `totalSupply()`, then every MarketOperator's `COLLATERAL_TOKEN()`/`AMM()`/`total_debt()` and each AMM's full `min_band()..max_band()` span of `bands_x`/`bands_y` (bounded per market, batched at 500 calls). `bands_y` collateral joins the canonical taxonomy by symbol — WBTC, ETH, and wstETH — while any other accepted collateral is measured and priced under its own `money-llamma:other-<symbol>` bucket so nothing hides in a reviewed row. Collateral is priced through DefiLlama quotes (a missing price fails closed), MONEY debt is market-valued rather than assumed par (an off-par price publishes an info warning), and a collateralization ratio below 1 degrades with `reserve-undercollateralized` instead of erroring. Freshness is `not-applicable` latest-state with the primary `observedBlock` plus `allChainBlocks` for all three legs.

GHO-specific note:
the `gho` adapter binds each reviewed mainnet GSM to its issuing `facilitatorAddress` and replaces backing only within that parent's measured bucket, capped at the parent's issuance. Other facilitators retain their measured exposure; GSM surplus cannot dilute Core or substitute for an unreadable parent. Uncovered GSM exposure, unknown facilitator labels, and supply not reconciled to the registry remain explicit high-risk slices counted in `metadata.unknownExposurePct`, rather than being redistributed across known facilitators. If observed issuance exceeds the supply observation, the measured exposure total is retained and the snapshot degrades instead of silently rescaling it to supply. Tracked GSM slices preserve reviewed `coinId` and `depType`. Evidence remains independent latest-state on-chain observation with `not-applicable` freshness, not a timestamped issuer disclosure. Redemption status comes from explicit freeze/seize flags: a readable unpaused route stays `open` even with capacity zero; all observed routes paused yields `paused`; incomplete status without an observed open route yields `unknown`. Frozen/seized modules publish degraded observations and unreadable status contributes no immediate capacity. Fees retain the worst tracked buy fee and min/max diagnostics.

Liquity v1 note:
the `liquity-v1` adapter covers `lusd-liquity` by reading `getEntireSystemColl()` and `getEntireSystemDebt()` from the official Ethereum `TroveManager`, preserving LUSD as a one-slice 100% ETH reserve view while classifying the feed as independent latest-state on-chain evidence rather than a generic ERC-20 liveness probe. The adapter also publishes nested redemption telemetry from the same run: `capacityUsd` is derived from `getEntireSystemDebt()`, `capacityKind = "live-direct-bounded"`, `freshnessKind = "same-run-onchain"`, and the existing redemption-fee probe populates the nested fee field when available. Route status is measured, never assumed: one Multicall3 packet carries the collateral and debt reads together with `MCR()` and `getTCR(price)` priced by the protocol's own immutable PriceFeed (`fetchPrice()`), because `redeemCollateral()` reverts with "Cannot redeem when TCR < MCR". The published `routeStatus` is `open` only while the observed TCR clears MCR; a TCR below MCR publishes `paused` with the measured ratios and a `redemption-route-status-degraded` warning, and an unreadable gate (price, `getTCR` or `MCR`) publishes `unknown` with `redemption-route-status-unreadable` rather than claiming an open route. Both ratios are exposed as `totalCollateralRatio` (protocol-priced TCR) and `minimumCollateralRatio`, alongside the DefiLlama-priced `collateralizationRatio`.

Liquity v2 branch note:
the `liquity-v2-branches` adapter reads branch ActivePool collateral balances, DefiLlama prices, ActivePool debt, optional branch shutdown status, and optional live redemption-fee telemetry. `bold-liquity`, `bd-basedollar`, `feusd-felix`, `usdq-quill`, `nect-beraborrow`, and `cdp-enosys` use this path so their reserve slices remain branch-collateral based while nested redemption telemetry publishes bounded current debt with `capacityKind = "live-direct-bounded"` and `freshnessKind = "same-run-onchain"`. BOLD and Base Dollar additionally configure protocol-priced mechanism telemetry: one bounded Multicall3 packet reads token total supply, each TroveManager's fresh price/redeemability tuple, and each branch Stability Pool's committed stablecoin deposits. For those two configs, immediate redemption capacity includes only branches whose same-run mechanism tuple is readable and `redeemable = true`; false branches are excluded and degrade the route, while an unreadable required tuple leaves redemption capacity unpublished/unrated and degrades the snapshot instead of falling back to full ActivePool debt. A supply/debt reconciliation gate must pass before the adapter publishes `collateralizationRatio` and `liquidationCapacityRatio`; unavailable or unreconciled optional solvency telemetry is omitted with an informational warning so the reviewed detail-page values remain the fallback, while an observed ratio below 1.0 degrades the snapshot. Base Dollar binds the five launch branches (WETH, wstETH, rETH, wrapped cbBTC, and cbETH) on Base; its wrapped cbBTC balance is priced through the underlying cbBTC token. The Beraborrow binding also supports ERC-4626-style Collateral Vault share branches, branch `fetchPrice()` fallback, and per-branch `getRedemptionRateWithDecay()` fee telemetry.

Both `liquity-v2-branches` read paths now scale that per-branch fee identically: the individual-call fallback used when Multicall3 is unavailable passes the same 18-decimal scale as the batch path, so a live branch rate (for example NECT's 50 bps WBERA rate) no longer resolves to a null fee just because the batch was unavailable.

Liquity native ActivePool note:
the `liquity-native-active-pool` adapter covers Liquity-style forks whose native collateral balance is exposed directly on ActivePool instead of through per-branch ERC-20 balances. `meusd-mezo` uses this path on Mezo: the adapter reads ActivePool debt and collateral, values native collateral through the protocol price feed, checks TCR against MCR for current route health, and emits same-run redemption-fee telemetry from BorrowerOperations when available.

M0 wrapper note:
the `m0-wrapper-underlying` adapter covers token-specific M0 wrappers whose immediate redemption capacity is the current M token balance held by a wrapper or reviewed facility. `wm-m0` reads the Ethereum wM wrapper's M balance directly. `usdsc-startale` reads the Soneium SwapFacility's M balance only after verifying the stable token, underlying M token, approved swapper, and unpaused route state, then emits whitelisted-primary direct capacity.

ctUSD's Citrea configuration includes the observed swapper `0x4743A4e199E3d5a86fDd2d3E3A1653b9BE9d4c7a`. Its [successful September 6, 2026 `swapOutM` transaction](https://explorer.mainnet.citrea.xyz/tx/0xc118b7e5fc92d0835123fed959346775b041192f7dec47040cd3636a87f93c06) and a September 7 `canSwapViaPath(swapper, ctUSD, M)` read establish an eligible exit caller. The adapter rechecks that permission on every run; this is whitelisted-primary route evidence, not a claim that every holder has direct access. Omitting the swapper makes route status unverified and degrades the reserve snapshot.

`erc4626-single-asset` accepts up to eight `redemptionLock` probes on the vault, using a four-byte selector or zero-argument function signature. Cooldown, unstake-window, and pause reads join the initial Multicall3 state round. Positive cooldowns publish `redemption.settlementDelaySec` and `documented-bound` capacity; withdrawal windows also bound capacity but are recorded separately as `unstakeWindowSec`, not added to waiting time. Configured unreadable/malformed locks fail closed; observed pauses preserve composition with a degraded warning and `routeStatus: paused`. stkGHO probes `getCooldown()`, `getUnstakeWindow()`, and `paused()`; sUSDe probes `cooldownDuration()` instead of assuming seven days. These are latest-state on-chain observations (`not-applicable` reserve freshness, same-run redemption evidence), not issuer timestamps.
The same `cooldownDuration()` probe is configured for savUSD and sYUSD. sUSN and apyUSD instead configure reviewed `redemptionRoute: "async-request"`: Noon uses withdrawal demands and Apyx uses per-receipt ERC-7540 maturity, neither exposing a global cooldown getter. Their held backing is only `documented-bound` with `settlementBoundUnproven: true`, so the redemption consumer withholds executable/scoring capacity; no fixed delay is substituted. `details.redemptionMechanism` records the asynchronous mechanism.

`astherus-earn-wrapper` reads Astherus's custom `asUSDFEarn` contract (`0xdb57a53c428a9fafcbfeffb6dd80d0f427543695`) with one Multicall3 `aggregate3` packet of seven calls over a public BSC RPC. Because asUSDF is not ERC-4626 (`asset()` and `totalAssets()` revert), it publishes net USDF backing after subtracting `getUnvestedAmount()`. It emits one 100% parent-linked `usdf-astherus` slice and no redemption capacity.

`initia-wrapper-vault` is an `http-json` adapter that reads Initia's `interwoven-1` REST/LCD endpoints in four intentionally sequential calls: POST `/initia/move/v1/view/json` for the vault's AUSD0 balance; GET `/cosmos/bank/v1beta1/supply/by_denom` for iUSD supply; and GET `/initia/move/v1/accounts/{address}/resources/by_struct_tag` twice for the iUSD `ObjectCore` owner and AUSD0 `Metadata` identity. These reads pin the vault and metadata identities. Peak concurrency stays at one connection against the trigger-wide six-connection budget; a parallel refactor would consume four slots. It emits one 100% parent-linked `ausd-agora` slice and no redemption capacity.
Initia publishes shortfalls (including zero backing) and zero supply as degraded observations, omitting the ratio for zero supply; every positive surplus, including six AUSD0, preserves the measured ratio with an informational warning.

Both adapters fail closed on identity drift, malformed required payloads, and required source failure: RPC failure for Astherus and LCD request failure for Initia; Astherus additionally rejects non-positive backing and zero supply.

`sgho-aave` binds `erc4626-single-asset`: the verified sGHO contract exposes standard `asset()` and `totalAssets()`, with `totalAssets() == previewRedeem(totalSupply())` at review. The config pins GHO identity; measured idle GHO and `paused()` now bound redemption instead of treating a preview price as liquidity. ERC-4626 idle slices use `erc4626-single-asset:<chain>:<asset>` keys and deployed/unattributed slices use `erc4626-single-asset:<chain>:<vault>:deployed`, matched by reviewed reserve sidecars.

`united-por` reads United Stables' public aggregate PoR payload (`https://u.tech/u-client-api/v1/public/u/por`) directly: `totalReserve` vs `totalToken` decimal strings and an ISO `updatedAt` drive `collateralizationRatio` and verified freshness for `u-united-stables`, while per-asset composition detail stays on the coin's configured `params.slice` (mirroring its curated reserve bucket) since the source only discloses the aggregate. A missing or non-boolean `ripcord` alarm, or an `accountName` other than the reviewed United Stables account, fails the adapter closed so an alarm-less or foreign payload can never read as healthy. The payload's own `ripcord` data-quality alarm is treated as a degraded warning rather than a thrown error — the snapshot is still stored with its ratio and freshness, but a ripcord run (with any disclosed `ripcordDetails` folded into the warning message) never reads as healthy, and it is excluded from score-grade scoring passthrough same as any other degraded snapshot.

Mento reserve note:
when the reserve dashboard payload no longer carries per-stablecoin `cdp_backings` totals for a CDP stablecoin, the `mento` adapter degrades with `mento-cdp-coherence-unavailable` instead of silently skipping the dashboard-vs-API coherence gate. When both sources are readable but their per-stablecoin CDP totals diverge by more than 1%, the snapshot publishes the analytics-API composition as degraded with a `mento-cdp-coherence-diverged` warning (both totals and divergence percentages in the warning and `details.cdpCoherenceDivergence`) instead of failing the run; unreadable or malformed sources still fail closed.

USDD data-platform note:
the `usdd-data-platform` adapter fails closed when a collateral row drops a finite non-negative `lockedValue` instead of silently summing the remaining vaults.

Makina strategy note:
`makina-strategy` reads a Makina strategy's first-party accounting API with two JSON requests: the strategy state endpoint for AUM, last reported AUM, share price / reference NAV, share supply, share limit, APY context, accounting token, and NTT metadata; and the allocation endpoint for base-token balances, positions, chain totals, protocol totals, debt flags, and position update times. Accounting-token integer strings are normalized with the configured decimals (DUSD uses USDC, 6 decimals). Position values marked `is_debt` are subtracted from assets, while positive positions and base-token balances are grouped by material protocol bucket. Identified dust below the configured display threshold becomes `Other identified Makina positions`; genuinely unlabelled exposure remains `Unknown Makina exposure` and contributes to `unknownExposurePct`. The adapter reconciles allocation net value to current `aum` within the configured tolerance, while retaining `lastReportedAum` as diagnostic metadata because it can lag the current allocation packet; reconciliation failure aborts the snapshot so the reserve API keeps the last-known-good or curated fallback. Freshness uses the oldest strategy-envelope timestamp, allocation-envelope timestamp, or nonzero position update time, and the adapter applies the reviewed Machine's three-hour position-staleness threshold instead of the generic dashboard window. This prevents a newly generated API response from making stale underlying Caliber accounting appear current. Snapshot metadata retains gross assets, debt, total reserve value, share supply, reference NAV, current and last-reported AUM, chain totals, protocol buckets, debt count, oldest position update, and oldest material position update. When an `asyncRedeemerAddress` is configured, the same run pins one Ethereum block, validates the AsyncRedeemer beacon implementation, confirms `machine()`, Machine `accountingToken()` as canonical Ethereum USDC, and Machine `shareToken()` as the tracked coin contract, then reads whitelist state, minimum finalization delay, request counters, minimum redeem amount, Machine idle USDC, AsyncRedeemer locked shares, and AsyncRedeemer reserved USDC. DUSD queue capacity is `max(0, Machine idle Ethereum USDC - convertToAssets(lockedShares))` and is emitted as `redemption.capacityKind = "live-queue"` with same-run on-chain freshness; `queueDepthUsd` is the converted locked-share liability. The minimum finalization floor is stored in `redemptionQueue.minimumFinalizationDelaySec`, not score-bearing `redemption.settlementDelaySec`. Whitelist-enabled DUSD redemption is modeled as `holderEligibility = "issuer-discretionary"` and `routeStatus = "open"` rather than `cohort-limited`. If the proxy identity, token wiring, or capacity proof fails, the adapter keeps reserve composition visible with an informational warning and omits redemption capacity instead of falling back to AUM, supply, deployed positions, or reserve weights. The numerical reserve accounting is publicly readable from the Machine and Caliber contracts, while Makina's first-party API indexes that state and supplies the human protocol and strategy labels. Because Pharos still consumes the API-labelled composition, its descriptor conservatively retains `sourceOriginClass: issuer-attested`; the proof-style detail badge distinguishes that mixed path from a wholly independently measured live feed, and the independently read redemption route remains same-run on-chain telemetry. Allocation positions that omit `updated_at` emit the `makina-position-timestamp-missing` degraded warning because position-accounting freshness cannot be fully bounded.

Re Protocol metrics note:
the `re-metrics` adapter parses Re Protocol's official metrics page and now extracts instant redemption vault capacity from the embedded `redemptionRows` payload. The reserve slices remain metrics-derived, while nested redemption telemetry carries the current direct vault capacity from the same API payload for `reusd-re-protocol`. The parser now anchors on the page's embedded `initialCards` payload (the older `series` anchor is gone from the page), emits a `re-metrics-offchain-capital-branch` info warning naming whether offchain capital came from the `initialCards` series or the `initialTvlData` fallback, and derives composition freshness from the chain-breakdown `asOf` timestamps while carrying the daily offchain-capital series date separately as `offchainAsOf`.

`fx` now publishes f(x) protocol pool debt balances as conservative live proxy redemption capacity for `fxusd-f-x-protocol`; the configured score-grade path reads the reviewed Ethereum WBTC and wstETH pool collateral/debt totals directly on-chain. Each pool's `getTotalRawCollaterals()` is denominated in the pool's base token unit — stETH for the wstETH pool (whose actual holding is wstETH via the pool's rate provider), WBTC at a unified 1e18 scale for the WBTC pool — so raw collateral is valued with the base token's price, never the wrapped token's. `fxsave-f-x-protocol` uses the generic `erc4626-single-asset` path to publish the vault's idle fxSP balance as live-direct capacity for its fxSP/router exit route. The same adapter can also attach reviewed ERC-4626 liquidity sidecars: Morpho V1/V2 vault configs use Morpho V2 `liquidity` or V1 `liquidity.underlying` as same-run API capacity after validating the vault address, chain id, listed status, and underlying asset; Yearn V3 configs use the vault's same-run default withdrawal queue, measuring `totalIdle()` plus each funded strategy's `min(currentDebt, convertToAssets(maxRedeem(vault)))`; `sbold-k3-capital` uses `sbold-sp-withdrawable` to read the BOLD amount returned by the vault's `calcFragments()` liquidity view, representing compounded Stability Pool deposits that the vault can withdraw; and `sfrxusd-frax` uses `fraxtal-hop-withdrawable` because Ethereum-local withdrawal is disabled. Two coin-specific paths override the generic idle-balance assumption: `eearn-ember` pins the vault, validator, and protocol-config proxy/implementation identities and reports zero immediate capacity for its operator-batched queue while exposing pause, queue, and current-fee diagnostics; `sdusd-dtrinity` pins the dSTAKE router and exact strategy/adapter set, then uses the current dLEND `maxWithdraw` cross-checked against dUSD available liquidity and the current unstaking fee. Required identity or state-read failures abort those snapshots rather than falling back to idle assets or full NAV. The sfrxUSD observer pins finalized Ethereum and Fraxtal blocks, all six proxy implementations, direct-contract code, peers, tokens, oracles, inventory, quote components, and return-message funding. It publishes the accepted packet for diagnostics and bounded capacity, but the V9 route remains non-scoreable while Ethereum transaction gas and a measured or primary-source settlement upper bound are missing. Morpho V2 `forceDeallocatableLiquidity` is persisted for context but not used as scoring capacity. Yearn V3 funded-strategy probe failures, sBOLD liquidity-read failures, and any sfrxUSD identity/state drift degrade instead of falling back to full NAV or the disabled local withdrawal path. `jupusd` consumes Jupiter's public transparency API and latest snapshot timestamp, grouping USDC/USDtb holdings into reserve slices while emitting whitelisted-primary live redemption capacity and route status from the public oracle endpoint. Snapshot selection no longer trusts the feed's descending order — the adapter explicitly takes the newest parseable snapshot timestamp, so an upstream ordering flip cannot silently stamp an eight-month-old verified timestamp.

`solstice-attestation` and `river-protocol-info` are proof-class reserve-sync adapters. They make current issuer/protocol telemetry visible on reserve detail and status surfaces, but their registry evidence class is `weak-live-probe`, so they do not become V9 Backing reserve evidence. Solstice remains proof-class until its aggregate solvency feed exposes timestamped asset-category composition; River remains proof-class because its protocol-info endpoint exposes aggregate TVL/circulating-supply telemetry rather than asset-level collateral composition. River snapshots degrade when the aggregate TVL is below circulating satUSD, and timestampless protocol-info payloads remain freshness-unverified. River's bounded redemption probe tolerates reverts in speculative manager slots beyond the on-chain count; every manager inside that count and its branch reads remain mandatory. River's TVL-to-supply figure is now published as the clearly-named diagnostic `details.protocolTvlToSupplyRatio` instead of a computed `collateralizationRatio`, and no `reserve-undercollateralized` warning is derived from it, because protocol-wide TVL spans every River product and chain rather than satUSD backing.

Solstice [documents weekly independent verification](https://docs.solstice.finance/legal-documents/attestations-and-proof-of-solvency). Its adapter source-age limit is now 1,209,600 seconds (14 days), admitting two missed weekly attestations where the previous 700,000-second (8.10-day) cap sat seconds away from the observed 5-9-day publication gaps. This replaces the generic three-day dashboard limit for this adapter only. A report older than that bound still degrades, and a successful fetch does not refresh its report timestamp or change its weak-live-probe evidence class.

The adapter also emits the proof artifacts Accountable publishes beside the aggregate timeline — the signed `merkle_root` hash, the ZKP liabilities/collateral data commitments, the snapshot id, and the SEV-SNP enclave attestation address — into `details.evidenceBasis` as review input for a future evidence-class decision, and cross-checks the headline `total_reserves`/`total_supply` totals against the timeline values it scores, degrading beyond a 0.5% divergence; a payload without the attestations block or headline totals warns info rather than failing. These artifacts are recorded, never verified in-adapter, so they do not change the weak-live-probe class.

`audx-independent-assurance` uses the reviewed July 31, 2026 report: AUD 3,231,371.79 in designated bank accounts against AUD 3,208,057.00 issued supply. Aura Partners signed its limited-assurance report on August 20, 2026; the posting date is unknown. The manifest retains the report's printed AEDT timestamp and exact PDF hash, with freshness measured from the examined period rather than the signature or fetch date.

`gemini-independent-assurance` binds `gusd-gemini` to BPM LLP's monthly independent accountants' examination of Gemini's compliance with NYDFS Guidance item 3(a), examined as of May 4 and May 29, 2026 at end of day (5pm Eastern). Gemini serves no server-rendered attestation index: the official machine-readable index is the public Contentful delivery collection behind `/dollar` (`content_type=gusdAttestation`, newest first, 94 entries), whose delivery token ships in the gemini.com client bundle and is not a secret. Runtime parses that JSON and requires exactly one newest entry whose resolved `assets.ctfassets.net` PDF URL equals the reviewed manifest URL, failing closed on any newer unreviewed entry; the exact PDF bytes are then hash-verified. The May 29 column reconciles $40,637,066.22 gross cash deposits at State Street/Western Alliance less a $500,211.38 net timing-and-settlement payable (recorded as an adjustment) to a $40,136,854.84 Total Reserve against an equal GUSD circulation figure covering every chain (NEAR is a bridged representation of the native Ethereum supply). Freshness is measured from the examined instant; Gemini has not published a June 2026 or later report as of 2026-09-09, so the feed degrades honestly until a newer report is reviewed.

`straitsx-independent-assurance` now verifies the July 31, 2026 XUSD/XSGD reports signed August 28. XUSD reconciles USD 45,269,767 assets against USD 45,182,310 liabilities. XSGD reconciles SGD 21,294,294 assets against SGD 21,283,486 liabilities, including the newly disclosed five XSGD on XLAYER. The compiler and runtime both retain complete chain liabilities. Report-time freshness remains capped at 4,000,000 seconds; publishing or fetching a report does not reset its examined-period clock.

`paxos-independent-assurance` upgrades PAXG from a weak single-asset probe to independently examined reserve evidence. KPMG's July 31, 2026 examination reconciles 442,217 allocated fine troy ounces with 441,939 Ethereum and 278 Solana redeemable tokens, without dollar conversion or timing adjustments. The official Framer transparency page loads its report selection through JavaScript modules: runtime verifies the sole reviewed main-module URL and exact SHA-256 of that module and its reviewed PAXG product module, using sequential bounded reads, before the shared assurance verifier checks the PDF. No website JavaScript executes in the Worker. Any Framer deployment can invalidate the pins, including unrelated website changes. To refresh, review the official page's main-module route-to-product mapping and latest report selection, update the two URL/hash pins in `paxos-independent-assurance.ts`, then compile the reviewed PDF with `refresh-independent-assurance-reports.ts --product PAXG --pdf <path> --write` and verify with `--check`. Do not update website pins without rechecking the latest report.

The same adapter binds `PYUSD`, `USDG`, and `USDP` through `params.product`, with separately reviewed product-module hashes and July 31, 2026 17:00 EDT manifests (Poppler 26.07.0). PYUSD reconciles $2,694,072,163 across bank cash, reverse repos and Treasury bills against 2,689,335,674 redeemable tokens on seven native chains; USDP reconciles $31,975,703 cash/repo assets against 31,954,027 Ethereum/Solana tokens. Both carry KPMG's unmodified AICPA examination. USDG carries KPMG Singapore's unmodified SSAE 3000 reasonable assurance, covering PDS and PIE: $3,404,876,225 bank cash, government money-market funds and Treasury bills against 3,400,474,143 redeemable tokens. Its image-only Exhibit A six-chain total is extracted from the matching Exhibit C aggregate; it includes Ethereum, Solana, Ink, X Layer, Arbitrum One and Robinhood. USDG's reviewed 2026 July variant override selects `IJR7RhuqfckbBnl7fVubmqrnc.pdf`, not the base July file. GUSD remains unbound to assurance: no stable server-rendered Gemini HTML report index was found; the official client instead retrieves `gusdAttestation` entries from Contentful JSON.

`usdgo-transparency` uses the Deloitte examination as its authoritative composition/liability source through the independent-assurance manifest (exact PDF SHA-256 binding), with the issuer transparency API retained as a cross-check that raises informational warnings on later-period drift. The July 31, 2026 report supersedes June and locates BUIDL on Solana. Config version 3 retires the obsolete June Avalanche balance check; current report hash, latest-report detection, asset/liability reconciliation, and same-period issuer disagreement checks remain mandatory. Pharos does not claim an independent replay of the report-time Solana balance. The offline compiler verifies the report's zero Morph liabilities before omitting that empty chain from positive liability…

`audd-independent-assurance` verifies William Buck's ASRS 4400 agreed-upon-procedures report of factual findings for AUDD. The engagement is explicitly "not an assurance engagement" and expresses no opinion, so the manifest's `agreed-upon-procedures` conclusion binds the adapter to the `static-validated` / `issuer-attested` descriptor rather than the independent class — AUDD gains hash-pinned, reconciled report evidence without score-grade admission. The August 31, 2026 report (signed September 4) reconciles AUD 11,544,609.77 held at Banking Circle and in the Westpac AMAL Bare Trust against AUD 11,408,211.96 circulation across all eight chains (Stellar, XRP Ledger, Ethereum, Solana, Hedera, Base, XDC, Redbelly). The report no longer discloses in1Bank; the January 2026 in1Bank deposit-return statement records the earlier custody move, and current reserves sit with Banking Circle and the AMAL trustee structure. Report-time freshness is measured from the examined 11:59pm UTC instant, and the runtime index check fails closed on any newer or undated report.

`anchorage-independent-assurance` verifies Deloitte & Touche LLP's monthly USAT examination through the Anchorage attestation page (`anchorage.com/platform/usat-reserve-attestations`), whose report links live on the same `learn.anchorage.com` host the USDGO manifest already pins. The page's schema.org `Dataset` JSON-LD is stale relative to the rendered anchors, so discovery reads the anchors the page serves; the firm identity rests on the page's Big Four disclosure and a letterhead byte-identical to the reviewed USDGO report. The July 31, 2026 examination (signed August 28, examined at 11:59:59pm UTC) reconciles $175,906,606 reserve assets ($17,507,606 cash, $158,399,000 Treasury-collateralized reverse repos) against 175,245,527 redeemable tokens across Ethereum (175,204,971) and Celo (40,556), an unmodified AICPA conclusion with a $661,079 surplus. Any newer or duplicated report fails the runtime closed. The adapter is product-keyed for Western Union's USDPT: the `usdpt-v1` profile discovers Deloitte's monthly examination on `anchorage.com/platform/usdpt-reserve-attestations-anchorage-digital` under the identical `learn.anchorage.com` report-host pin, and the July 31, 2026 examination (signed August 28) reconciles $6,935,076 of FDIC-insured demand deposits and money market funds against 6,823,001 Solana redeemable tokens. The USAT manifest now records `attestorIdentification: { method: "reviewed-inference" }` — the report never prints the firm name in extractable text, so the identity is inferred from the letterhead-image md5 plus the page's Big Four wording — and the adapter surfaces that identification in `details.assurance` so the name is not presented as an extracted attestor.

`fidd-independent-assurance` and `sbc-independent-assurance` extend the family to Fidelity Digital Assets' FIDD and Brale-issued SBC: PwC's unmodified AICPA 2025 Criteria examination of FIDD as of July 31, 2026 5:00pm EDT (issued August 26) reconciles $20,860,119.65 of cash deposits plus $30,634,836.55 of U.S. Treasury bills — $51,494,956.20 gross — against 48,882,278 Ethereum-mainnet tokens, with the report's −$108,517.43 other-liabilities netting pinned by compile-time requiredText guards because the manifest engine forbids negative asset rows, and MCCPA's unmodified direct examination of SBC as of July 31, 2026 11:50pm EDT (issued August 11) reconciles $5,972,459 of cash and cash equivalents plus $751,220 of U.S. government backed debt — $6,723,679 — against SBC issued across all supported blockchains while preserving the engagement's "has not attempted to, independently confirm the authenticity and accuracy" limitation verbatim in the manifest.

`cadd-independent-assurance` verifies Baker Tilly WM LLP's monthly CSAE 3000 reasonable assurance report for CADD through the CAD Digital attestation index (`tetradg.com/cadd-reserve-attestations/`). The index publishes the reports as Google Drive share links (`drive.google.com/file/d/<id>/view?usp=sharing`), which carry no report date, so discovery dates each candidate from its anchor text ("June 2026 attestation" → 2026-06-30) and rewrites the share links to direct-download URLs (`drive.google.com/uc?export=download&id=<id>`) on the pinned `drive.google.com` / `drive.usercontent.google.com` report hosts. As with every assurance publisher, a newer unreviewed attestation on the index fails the adapter closed rather than silently serving the older reviewed report. The June 30, 2026 report (signed July 20, examined at 11:59 p.m. UTC; the report also prints a June 22 instant, which the manifest guards against) reconciles CAD 1,156,401.00 of Canadian-dollar cash against 1,142,226 CADD in circulation across the Base, Tempo and Ethereum chains.

`brla-independent-assurance` verifies UHY Bendoraytes & Cia Auditores Independentes' unmodified reasonable-assurance engagement under NBC TO 3000 (ISAE 3000 equivalent) for the July 31, 2026 report linked from BRLA's official Notion transparency page. The report reconciles R$126,110,433.68 of BRL cash and overnight repos collateralized by Brazilian corporate debentures against R$116,923,169.82 of redeemable BRLA tokens across Polygon, Moonbeam, Celo, and Gnosis. Runtime discovery re-resolves the signed download URL through Notion's `loadPageChunk`/`getSignedFileUrls` APIs and fails closed unless every pinned identity — root page, root child tree, 2026 year header, report block, attachment — matches exactly. The fetched PDF is then pinned by exact SHA-256 and byte length in the reviewed manifest, and the unmodified conclusion keeps the binding in the independent-assurance tier. An observed-but-unreviewed positive asset code no longer throws: it publishes as a slice with no `assetClass` (the scorer's bounded-unknown path) counted in `metadata.unknownExposurePct` with a degraded `brla-asset-code-unclassified` warning, while an unparsable amount still fails the adapter closed.

`agora-independent-assurance` binds AUSD to Grant Thornton LLP's unmodified AICPA attestation examination of the July 31, 2026 Agora Dollar Reserve Report published on the official Fern-hosted transparency index. The report reconciles $241,476,955 of assets — short-dated Treasuries, overnight Treasury repos, fund and company cash, and segregated USDC/PYUSD — against $240,745,867 of AUSD in circulation across all AUSD Approved Blockchains, excluding company-custodied AUSD Created from circulation. The index links the report through an expiring signed URL whose path segment is the PDF's SHA-256, so runtime rewrites that July link to the reviewed stable Fern mirror URL only when the path hash matches the reviewed bytes; a missing, drifted, or duplicated July link fails closed. The shared assurance verifier then fetches the mirror and re-verifies the exact bytes; the unmodified examination holds AUSD in the independent-assurance tier.

`fdusd-independent-assurance` binds `fdusd-first-digital` to AOGB CPA Limited's independent limited-assurance conclusion under ISAE 3000 (Revised) for the July 31, 2026 report (Poppler 26.07.0): $351,643,471.73 of assets — $266,841,243.14 Treasury bills, $13,000,000 government-guaranteed fixed deposits, $71,802,228.59 custody cash — against $350,156,619.24 of redeemable FDUSD across Ethereum, BSC, Sui, Solana, Arbitrum, and TON. The issuer's firstdigitallabs.com index blocks Cloudflare Worker egress on every fetch strategy, so discovery pins the issuer's Webflow mirror (`firstdigitallabs.webflow.io/transparency`) with reports served from `cdn.prod.website-files.com`; candidates must carry an ISAE 3000 marker, whitepaper handouts are excluded, and each candidate is dated to its printed month-end. The manifest records the examined instant exactly as printed — 9:00pm Eastern, `2026-07-31T21:00:00-04:00`, i.e. `2026-08-01T01:00:00Z` — instead of the earlier 00:00Z misdate, so verified freshness starts 25 hours later. The legacy `fdusd-transparency` parser is retired (`parkedSince: 2026-09-09`, `nextReview: 2027-03-09`) and retained only for historical review.

`issuer-attested-report` compiles hash-pinned, reconciled issuer-signed reserve reports through the shared manifest pipeline without implying an examiner. `brlv-crown` binds Fact Finance's 31 August 2026 "Prova de Reservas" memo — a technical verification by an RWA verification agent (signer João Eduardo Viana, "Responsável técnico"), not an accounting assurance opinion — reconciling R$ 380,315,618.44 of Treasury bonds, Selic ETFs and cash against R$ 378,063,743.00 of BRLV in circulation (100.60% collateralization). `audm-macropod` binds Catena Digital's issuer-CEO attestation as at 1 September 2026, reconciling A$ 2,899,657.78 of Westpac AUDM Trust Class A cash against A$ 2,898,943.78 of AUDM across Ethereum, Redbelly, Solana and Base. Both conclusions are `issuer-attested`, so the shared descriptor is `static-validated` / `issuer-attested` and neither coin gains score-grade admission. AUDM's index also hosts a Hall Chadwick ASRS 4400 agreed-upon-procedures report (30 June 2026) that is stronger in attestor independence but ~10 weeks older than the monthly issuer report; it remains a future `agreed-upon-procedures` candidate.

`anzen-usdz` is now independent: it reads USDz `totalPooledSPCT()` and actual SPCT holdings on-chain and reconciles them against bridge-adjusted USDz liabilities across the five issuance chains instead of trusting global SPCT supply. Pooled or held SPCT shortfalls publish the lesser backed amount and its liability ratio with a degraded `reserve-undercollateralized` warning, while paused or blocked redemption prerequisites retain the slice and emit `routeStatus: paused` with a degraded `route-paused` warning.

ERC-4626 held-versus-deployed accounting: `erc4626-single-asset` measures `asset().balanceOf(vault)` in the dependent Multicall3 alongside share conversion and decimals for every route. Only the idle share of `totalAssets()` retains the configured token dependency; the remainder is a vault-named, high-risk strategy slice without `coinId`, quantified as `unknownExposurePct`. Holdings at or above total assets preserve one underlying slice; unreadable holdings publish wholly unattributed exposure with a degraded warning rather than inventing a token balance. fxSAVE's pinned asset is untracked fxSP pool shares, so no fxUSD dependency is fabricated. `anzen-usdz` values the lesser of pooled and held SPCT using the same-run 18-decimal oracle price, marks the receipt blacklistable, and publishes oracle-driven coverage below one as degraded. Both adapters retain independent on-chain observation evidence with not-applicable freshness from latest-state reads; neither claims an independent look-through into unattributed strategy or private-credit holdings.

`moc-doc` replaces DOC's curated-only adapter with independent same-run Rootstock accounting evidence. Three serialized RPC batches pin a fresh block, 28 accounting/identity reads, and a closing header. Four reviewed implementation pins and canonical MoC/state/connector/DOC/oracle identities must match. Accounted rBTC must reconcile with protocol collateral, DOC supply, senior locked bitcoin, and residual junior BPRO capital; disabled leveraged positions must remain empty. BPRO represents residual risk capital, not another par-dollar claim. Only accounted collateral is admitted; donations do not inflate backing, and a custody deficit caps backing at the actual balance and degrades. A valid protocol oracle, pause/liquidation checks, and the closing block hash are mandatory. Undercoverage and unsafe protocol state degrade. Values use the protocol BTC oracle, not an independent market-price stress valuation. Pinned `freeDoc()` capacity supports the existing fee-bearing redemption route only when coverage is strictly above the onchain `getProtected()` threshold; equality or lower coverage degrades and publishes zero capacity. The RPC URL is explicitly configured and schema-validated. Implementation/accounting drift fails closed and requires a new contract review.

`moc-v3-buckets` is a narrow Money-on-Chain V3 adapter (USDRIF): pinned Rootstock block/header and proxy implementation hashes, bucket-identity and sole-token revert checks, liability/supply reconciliation, wallet-accounting exclusions, and protocol-vs-market price divergence guards; it emits no redemption telemetry. The September 5 review at Rootstock block 9214021 binds both upgraded buckets to the verified MocCARC20 implementation `0x5316384be99310fdea0da0b3cc0d0bb9e7f0887f` and rechecks the RIFUSD peg-container identity.

`zephyr-scanner` consumes Zephyr's reserve snapshot API for `zsd-zephyr-protocol`, preserving the snapshot capture timestamp, ZEPH reserve value, ZSD supply, reserve ratio, moving-average reserve ratio, and ZSD yield-reserve metadata. For `zys-zephyr-protocol` the published `collateralizationRatio` is the ZSD yield reserve's underlying ZEPH backing ratio (ZSD's own reserve ratio), not the tautological share-price identity, so a ZYS wrapper reports the same material coverage as the ZSD it wraps. It is proof-class because the feed is protocol-published native-chain telemetry over volatile ZEPH collateral rather than independently verified asset-level reserve evidence. A snapshot carrying none of the four circulating-supply encodings (`raw.liabilities`, `raw.num_stables`, `on_chain.zsd_circ_atoms`, `on_chain.zsd_circ`) fails the adapter closed instead of publishing a supply-less snapshot.

Accountable note:
`accountable` configs may exclude reviewed buckets from strict `total_reserves` reconciliation when the dashboard publishes auxiliary reserves outside the reported total. Those excluded buckets are also omitted from normalized reserve slices so auxiliary values cannot dilute or inflate the published collateral mix. Configs may also allow reviewed signed exposure buckets; negative buckets are omitted from reserve slices, recorded in metadata, and degrade the snapshot instead of opening the breaker.
Yuzu's timestamped `exposure_split` reconciles its signed bucket sum against the nearest valid reserve-total point in the dashboard timeline and uses `exposure_split_ts` for composition freshness. The current outer `total_reserves` and `total_supply` remain the coverage inputs, so a lagging composition packet cannot be compared to a newer headline total or presented as newly refreshed.
Deployment-named buckets do not imply an asset-quality tier: Unitas's `Binance`, `Solana`, and `Bnb_smartchain` rows are all classified as the protocol's high-risk delta-neutral strategy basket because the source discloses location, not the underlying collateral split. Apyx's timestamped `reserves_split` remains asset/category-oriented and maps STRC, Protocol Owned Liquidity, Cash & Equivalents, and Inventory independently. Accountable's source-level `collateralization` value is also emitted as normalized `collateralizationRatio`; ratios below the shared 99.5% reserve-coverage threshold degrade the snapshot. The reviewed July 18 APXUSD snapshot reports `0.881615`, so its reserve composition remains fully normalized for mix display while the 11.8385% liability-coverage deficit is explicit and the snapshot is not score-grade eligible.
Yuzu's Fasanara `mGLO` loop remains a high-risk named reserve slice but carries no tracked dependency edge because mGLO is not a tracked Pharos asset and must not be conflated with tracked mGLOBAL. The distinct `mGLOBAL` loop keeps its reviewed tracked-asset mapping.
Feeds that publish composition outside `data.reserves` can opt into `layout: "asset-breakdown"` to read the root-level `data.assetBreakdown` category tree instead of a `reserves` sub-bucket; Tori's four categories (Money Markets, Cash & Equivalents, Delta-Neutral Futures Arbitrage, On-chain Buffer) are mapped this way with `data.ts` as the verified source timestamp, while the undisclosed custodian identities stay captured as reviewed custody/counterparty risk factors rather than unknown reserve value.

Chainlink NAV note:
`chainlink-nav` now supports both standard AggregatorV3 feeds and Ondo router-style NAV lookups. When `oracleMethod = "getAssetPrice"`, the adapter calls `getAssetPrice(token)` on the router and, when available, follows `tokenToRWAOracle(token) -> getPriceData()` to recover a verified freshness timestamp instead of treating the feed as permanently timestampless.

P5 NAV admission is scoped to **native fund-share exposure**, not look-through holdings: `chainlink-nav` requires `params.navScope = "native-fund-share" | "portfolio"` (also inherited by `superstate-liquidity`); `chronicle-nav` declares the same required `navScope` on its params schema. The seven reviewed native Chainlink bindings are **JAAA, ACRED, VBILL, JTRSY, USTBL, EUTBL, USYC**; each emits one 100% slice naming its fund share. Chronicle's **ACRDX/STAC/BUIDL**, the six Spiko API share classes **eurSAFO, SAFO, gbpSAFO, UKTBL, eurSPKCC, SPKCC**, and Superstate's **USTB** retain `independent` admission on that same narrow basis, not as measured internal holdings; Spiko's API origin remains `issuer-attested`. The ten excluded portfolio/strategy bindings are **OUSG, XAUM, IAUon, mTBILL, mGLOBAL, mF-ONE, mHYPER, mAPOLLO, mRe7YIELD, mMEV**: `portfolio` emits `nav-portfolio-composition-unverified` with `effect: degraded`, preserving detail-visible NAV while `store-views.ts`'s scoring-warning gate excludes it despite the fixed adapter-level `independent` class. This warning is not scoring-allowlisted; restoring composition scoring requires a dated holdings producer, not a wider NAV freshness window. Suspended USDY also carries `portfolio`; quarantined WTGXX/TBILL carry native fund-share scope without changing their suspension. Freshness remains `verified` only from upstream oracle/API as-of time (Chronicle consumer age), never from the fetch clock; timestampless NAV stays `unverified`. Existing transport and scoring windows, including the five four-day transport-only fund configs, are intentionally unchanged.

Oracle transport acceptance (`params.maxOracleAgeSec`) and scoring freshness (`scoring.maxSourceAgeSec`) remain separate. USYC, OUSG, IAUon, mTBILL, mF-ONE, mAPOLLO and BUIDL have explicit four-day scoring windows; mMEV has five days and mHYPER seven days. The September 7, 2026 review checked issuer pricing documentation and recent oracle rounds: business-day updates for USYC/OUSG, market-calendar updates for IAUon, and observed daily or less frequent Midas product updates. These per-coin limits prevent the default 48-hour source-age fallback from rejecting ordinary weekend NAVs. The independent 48-hour fetch-liveness limit remains unchanged, and oracle acceptance does not automatically extend scoring age. ACRDX retains its existing policy because its Chronicle feed continued updating through the weekend. Source references: [Circle USYC pricing](https://help.circle.com/support/en/understand-usyc-pricing?id=kb_article_view&sysparm_article=KB0010576), [Ondo OUSG yield](https://docs.ondo.finance/qualified-access-products/ousg/yield), and [Midas independent reporting](https://docs.midas.app/tokens/mtbill/independent-reporting).

USDY's `usdy-holdings-report` binding is **suspended**. September 9 server probes of the correct Dropbox archive returned ZIP downloads or HTML whose monthly file entries were encoded in proprietary Edison protobuf payloads, not a verified dated HTML/JSON index. A pinned daily PDF cannot establish newest-report freshness and ages out even when the report series continues. The September 3, 2026 compiled manifest and reviewed sidecar remain static evidence: 15 Treasury CUSIP positions plus First Citizens MMA and StoneX cash total $2,147,340,936.79 against $2,139,209,197.45 LLC principal. The reviewed composition retains 2.62% excluded BVI issuance as high-risk unknown. USDY has no live or score-grade reserve evidence under this suspended binding.

USDY maintenance: use the [USDY LLC daily archive](https://www.dropbox.com/scl/fo/375wdvar3rbc7o23nxsgp/AOFY8jhpENaNx9WAw-WPnbY?rlkey=4icqn1z9bez725wywr30fx52a&dl=0), not the separate Ondo Stocks archive. Download and review the newest LLC PDF, then run `npx tsx scripts/maintenance/refresh-usdy-holdings-report.ts --input <download.pdf> --url <exact-dl=1-file-url> --listing-url <current-month-folder-url>` (requires Poppler `pdftotext`); refresh the reviewed sidecar alongside the manifest. Manual review does not lift suspension. Re-enabling requires proven newest-report discovery and an `attestation-pdf-index`-style validated-static model: validate the newest report's host, PDF magic, bounded size and business-day freshness using its New York end-of-day date, require it not to predate the manifest, and publish only the last reviewed composition. That model must expose `reviewedReportDate`, `newestReportDate`, and excluded-scope `unknownExposurePct`, and degrade with `reviewed-composition-lagging` when the date gap exceeds 30 days. Its `static-validated` evidence remains detail-visible, not score-grade; Worker PDF content parsing is not permitted.

Felix's kHYPE branch uses the reviewed Kinetiq label so the next live observation joins its existing collateral classification.

Chronicle NAV note:
`chronicle-nav` reads Chronicle Labs Proof-of-Asset VAO consumer contracts via `readWithAge()` (value + age), validating the value, rejecting stale or future-dated ages, and reading the coin token's on-chain supply with its actual decimals. The v1 path is scalar-only: it emits one configured 100% NAV slice with verified freshness from the consumer age, mirroring `chainlink-nav`'s single-bucket model. Chronicle's IPFS `UPoked` proof objects (position-level composition) are a documented future enhancement, not consumed today. ACRDX, STAC and BUIDL bind this adapter. `params.navScope` is required; `portfolio` scope emits the same `nav-portfolio-composition-unverified` degraded warning as `chainlink-nav`. Supply is aggregated across every registry-typed EVM/Tron deployment in `coin.contracts` (mirroring `chainlink-por`): non-EVM deployments (BUIDL's Solana and Aptos, ACRDX/STAC's Solana) are omitted with a `por-supply-chain-omitted` info warning, a chain without a configured RPC is omitted with an info warning, a failed per-chain read degrades with `supplyReadComplete = false`, and a run fails closed only when every read fails. `supplyCoverageComplete` additionally requires zero omissions, so the EVM-only BUIDL aggregate (which omits ~41% of outstanding shares on Solana/Aptos) can never present itself as the full supply. BUIDL's reviewed look-through composition (tri-party repos plus Treasury bills from the Chronicle holdings table) remains recorded in its reserve sidecar rationale while the live row carries the aggregate fund-share identity.

Chainlink PoR note:
`chainlink-por` supports USD-denominated reserve feeds, commodity reserve-unit feeds, and share-quantity feeds. Commodity configs such as Kinesis KAG set `reserveUnit = "XAG"`; those snapshots persist reserve quantity/unit metadata and intentionally omit USD reserve totals, supply USD, and collateralization ratios because the feed proves physical commodity quantity rather than a USD liability value. Gram-denominated commodity units `XAU_G`/`XAG_G` ("grams of fine gold/silver") additionally compare feed grams 1:1 against token supply and apply only to tokens pegged to one gram of the metal. Kinesis KAU is bound this way with `reserveUnit = "XAU_G"` ("grams of fine gold" — the feed proves grams of fine gold, not troy ounces): Chainlink's feed registry marks the value `g`, matching Kinesis's published gram-denominated holdings, and the live answer on 2026-09-09 (2,567,133.466) matches the April 2026 Bureau Veritas fine-gold gram figure digit for digit; one KAU = one gram, so the gram answer divides cleanly by token count. KAU's prior binding was removed on 2026-09-04 in commit `de9d47fe4` with no recorded rationale; it was re-enabled on 2026-09-09 on the gram unit. Kinesis KAG keeps `reserveUnit = "XAG"` and is not bound to a gram unit: verified on chain, its feed answers track the token count ~1:1 in troy ounces (3,688,827.985 against 3,721,963.82 KAG, ~99.1% coverage; a gram-unit answer would read as ~3.2%), and one KAG is one troy ounce rather than a gram. Tracker certificates whose feed answer is a count of underlying fund shares (bIB01) set `reserveUnit = "SHARES"`: the snapshot persists `totalReserveQuantity`/`supplyTokens` quantities instead of USD-named fields, and coverage compares share quantity against token quantity because each token is a 1:1 share claim.

Supply-comparable coverage (USD, SHARES, and gram-commodity units) measures feed reserves against the multichain `totalSupply()` aggregate over the coin's registry-typed EVM and Tron contracts (chain classification comes from `CHAIN_META` in `shared/lib/chains`; non-EVM deployments such as Solana or NEAR are omitted with an info warning, and a zero `totalSupply()` read is a valid empty deployment rather than a read failure). `supplyReadComplete` records only whether every attempted EVM/Tron read succeeded, while `supplyCoverageComplete` additionally requires that no registry deployment was omitted (a non-EVM chain such as Solana or NEAR sets `supplyCoverageComplete = false` while `supplyReadComplete` stays true and the info warning fires). For issuers that pre-mint inventory into their own custody — where gross `totalSupply()` is not the reserve liability — an optional `params.issuerCirculationProbe` (`kind: "backed-graphql"`, currently bIB01 against Backed's public `assetReserves` GraphQL feed at `api.backed.fi`) switches the liability basis to the issuer-published circulating supply. The probe fails closed: every nonzero circulating deployment must match a configured canonical contract (chain + address) for its decimals, an implausible circulation figure above the on-chain supply envelope is rejected, and a failed or rejected probe publishes no coverage ratio at all (degraded `por-circulation-probe-failed` / `por-circulation-implausible`) instead of falling back to the misleading gross ratio. On the circulating basis, reserves above circulation are an informational issuer-inventory surplus, not a scope-mismatch degradation.

`superstate-liquidity` extends USTB's on-chain NAV reserve proof with Superstate's public liquidity endpoint. Reserve slices remain NAV-based, while nested redemption telemetry uses the current Circle USD available amount plus USDC RedemptionIdle balance as bounded proxy capacity. Missing or malformed liquidity fields fail the adapter instead of falling back to NAV/AUM as immediate liquidity.

`usd1-bundle-oracle` reads USD1's Chainlink bundle oracle on Ethereum. The adapter decodes `latestBundle()` into a source timestamp and reserve value, cross-checks `latestBundleTimestamp()`, aggregates live USD1 `totalSupply()` across every registry-typed EVM and Tron deployment in `coin.contracts` for the supply denominator, and stores reserve metadata from latest-state on-chain data. Supply aggregation mirrors `chainlink-por`: non-EVM deployments (Solana, Aptos) and EVM deployments without a configured RPC are omitted from the denominator and reported as the `por-supply-chain-omitted` info warning, a failed per-chain `totalSupply()` read emits `partial-supply-read-failure` (degraded) with `supplyReadComplete = false`, and the run fails when no chain read succeeds — an Ethereum-only denominator would understate the liability and inflate the published ratio. The oracle publishes WLFI aggregate fund reserves across multiple products rather than USD1-earmarked collateral, so the adapter intentionally does not emit a `collateralizationRatio`. The raw fund-reserve / multichain-USD1-supply ratio is persisted as `fundBackingTotalRatio` alongside a `details.fundScope` disclaimer and per-chain `supplyContributions`; consumers must not read it as a 1:1 collateralization signal.

`frax-balance-sheet` now covers both `frxusd-frax` and legacy `frax-frax` through the Frax v2 balance-sheet API. It is a Frax-operated aggregation of largely public on-chain positions, with issuer-disclosed off-chain balances where applicable. Independent audit reads matched frxUSD's WTGXX, USTB, BUIDL, USDC, and Solana USDB balances at the API-pinned blocks, but Erebor cash, inventory completeness, valuation, and the broader FRAX position set still rely on Frax's response. Its diagnostic source origin therefore remains `issuer-attested`; that label is separate from the adapter's unchanged score-bearing `evidenceClass`, and the detail page uses the proof-style badge rather than claiming a wholly independently measured live feed. Known Frax ecosystem assets are classified explicitly, including the Erebor reserve account, locked VELO position, mixed sfrxUSD/scrvUSD Curve LP, and small USDT, BNB, and MATIC balances; the mixed LP remains unlinked rather than being misrepresented as pure crvUSD exposure. Any future unmapped balance-sheet exposure is aggregated and only degrades the run when material. For frxUSD redemption modeling, the adapter emits current stablecoin capacity as a USD amount and intentionally avoids reusing reserve-composition ratios as supply-relative redemption capacity.

`frax-fpi-collateral` parses Frax's `/v2/fpifpis/fpi-collateral` endpoint for FPI. Direct balances, supplies, ferry balances, and the Fraxswap LP decomposition are independently reconstructible on-chain, but the Convex allocation, complete inventory, and USD valuation still come from Frax's aggregation. The reserve evidence origin therefore remains `issuer-attested`; the nested Controller Pool observation does not relabel the complete reserve payload as independent on-chain evidence, and the detail page uses the proof-style badge. It excludes self-held FPI from reserve slices, nets self-held FPI against liabilities, classifies known Frax ecosystem assets, and degrades only when unmapped non-FPI collateral exposure becomes material. The same adapter run also records a V9-only Controller Pool redemption attempt from one pinned Ethereum block. The attempt validates dependency bytecode, oracle round freshness and agreement, CPI tracker identity and age, pause and peg-band state, fee, quote, FRAX balance, and maximum input capacity. A rejected attempt remains diagnostic and adds an informational warning; an accepted attempt is available only to the V9 extension route. The issuer collateral response and route attempt are published together, so an issuer API failure publishes neither.

`yamato` reads CJPY reserve state directly from Yamato's `getStates()` contract path and values ETH collateral with the configured Yamato price-feed `getPrice()` call. The latest-state on-chain read uses `freshnessMode = "not-applicable"` and publishes protocol collateral ratio and threshold metadata for the reserve detail view. Redemption `routeStatus`/`routeStatusSource` are only emitted when the same-run `paused()`/registry probe succeeds; a null probe withholds both fields (with an informational warning) instead of asserting an unobserved `open` route.

`youves-tezos` reads the Youves uUSD census for `uusd-youves` off the TzKT indexer at one pinned Tezos level (the first Tezos reserve adapter; `tezos` is a `type: other` chain whose REST endpoint `https://api.tzkt.io` is registered in the public read registry, and the bounded `tzkt.ts` reader caps bigmap enumeration so an attempt can never paginate without limit). The adapter sums vault-context collateral across all eight engine generations of the four branches — XTZ (v2 + legacy v1), tzBTC (v2, v1, and the pre-v1 engine the reviewed sidecar already counts), SIRS (v2 + legacy v1 LP engine), and USDt (v3) — validates every engine's `token_contract` against the uUSD token, and reads the uUSD token's own `total_supply` bigmap as the liability denominator. XTZ vault balances are the engine-recorded contexts whose physical XTZ sits in the per-vault KT1 contracts they reference (verified exact reconciliation), which the adapter documents as an informational warning. SIRS is valued through the reviewed on-chain SIRS LP reserve ratio (tzBTC atomic units per SIRS) times the DefiLlama tzBTC quote; XTZ, tzBTC, USDt, and the below-par uUSD liability are valued with DefiLlama quotes, and the adapter fails closed when any of the four collateral assets or uUSD lacks a qualified quote so an `independent` snapshot only publishes when every material collateral has a supported valuation path. Every read is pinned to the same level and the snapshot publishes that level and timestamp as `observedBlock` under `not-applicable` freshness.

`kava-cdp` reads Kava Mint's legacy CDP ledger for USDX (`usdx-kava`) straight off the public Kava LCD (`api.data.kava.io`, the same host the `kava-pricefeed` runtime price source trusts): per-collateral-type totals from `/kava/cdp/v1beta1/totalCollateral` and `/totalPrincipal`, the module's `collateral_params` for each type's spot pricefeed market and conversion factor, the pricefeed's aggregate price list, and USDX bank supply (`/cosmos/bank/v1beta1/supply/by_denom?denom=usdx`). Every non-zero collateral type is valued at its live spot market and published as one slice per collateral denom; a type with a positive balance but no live pricefeed market degrades with a quantified `unpriced-collateral-type` unknown-exposure warning (quantified as that type's share of USDX principal, the only precise per-type weight the CDP ledger exposes) instead of being silently dropped or proxy-priced. USDX trades below par, so the liability is never assumed to be one dollar: `collateralizationRatio` is total collateral USD over USDX principal valued at the live `usdx:usd` pricefeed price, and `supplyUsd` applies the same market price. Freshness is `not-applicable` latest-state with the LCD block height and time pinned into `details`, mirroring the pricefeed module's block identity and staleness checks; there is no holder-facing redemption route (borrower repay-only), so no redemption telemetry is emitted.

`hliquity-hedera` reads HCHF's Liquity-style CDP state (`hchf-hedera-swiss-franc`) through the public Hedera mirror node REST API (`https://mainnet-public.mirrornode.hedera.com/api/v1`). Hedera has no Multicall3, so the adapter pins the latest record-stream block and executes a same-block census of `contracts/call` simulations: `TroveManager.getEntireSystemColl()`, `getEntireSystemDebt()`, `MCR()`, `getTCR(priceFeed.fetchPrice())`, `StabilityPool.getETH()` and the HCHF HTS token `totalSupply()` (debt and supply are reconciled to the unit at every block). The single HBAR slice values the complete collateral census (active troves plus Stability Pool) at the DefiLlama HBAR market price and the HCHF debt at the ECB CHF/USD reference rate (Frankfurter); a missing or stale franc rate fails the attempt closed, a missing HBAR quote degrades and omits the ratio, and a market ratio below the 110% MCR publishes as a `reserve-undercollateralized` degraded warning. Freshness is `not-applicable` with the pinned block number and consensus timestamp in `observedBlock`, and the holder-facing redemption route is reported open only while the protocol-priced TCR clears MCR at the same block.

`djed-cardano` reads Djed's ADA bank census for `djed-coti` through the public Koios Cardano query API (registered in the public read registry like the Tezos and Hedera entries). The bounded `koios.ts` reader pins the `/tip` head — rejecting a head older than twenty minutes or more than two minutes in the future — stamps `{ chain: "cardano", number, timestamp }` into `observedBlock` under `not-applicable` freshness, caps every address read at 500 UTxOs and 100 assets per UTxO inside a twelve-request attempt budget, and echoes every response against its request (unrequested, duplicate, or missing rows fail closed). The adapter locates the bank's single `DjedStableCoinNFT` marker UTxO (quantity must be exactly one, else the run fails closed), treats the whole bank ADA balance as the single reserve slice, and derives circulating DJED as `asset_info` minted supply minus the unissued DJED stock held in the bank — the bank's own DJED/SHEN stock is unissued inventory, not reserve backing — so both sides of the ratio come from the same pinned tip. Valuation uses live DefiLlama ADA and DJED quotes with no silent $1 DJED fallback, an undercollateralized market ratio publishes degraded with `reserve-undercollateralized`, extraneous bank assets are reported as an info warning and excluded from valuation, SHEN circulating is emitted as a diagnostic only, and the descriptor declares `redemptionTelemetry: none` because Djed mint/burn is protocol-internal rather than a holder-facing route. The adapter also reconciles its on-chain liability (minted DJED minus bank-held unissued stock) against the DefiLlama list `circulating`, emitting a degraded `djed-supply-divergence` warning when the relative divergence exceeds 10%; the list figure is recorded in `details` (`listCirculatingUnits`, `supplyDivergencePct`) but never replaces `supplyTokens`.

Business-day NAV feeds can set `maxOracleAgeSec` when their oracle is expected to pause through weekends or market holidays; `ousg-ondo-finance` and `mtbill-midas` use a 4-day window so normal Friday-to-Monday NAV cadence does not trip their reserve circuit breakers. Weekly or institutional NAV feeds can also set a wider oracle-read grace window while pairing it with `scoring.maxSourceAgeSec`; for example, `mre7yield-midas` accepts up to 14-day Chainlink NAV reads at the adapter layer but degrades snapshots older than 7 days before they can enter score-grade live collateral. Spiko EUTBL uses the same split policy with a 7-day Chainlink NAV read window and a 4-day scoring freshness cap, so delayed institutional NAV updates can keep reserve detail/status surfaces populated without entering score-grade live collateral.

`spiko-api` reads Spiko's public `share-classes/{symbol}/totals` REST endpoint directly for the six Spiko fund shares that do not have a Chainlink NAV oracle (`eursafo-spiko`, `safo-spiko-usd`, `gbpsafo-spiko`, `uktbl-spiko`, `eurspkcc-spiko`, `spkcc-spiko`). `totalAssets.value`, `totalShares`, and `netAssetValue.amount.value` are all denominated in the same fund currency, so `collateralizationRatio` needs no FX conversion; `totalReserveUsd`/`supplyUsd` are only persisted when the fund currency is USD so EUR/GBP fund totals are never mislabeled as USD. A payload whose asset and NAV currencies differ or are missing fails closed, and an under-collateralized ratio publishes as a `reserve-undercollateralized` degraded warning. The adapter emits the coin's single configured reserve slice (matching its curated bucket: swap/money-market exposure for the SAFO share classes, UK Treasury bills and cash for UKTBL, and the cash-and-carry strategy for the SPKCC share classes) at 100%, and uses `netAssetValue.updatedAt` as a verified source timestamp. Because the CACEIS-administered NAV only updates on business days, `spiko-api` uses `BUSINESS_DAY_NAV_SOURCE_MAX_AGE_SEC` (5 days) so a holiday-extended weekend doesn't trip staleness before the next NAV lands.

`usdtb-transparency` reads Ethena's public `backing-and-supply/current` REST endpoint for `usdtb-ethena`. `backingAssets` amounts are summed per asset across custodian entries; BUIDL and BUIDL-I (the same BlackRock fund's institutional share class) merge into one slice, USDC and USDT map to their tracked coins, and `assetsInMotion` becomes its own honestly-labeled settlement-float slice. USDtb's own self-held balance is excluded from backing as self-referential (an info warning fires if it is ever nonzero), and any future unmapped backing-asset key degrades into an "unmapped" bucket rather than failing the sync closed. `collateralizationRatio` is the sum of all backing slices (including assets in motion) over `supply`, and `lastUpdatedAt` is a verified source timestamp under `DASHBOARD_SOURCE_MAX_AGE_SEC` (3 days). Numeric strings in `backingAssets` amounts, `supply`, and `assetsInMotion` are deliberately converted, and a non-numeric value now fails the sync closed instead of silently summing as zero.

`bridge-transparency` reads Bridge's public transparency API (`transparency.bridge.xyz/v0/stablecoins/{slug}`) for `usdsui-sui` (`usd_sui`) and `pathusd-bridge` (`path_usd`). The disclosed `reserves` components (cash, treasury) are published as measured slices with stable source keys; `totalReserveUsd` and the on-chain liability (`supplyUsd`) come from the payload's own totals, and `collateralizationRatio` is reserves over liability — both sides from the same issuer snapshot, never a mixed local/global supply. Component sums are reconciled against both published totals with a tight absolute tolerance: Bridge rounds its headline totals to cents (pathUSD's components sum $0.32 high), so drift up to $1 is treated as rounding while larger drift publishes as a degraded `bridge-component-sum-drift` warning, and a liability shortfall beyond the same tolerance publishes the below-1 ratio with `reserve-undercollateralized`. `last_updated` dates the reserve observation (it holds across repeated requests and moves only when balances change), so it is a `verified` source timestamp under the dashboard age tier. The origin class is `issuer-attested` — independent measurement of an issuer-published feed, mirroring the frax/tether balance-sheet precedent, not third-party assurance. No redemption capacity is derived from the cash label. The configured `params.slug` must match the primary URL's final path segment, so one coin's binding can never silently read another stablecoin's payload.

`megausd-custody` reads MegaUSD's public `backing-and-supply/current` REST endpoint for `usdm-mega`. `backingAssets` amounts are summed per asset across custodian entries; USDC maps to `usdc-circle` and USDtb to `usdtb-ethena`. USDm's own self-held balance is excluded from backing as self-referential (an info warning fires if it is ever nonzero), and any future unmapped backing-asset key degrades into an "unmapped" bucket rather than failing the sync closed. The endpoint's local `supply` field is a single-chain figure that does not represent the tracked global supply, so it is never used as the liability denominator: the adapter persists `totalReserveUsd` without a `collateralizationRatio`, and `lastUpdatedAt` is a verified source timestamp under `DASHBOARD_SOURCE_MAX_AGE_SEC` (3 days). The diagnostic source origin is `issuer-attested` (the issuer's custody inventory), separate from the unchanged score-bearing `evidenceClass: "independent"`. Numeric strings in `backingAssets` amounts are deliberately converted, and a non-numeric value fails the sync closed instead of silently summing as zero.

`krwq-custodian` reads KRWQ's public `custodian-assets` REST endpoint (`https://www.krwq.cash/api/custodian-assets`) for `krwq-iq`. The payload's three legs — USDC custodian, frxUSD custodian, and Base treasury USDC (documented as the Korean Treasury Bond transition bucket) — map to `usdc-circle` and `frxusd-frax` and are cross-verified against on-chain `balanceOf` on the pinned Circle/frxUSD token addresses (the Ethereum legs read in one Multicall3 aggregate, the Base treasury as a second-chain balance). The issuer-reported USD value is always kept, and a `degraded` warning fires when an on-chain read fails (the frxUSD custodian proxy's `balanceOf` reverts on public RPCs) or diverges from the raw amount, so a verification gap never fails the sync closed. Multichain `totalSupply` over the six tracked KRWQ contracts yields `supplyUsd` and `collateralizationRatio = totalReserveUsd / supplyUsd`, and the payload's ISO `timestamp` is a verified source timestamp under `DASHBOARD_SOURCE_MAX_AGE_SEC`.

`kerne-signed-por` reads Kerne's hourly EIP-191-signed proof of reserves (`https://app.kerne.fi/api/por/signed`) for `kusd-kerne`. It rehashes `signed_payload_canonical` to `attestation_hash`, recovers the signer over the raw SHA-256 digest against the pinned `params.signerAddress` (failing closed on hash or signer mismatch), then reads the combined USDC `balanceOf` at the configured Base PSM contracts in one Multicall3 aggregate and degrades rather than fails when that cross-check read or divergence occurs. The signed `timestamp` is `verified` freshness under the issuer's published 7,800-second stale threshold, and the single measured slice is `kerne-signed-por:psm-usdc` (`usdc-circle` collateral) with `collateralizationRatio = psm_usdc_reserve / outstanding_kusd`.

`ethena-whitelabel` reads Ethena's whitelabel transparency endpoint (one `data[]` payload shared by every whitelabel stablecoin, selected by `params.stablecoin`) for `suiusde-sui`. The adapter sums the `custodians` array, never the display-level `rows` projection (which merges USDe+USDC under one custodian into a single "USDe/USDC" row), and maps USDe to `usde-ethena` and USDC to `usdc-circle`. Custodian rows on the off-chain Coinbase Prime network are kept as a separate slice without a `coinId` and surfaced with an informational `off-chain-custody` warning so the adapter never describes every row as on-chain verified; a genuinely unmapped asset degrades into an "unmapped" bucket instead of failing closed. `sourceTimestamp` comes from the payload's millisecond-epoch `lastUpdated`, and `collateralizationRatio` is the summed custodian backing over `totalSupply`.

`tether-transparency` reads Tether's public `transparency.json` (one payload shared by `usdt-tether` and `xaut-tether`, selected by `params.currencyIso`). This is a coarse issuer balance-sheet feed like `frax-balance-sheet`: it does not itemize reserve composition, so `params.slices` carries a configured static mix reviewed alongside each coin's `collateral` disclosure, while the live payload validates `total_assets` / `total_liabilities` and freshness. `collateralizationRatio` is `total_assets / total_liabilities`; the USD-denominated `totalAssetsUsd`, `totalLiabilitiesUsd`, and `shareholderEquityUsd` metadata fields are only persisted for `usdt-tether`; XAUt's unit is gold-equivalent, not verified USD, so only the dimensionless ratio is kept. Per-chain issued liabilities (`totalAuthorized - notIssued`) and quarantined amounts from `data_formatted[].blockChains` populate `metadata.details.chains`, and a nonzero quarantined balance on any chain raises an info warning. The freshness timestamp comes from the entry's `id` (Unix seconds), validated against `DISCLOSURE_SOURCE_MAX_AGE_SEC` (the 7-day disclosure tier) rather than the 3-day `DASHBOARD_SOURCE_MAX_AGE_SEC` tier, because the feed publishes on a Friday-publish/weekend-skip cadence that left no margin under the narrower bound. USDT liabilities are substantially reconstructible from the known native ledgers, and XAUt's canonical Ethereum liability is directly reconstructible, but the live reserve-asset totals are not: cash, securities, loans, custody, and vaulted gold remain off-chain between quarterly BDO assurance dates. The diagnostic source origin therefore remains `issuer-attested`, and the detail page uses the proof-style badge rather than claiming an independently measured live reserve feed. The separate `evidenceClass: "independent"` remains unchanged pending a score-methodology decision.

Adapter helpers now live in a small helper family, with `worker/src/cron/reserve-adapters/helpers.ts` kept as the shared import surface:

- HTTP JSON / HTML / binary fetch wrappers (`fetchJsonWithRetry`, `fetchTextWithRetry`, `fetchBinaryWithRetry`); response bodies are consumed inside the per-attempt timeout, and every failed body (oversized, aborted, HTTP error) is cancelled so connections return to the six-request budget
- HTML parser failure helpers that distinguish upstream layout drift (`layout-changed`) from content decoding failures (`parse-failed`) so attempt logs are more actionable for scraped disclosures
- Shared bucketed-composition accumulation and classification helpers (`classification.ts`) reject non-finite or negative rows before classification; zero rows are ignored. Unknown-exposure warnings use the adapter's reviewed policy threshold (strictly greater than the maximum, matching validation), rather than a separate hardcoded cutoff. Cap reports unconfigured asset value, Origin includes the measured coverage residual, Nest includes unmapped positions and reconciliation residuals, and DOLA includes non-FiRM issuance relative to fresh circulating supply; unavailable DOLA supply degrades the observation instead of claiming zero unknown exposure.
- Shared unverified-freshness metadata helper so timestamp-less dashboard feeds explain why they remain non-scoring, plus a not-applicable freshness helper for reviewed latest-state on-chain/API reads
- DefiLlama valuation quotes (`fetchDefiLlamaPrices`) require timestamps no older than one day and confidence at least 0.8 for clean admission. Missing/failed quality evidence emits a degraded warning; a numeric value is retained only with that warning. Upstream quote requests use sorted, deduplicated asset identities, while each caller receives its own logical-key price map and warnings, including on cache hits. Branch underlying-token and tracked-cache substitution requires an explicit reviewed `underlyingPrice1to1: true`; fixed prices and configured `priceToken` remain explicit configuration choices. Collateral-position APIs use the oldest per-asset timestamp for fully timestamped baskets, unverified freshness with a warning for partial coverage, and not-applicable freshness when no timestamps exist. Parallelizer values collateral using oracle word 4 (redemption), not word 0 (mint).
- EVM balance, total-supply, hex-call, and Multicall3 aggregate reads (`fetchErc20Balance`, `fetchErc20TotalSupply`, `fetchOnchainMulticall3`)
- Solana / on-chain mint-supply reads (`probeTrackedTokenSupply`, `probeOnchainTotalSupply`) used by `curated-validated` for tracked assets
- Input-kind type guards and validators (`requireJsonInput`, `requireJsonInputFromConfig`, etc.)
- Slice normalization / valuation / unknown-exposure math (`slice-math.ts`) preserves every identity and classification field when grouping slices. Percentage-source adapters retain the absolute upstream sum deviation in `metadata.diag.rawSumDeviation` before rounding repair; validation degrades deviations above 0.5 percentage points and rejects deviations above 2. Configurable precision remains available through `normalizeSlices`, `slicesFromValues`, and `valueUsdFromBigIntPrice`.
- Risk validation (`isReserveRisk`)

`worker/src/cron/reserve-adapters/evm.ts` provides EVM address-decoding/contract-resolution helpers (`parseEvmAddressResult`, `resolveCoinContractAddress`); ERC-4626 vault introspection helpers live in `erc4626.ts` and raw on-chain call helpers in `onchain.ts`.

Pinned EVM observations use `pinnedBlockPlan` in `evm-observation-plan.ts`: resolve `eth_blockNumber` once for the attempt's chain, read that block's timestamp, and pass the returned attempt-local context through every dependent Multicall3 wave and individual-call fallback. `evm-branch-balances` (including Honey custody), `liquity-v2-branches`, `gho`, `liquity-v1`, `crvusd`, `resupply-pairs`, `cap-vault`, the on-chain `reserve-protocol-dtf` path, `origin-vault-balances`, `yamato`, and `usd1-bundle-oracle` publish `metadata.observedBlock = { chain, number, timestamp }`; on-chain quantities, conversion rates, protocol prices, and route flags share the pinned number rather than independently reading `latest`. DTF combines discovery, basket accounting, plugin prices, and optional wrapper output valuation into at most six dependent Multicall3 waves (five without Comet-decimal discovery), serialized under the adapter I/O limiter instead of issuing individual component calls. USD1 reuses its Ethereum pin for oracle and Ethereum supply reads and resolves a separate pin for each additional EVM supply chain; its primary `observedBlock` describes Ethereum, not the multichain aggregate. Tron supply and HTTP market-price inputs are not EVM-block-pinned. Missing block identity or timestamp fails the observation rather than silently reverting to `latest`; a secondary USD1 chain pin failure follows its existing degraded partial-supply policy. P13 Stage A treats block age as diagnostics only: a block timestamp more than ten minutes behind the attempt start adds the informational `observed-block-lag` warning, without changing admission or freshness classification. Number pinning is not a finalized-block or reorg-proof guarantee.

Sui has no bound reserve-adapter transport: the 2026-09-09 BUCK review verified the V1 protocol and TreasuryCap live through Sui's public GraphQL endpoint, but **does not bind `buck-sui-graphql`** — material BUCKETUS/BLUEFIN LP balances have no DefiLlama price, LP underlying BUCK circularity is unresolved, and V2 reserves belong to USDB. A weak evidence label cannot justify inventing unknown USD exposure or a collateralization ratio; the exact blocker is retained in `scripts/lib/reserve-coverage-notes.ts`.

### Adding a New Adapter

To register a new adapter for a coin's `liveReservesConfig.adapter`, edit these surfaces in order: **5 files, 6 edit sites** (4 files / 5 sites when the adapter takes no per-coin params). The shared schema and Worker registry tests fail if definition or fetcher coverage drifts.

1. **Params schema + descriptor declaration** — both live in `shared/types/live-reserve-adapter-declarations.ts`. Define the Zod params schema as a const above the table (or reuse `noParamsSchema` / an existing schema), then add one entry to `LIVE_RESERVE_ADAPTER_DESCRIPTOR_DECLARATIONS` whose `paramsSchema` **references that schema object directly** — there is no separate schema module and no string identifier to register. Declare accepted primary input kinds, source/evidence class, source-sharing policy, supported semantics/versions, redemption telemetry, validation policy, and only non-default provenance or display metadata. Prefer a named validation tier from `shared/types/live-reserve-adapter-policy.ts` (`LATEST_STATE_VALIDATION`, `DASHBOARD_VALIDATION`, `DASHBOARD_WITH_UNKNOWN_CAP_VALIDATION`, `MONTHLY_VERIFIED_VALIDATION`, `LATE_MONTHLY_VERIFIED_VALIDATION`, `DISCLOSURE_VALIDATION`, …); an inline `validation` block should carry a comment explaining why the adapter is an exception. EVM addresses use `EvmAddressSchema`, never a bare `z.string()`. The key union, `LIVE_RESERVE_ADAPTER_KEYS`, and the enriched `LIVE_RESERVE_ADAPTER_DEFINITIONS` map all derive from this entry.
2. **Adapter fetch function** — add `worker/src/cron/reserve-adapters/<key>.ts` exporting `async function fetch<Name>Reserves(coin, config, signal, ctx?): Promise<AdapterResult>`. Adapter contract lives in `worker/src/cron/reserve-adapters/types.ts` (`AdapterFn`, `AdapterContext`, `ReserveAdapterDefinition` — the last is a `Pick<>` of the shared declaration, so declaration fields reach the Worker without being re-declared). Use helpers from `./helpers` rather than rebuilding fetch/parse/freshness primitives.
3. **Worker fetcher wiring** — import the fetch function and add it to `LIVE_RESERVE_ADAPTER_FETCHERS` in `worker/src/cron/reserve-adapters/index.ts`. This stays a hand-written map because Worker implementations cannot enter the runtime-neutral shared registry and several keys deliberately share one fetcher; its `Record<LiveReserveAdapterKey, AdapterFn>` annotation makes a missing key a compile error, and `registry.test.ts` fails on the same gap.
4. **Coin config** — bind the coin's `liveReservesConfig` in `shared/data/stablecoins/coins/<id>.json`. A `params` block is required whenever the adapter's schema rejects `{}`.
5. **Adapter test and fixture** — add `worker/src/cron/reserve-adapters/__tests__/<key>.test.ts` and capture an HTTP-html fixture when applicable. HTML, JSON, and txt fixtures are all age-gated by `scripts/ci/check-html-fixture-age.ts` under the same `captured-at` stamp rule; a JSON or txt fixture that cannot carry the HTML-comment header is exempted in that script with a documented reason.

Docs are not a per-adapter step: the Adapter Registry notes above are for non-obvious semantics only, and the roster/counts are derived from the declaration table rather than copied.

Minimal scaffold (HTTP-json single-asset shape):

```ts
import type { StablecoinMeta } from "@shared/types/core";
import type { LiveReservesConfig } from "@shared/types/live-reserves";
import { parseLiveReserveAdapterParams } from "@shared/lib/live-reserve-adapters";
import type { AdapterContext, AdapterResult } from "./types";
import {
  fetchJsonWithRetry,
  freshnessMetadataFromTimestamp,
  parseTimestampLikeToUnixSeconds,
  requireJsonInput,
} from "./helpers";

interface MyAdapterPayload {
  totalReserves: number;
  updatedAt?: string;
}

export async function fetchMyAdapterReserves(
  _coin: StablecoinMeta,
  config: LiveReservesConfig,
  signal: AbortSignal,
  ctx?: AdapterContext,
): Promise<AdapterResult> {
  const input = requireJsonInput(config.inputs.primary, "my-adapter");
  const params = parseLiveReserveAdapterParams("my-adapter", config.params);
  const payload = await fetchJsonWithRetry<MyAdapterPayload>(input.url, signal, 12_000, ctx);
  const sourceTimestamp = parseTimestampLikeToUnixSeconds(payload.updatedAt);

  return {
    slices: [{ name: params.assetLabel, pct: 100, risk: params.assetRisk }],
    metadata: {
      ...freshnessMetadataFromTimestamp(sourceTimestamp, "issuer-api", "payload has no source timestamp"),
    },
  };
}
```

---

## Frontend Consumers

Reserve composition and collateralization footers separate the source/report-as-of date from the last checked time, retain a stale label for out-of-window evidence, and never label a refreshed historical attestation as a new live measurement. Attestation-mix snapshots use the proof badge with an `Attestation` label. A source-age-only degradation explains that evidence is out of date; collection failures retain their separate sync-error notice.

- `src/hooks/use-stablecoin-reserves.ts` uses mode-aware polling: `live` responses follow the 4-hour reserve producer cadence (`staleTime = 4 hours` / `refetchInterval = 8 hours`), while stale or fallback modes tighten to `1 minute` / `2 minutes` so the UI re-checks recovery faster
- `src/hooks/use-stablecoin-detail-view-model.ts` injects the reserve result into the detail-page view model
- `src/lib/coverage.ts` uses the adapter badge taxonomy in `shared/lib/live-reserve-display.ts` so `/coverage` distinguishes true `Live` reserve feeds from `Curated-Validated` and `Proof` reserve-sync paths
- `worker/src/api/status.ts` uses `computeReserveCompositionOverview()` to surface reserve-sync health on `/status`

---

## Scope Boundaries

- Live reserve sync is detail-page and status-surface infrastructure, not a replacement for curated reserve metadata everywhere else.
- [Safety Score V9](./report-cards.md) uses fresh authoritative independent live reserve slices as Backing reserve exposures when available. `dynamic-mix` adapters can qualify when the retained snapshot passes the shared admission gates, regardless of a later failed attempt; only a subset of `single-bucket` adapters carry `evidenceClass = independent`, while `validated-static` / `weak-live-probe` feeds remain detail/status data only. The compiler reconciles accepted tracked slices to effective dependency edges, and falls back to reviewed static/curated rows only when no accepted live rows were captured.
- Reviewed metadata joins to live reserve rows by unique adapter-owned `sourceKey` when present, with unique normalized-name matching retained only for unkeyed compatibility captures. Percentages never define the join, and classification plus dependency compilation share the same matched identities.
- Blacklist attribution no longer treats live reserves as invisible just because most adapters lack `coinId` links. The report-card resolver enriches both live and curated reserve names with the same blacklist clue pipeline, then resolves inherited exposure to a fixed point across the tracked set so cyclic upstream graphs do not depend on traversal order. The collateral drift alert only compares comparable live reserve mixes: snapshots must still resolve to at least two live slices after normalization, so single-bucket proofs and collapsed one-slice snapshots do not generate noisy curated-vs-live drift alerts.
- [Dependency Map](./dependency-map.md) remains authoritative for graph behavior; dependency edges come from the effective V9 dependency fact set, including accepted live reserve links when present and reviewed static/curated reserve metadata plus manual dependencies on the defined fallback path.
