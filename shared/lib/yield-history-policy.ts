/** Public yield-history window and corresponding retained history span. */
export const YIELD_HISTORY_MAX_DAYS = 365;

/** Full-fidelity hourly history retained before older points use daily close snapshots. */
export const YIELD_HISTORY_RAW_DAYS = 30;
