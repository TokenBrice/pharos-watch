name: Pharos Change Contract

on:
  pull_request:
    branches: [main]
    types: [opened, synchronize, reopened, ready_for_review]

permissions:
  contents: read
  issues: write

concurrency:
  group: pharos-change-contract-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  comment:
    if: ${{ github.event.pull_request.head.repo.full_name == github.repository }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          persist-credentials: false
          fetch-depth: 0
      - uses: ./.github/actions/setup-workspace
        with:
          install-deps: "false"
      - name: Render change contract
        env:
          BASE_SHA: ${{ github.event.pull_request.base.sha }}
          HEAD_SHA: ${{ github.event.pull_request.head.sha }}
        run: >-
          node scripts/ci/pharos-change-contract.mjs
          --base-ref "$BASE_SHA"
          --head-ref "$HEAD_SHA"
          --markdown
          > /tmp/pharos-change-contract.md
      - name: Upsert PR comment
        env:
          COMMENT_FILE: /tmp/pharos-change-contract.md
          GITHUB_TOKEN: ${{ github.token }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
        run: node scripts/ci/upsert-github-pr-comment.mjs
