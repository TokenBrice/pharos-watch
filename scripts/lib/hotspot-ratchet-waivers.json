{
  "src/lib/coverage.ts": {
    "disposition": "deferred",
    "notes": "Coverage aggregation and scenario logic are still evolving with the matrix surface. Defer decomposition until the coverage data model settles."
  },
  "src/components/stablecoin-detail/hero-card.tsx": {
    "disposition": "queued-p4",
    "notes": "The stablecoin detail hero remains a large presentation hotspot. Track it for the planned frontend decomposition lane rather than mixing it into the worker-focused tranche."
  },
  "src/app/chains/nautical-chart.tsx": {
    "disposition": "queued-p4",
    "notes": "The chains harbor scene intentionally concentrates SVG ship, lighthouse, water, and label primitives during the nautical redesign. Split ship rendering, harbor background, and scene view-model helpers in the next chains visualization cleanup pass."
  },
  "src/components/command-palette.tsx": {
    "disposition": "queued-p4",
    "notes": "The command palette still combines indexing, keyboard handling, and rendering in one shell. Keep it waived until the navigation/search UI cleanup lane is scheduled."
  },
  "worker/src/lib/fx-rate-state.ts": {
    "disposition": "deferred",
    "notes": "FX fallback state handling has many branches but a wide caller surface. Defer refactoring until the next pricing/runtime state follow-up."
  },
  "worker/src/cron/sync-stablecoins/supplemental-assets.ts": {
    "disposition": "deferred",
    "notes": "Supplemental asset intake still mixes multiple provider families. Defer further splitting until the stablecoin sync surface stabilizes after the current tranche."
  },
  "worker/src/cron/sync-stablecoins/fallback.ts": {
    "disposition": "deferred",
    "notes": "Fallback stablecoin sync still mixes fallback intake, enrichment setup, cache publication, and post-enrichment orchestration. Keep this explicitly waived for the dedicated stablecoin-sync decomposition lane rather than folding a broad refactor into the current CAS visibility fix."
  },
  "worker/src/cron/dex-discovery/crawl-sources.ts": {
    "disposition": "queued-p4",
    "notes": "DEX discovery crawling is still source-dense and branchy. Track it for a later crawler-family extraction rather than expanding the current worker tranche."
  },
  "worker/src/api/audit-depeg-history.ts": {
    "disposition": "queued-p4",
    "notes": "The audit/repair handler still mixes validation, replay planning, and mutation orchestration. Keep it explicitly owned as a later admin-path hotspot rather than widening this tranche."
  },
  "worker/src/api/mint-burn-flows.ts": {
    "disposition": "queued-p4",
    "notes": "Mint/burn flow API handling now crosses the hotspot threshold with request parsing, aggregation, formatting, and response shaping in one handler. Track it for the repo-wide strategic cleanup pass instead of folding worker API refactors into the website cleanup slice."
  },
  "worker/src/cron/blacklist/amount-recovery.ts": {
    "disposition": "queued-p4",
    "notes": "Blacklist amount recovery still mixes provider fanout, pricing fallback, event-shape branching, and batch-write orchestration in one branch-heavy module. Track it for a dedicated blacklist recovery simplification pass rather than folding that work into the current reserve/pricing/status tranche."
  },
  "src/components/status/cron-metadata-summary.ts": {
    "disposition": "deferred",
    "notes": "Status metadata summarization has grown with the ops dashboard. Defer structural cleanup until the status surface settles after the current parallel work."
  },
  "src/components/status/api-keys-panel.tsx": {
    "disposition": "queued-p4",
    "notes": "The API keys operator panel currently mixes create/edit form state, admin mutations, expiry/rate-limit validation, and token reveal rendering in one client component. Keep it explicitly waived until the next ops-UI decomposition pass splits the mutation helpers and per-key row editor out of the panel shell."
  },
  "shared/lib/format.ts": {
    "disposition": "deferred",
    "notes": "Shared formatting helpers are branchy but broadly reused. Defer any breakup until a dedicated shared-utility cleanup lane exists."
  },
  "worker/src/cron/sync-fx-rates-helpers.ts": {
    "disposition": "deferred",
    "notes": "FX-rate helper orchestration remains a large policy surface after earlier trims. Defer another split until the next FX-state/runtime cleanup tranche."
  },
  "src/app/admin/status-dashboard.tsx": {
    "disposition": "queued-p4",
    "notes": "Admin status dashboard still centralizes top-fold triage, section composition, and operator controls in one client shell. Keep waived until the dedicated admin-status decomposition lane is scheduled instead of widening the current mixed status/frontend tranche."
  },
  "shared/lib/env-contract.ts": {
    "disposition": "queued-p4",
    "notes": "The shared env-contract manifest now centralizes the binding inventory plus generated example/docs views. Keep it waived until a dedicated follow-up pass decides whether to split generation helpers from the manifest definition without regressing the single-source-of-truth contract."
  },
  "shared/lib/classification.ts": {
    "disposition": "deferred",
    "notes": "Classification now intentionally centralizes peg labels, badge styles, chart colors, and filter labels behind a canonical metadata table. Treat the file-line count as a data-table hotspot, not a behavior hotspot; revisit only if classification grows more non-data logic."
  },
  "src/lib/contagion-layout.ts": {
    "disposition": "queued-p4",
    "notes": "The contagion layout engine still combines graph shaping, simulation setup, radius heuristics, and clamp/label rules in one branchy module. Track it for the next dedicated contagion graph simplification pass rather than mixing a layout-engine breakup into the current status/pricing tranche."
  },
  "worker/src/cron/daily-digest/prompt.ts": {
    "disposition": "queued-p4",
    "notes": "Candidate-led digest prompting intentionally centralizes the editorial contract while the digest rewrite stabilizes. Split prompt policy, rubric text, and safety instructions in a follow-up digest cleanup pass."
  },
  "worker/src/cron/weekly-recap.ts": {
    "disposition": "queued-p4",
    "notes": "Weekly recap input assembly grew with week-over-week deltas, weekly signal leaderboards, and split parse-row plumbing. Keep waived until the digest prompt rewrite stabilizes; a follow-up pass should extract aggregation vs. prompt rendering into separate modules."
  },
  "worker/src/api/telegram-webhook.ts": {
    "disposition": "queued-p4",
    "notes": "Transport entrypoint grew with the /status handler and callback_query branch added in the 2026-04-17 audit remediation. The file is a thin ingress coordinator — parsing, persistence, and message templates already live in adjacent telegram-webhook-* modules. Defer further extraction until the bot surface stabilizes."
  },
  "src/app/telegram/page.tsx": {
    "disposition": "queued-p4",
    "notes": "Static product landing page grew with the How-It-Works section, DEWS bands explainer, and JSON-LD block added in the 2026-04-17 telegram bot comprehensive audit. All content is intentionally colocated. Defer decomposition until another product-page cleanup pass."
  },
  "worker/src/cron/confirm-pending-depegs.ts": {
    "disposition": "queued-p4",
    "notes": "Grew during the 2026-04-18 depeg+DEWS hardening wave with pool-status count classification, circuit-breaker guards, offchainSourceLabel tracking, and composite pending-reason parsing. The main confirmPendingDepegs function is now the obvious split candidate — per-row evaluation (primary/native/off-chain/DEX/CEX/pool collection) should extract into buildConfirmationPlan; the decision matrix should extract into evaluatePromotionDecision. Follow-up in the next /simplify pass."
  },
  "worker/src/lib/dews.ts": {
    "disposition": "queued-p4",
    "notes": "DEWS scoring and contagion policy remain intentionally concentrated while the early-warning surface is still evolving. Split signal derivation, contagion weighting, and aggregate scoring in a dedicated DEWS cleanup pass."
  },
  "worker/src/cron/detect-depegs.ts": {
    "disposition": "queued-p4",
    "notes": "Depeg detection still mixes fetch orchestration, signal arbitration, and persistence decisions in one large cron module. Keep it waived until the next depeg pipeline simplification pass."
  }
}
