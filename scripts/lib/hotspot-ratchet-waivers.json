{
  "src/components/command-palette.tsx": {
    "disposition": "queued-p4",
    "notes": "The command palette still combines indexing, keyboard handling, and rendering in one shell. Keep it waived until the navigation/search UI cleanup lane is scheduled."
  },
  "worker/src/cron/sync-stablecoins/supplemental-assets.ts": {
    "disposition": "deferred",
    "notes": "Supplemental asset intake still mixes multiple provider families. Defer further splitting until the stablecoin sync surface stabilizes after the current tranche."
  },
  "worker/src/api/audit-depeg-history.ts": {
    "disposition": "queued-p4",
    "notes": "The audit/repair handler still mixes validation, replay planning, and mutation orchestration. Keep it explicitly owned as a later admin-path hotspot rather than widening this tranche."
  },
  "worker/src/lib/authoritative-price-sources.ts": {
    "disposition": "queued-p4",
    "notes": "Authoritative price overrides centralize per-protocol live and historical readers (Cap, infiniFi, ERC-4626 NAV, Idle CDO tranche, tracked-base inheritance). The shared shape — selector + provider record + parent-trust gating — is easier to audit in one file while new vault families are still being added. Split per-protocol providers into separate modules once the catalog stabilizes."
  },
  "src/components/status/cron-metadata-summary.ts": {
    "disposition": "deferred",
    "notes": "Status metadata summarization has grown with the ops dashboard. Defer structural cleanup until the status surface settles after the current parallel work."
  },
  "src/app/admin/status-dashboard.tsx": {
    "disposition": "queued-p4",
    "notes": "Admin status dashboard still centralizes top-fold triage, section composition, and operator controls in one client shell. Keep waived until the dedicated admin-status decomposition lane is scheduled instead of widening the current mixed status/frontend tranche."
  },
  "worker/src/cron/daily-digest/prompt.ts": {
    "disposition": "queued-p4",
    "notes": "Candidate-led digest prompting intentionally centralizes the editorial contract while the digest rewrite stabilizes. Split prompt policy, rubric text, and safety instructions in a follow-up digest cleanup pass."
  },
  "worker/src/cron/weekly-recap.ts": {
    "disposition": "queued-p4",
    "notes": "Weekly recap input assembly grew with week-over-week deltas, weekly signal leaderboards, and split parse-row plumbing. Keep waived until the digest prompt rewrite stabilizes; a follow-up pass should extract aggregation vs. prompt rendering into separate modules."
  },
  "worker/src/api/telegram-webhook-callbacks.ts": {
    "disposition": "queued-p4",
    "notes": "Callback routing now owns setup, settings, status, discovery, watchlist management, snooze, and per-coin alert toggles. Keep it waived after the command dispatcher split; the next Telegram decomposition pass should extract action-specific callback handlers."
  },
  "src/app/pharoswatchbot/page.tsx": {
    "disposition": "queued-p4",
    "notes": "Static product landing page grew with the How-It-Works section, DEWS bands explainer, and JSON-LD block added in the 2026-04-17 telegram bot comprehensive audit. All content is intentionally colocated. Defer decomposition until another product-page cleanup pass."
  },
  "worker/src/lib/telegram-alerts.ts": {
    "disposition": "queued-p4",
    "notes": "Telegram alert policy now centralizes preset routing, delivery formatting, and subscription-aware branch handling for the dynamic preset rollout. Defer splitting preset selection, payload shaping, and notification text helpers until the bot command surface stabilizes."
  },
  "worker/src/cron/confirm-pending-depegs.ts": {
    "disposition": "queued-p4",
    "notes": "Grew during the 2026-04-18 depeg+DEWS hardening wave with pool-status count classification, circuit-breaker guards, offchainSourceLabel tracking, and composite pending-reason parsing. The main confirmPendingDepegs function is now the obvious split candidate — per-row evaluation (primary/native/off-chain/DEX/CEX/pool collection) should extract into buildConfirmationPlan; the decision matrix should extract into evaluatePromotionDecision. Follow-up in the next /simplify pass."
  },
  "worker/src/cron/blacklist/amount-recovery.ts": {
    "disposition": "queued-p4",
    "notes": "Blacklist amount recovery is an existing cron repair surface with chain-specific parsing and persistence branches. Keep waived here; split chain fetch, event normalization, and balance reconciliation in a dedicated blacklist cron cleanup."
  },
  "worker/src/cron/yield-sync/cache.ts": {
    "disposition": "queued-p4",
    "notes": "Surfaced after merging the local yield coverage and publication-health tranche. Yield cache validation now covers rankings publication state, benchmark caches, supplemental source caches, and deterministic on-chain health; split schema parsing by cache family in a dedicated yield cleanup pass."
  },
  "worker/src/cron/sync-stablecoins/enrich-prices-primary-provider-collection.ts": {
    "disposition": "queued-p4",
    "notes": "Surfaced after the larger pricing-provider shells were split. This module still combines provider target collection, cache gates, and diagnostic shaping; split target selection from provider execution in a follow-up pricing cleanup."
  },
  "worker/src/cron/sync-fx-rates-helpers.ts": {
    "disposition": "queued-p4",
    "notes": "Surfaced after the stablecoin pricing hotspots dropped out of the candidate set. FX helper policy still combines source parsing, freshness decisions, and fallback diagnostics; split provider-specific helpers in a dedicated FX cleanup pass."
  },
  "src/components/pre-launch-detail.tsx": {
    "disposition": "queued-p4",
    "notes": "Surfaced after frontend hotspot reductions. The pre-launch detail page still combines hero, readiness, source, and status rendering in one component; split page sections once the pre-launch surface changes again."
  },
  "src/app/yield/client.tsx": {
    "disposition": "queued-p4",
    "notes": "Surfaced after Phase 6 reduced larger hotspots. The yield client still combines filter state, table shaping, and interaction handling in one route shell; split view-model and table controls in the next yield UI cleanup pass."
  },
  "src/app/compare/client.tsx": {
    "disposition": "queued-p4",
    "notes": "Surfaced after stablecoin detail and chains reductions. The compare client still owns selection state, URL wiring, metrics, and table rendering; split compare state/model helpers before expanding the comparison surface."
  },
  "shared/lib/redemption-backstop-configs/validation.ts": {
    "disposition": "queued-p4",
    "notes": "Redemption registry validation currently centralizes schema checks, source-module AST auditing, documentation support ratchets, policy rows, and coverage summaries. Keep waived for the v4 validation tranche; split source-module AST scanning and policy validation into helper modules in the next redemption-maintenance pass."
  },
  "worker/src/lib/redemption-backstop-capacity.ts": {
    "disposition": "queued-p4",
    "notes": "Redemption capacity resolution intentionally centralizes supply-full, supply-ratio, fixed-USD, and reserve-sync metadata semantics while v4 current-capacity behavior is still being audited. Split capacity profile construction from per-model resolution once the v4 route coverage expansion settles."
  }
}
