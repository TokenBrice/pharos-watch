{
  "name": "stablecoin-api",
  "private": true,
  "license": "MIT",
  "engines": {
    "node": ">=24 <25"
  },
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "node ../scripts/guard-worker-deploy.mjs",
    "typecheck": "tsc --noEmit",
    "typecheck:scripts": "tsc --noEmit -p tsconfig.scripts.json"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "4.20260507.1",
    "typescript": "~5.9.0",
    "wrangler": "4.90.0"
  },
  "dependencies": {
    "@cf-wasm/resvg": "^0.3.4",
    "satori": "^0.26.0",
    "viem": "2.48.11"
  }
}
